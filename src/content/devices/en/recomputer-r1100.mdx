---
title: reComputer R1100
description: 'Connect the Seeed Studio reComputer R1100 gateway to ThingsBoard via LoRaWAN / Ethernet. IoT integration for smart energy.'
platforms:
  - CE
  - Cloud
  - Cloud EU
  - Edge
  - Edge PE
  - PE
hasBranching: true
vendor: Seeed Studio
deviceImageFileName: recomputer-r1100.webp
hardwareType: Gateways
connectivity:
  - LoRaWAN
  - Ethernet
  - WIFI
  - Bluetooth
  - 4G
  - Zigbee
industry:
  - Industrial Manufacturing
  - Energy Management
  - Smart Buildings
useCase:
  - Smart energy
  - Environment Monitoring
  - Smart Office
  - Smart Retail
  - Smart Farming
  - Fleet Tracking
  - Health Care
  - Air Quality Monitoring
  - Waste Management
  - Tank Level Monitoring
chip: R1100
category: Other devices
---

import PlatformContent from '~/components/DeviceLibrary/PlatformContent.astro';
import ImageGallery from '~/components/ImageGallery.astro';
import DocImage from '~/components/DocImage.astro';
import { Aside, Tabs, TabItem, Steps } from '@astrojs/starlight/components';

<PlatformContent variant="ce">

## Introduction

The <a href="https://www.seeedstudio.com/blog/2024/11/20/just-launched-recomputer-r11-a%EF%BC%84179-edge-iot-gateway-controller-could-be-the-most-adapatble-and-cost-effective-edge-iot-gateway-controller-in-your-equipment-cabinets/?srsltid=AfmBOopWn625P_n59sPhbUJavUGYus7l3ah1NTd0-90w51unAIVVTgRp" target="_blank" rel="noopener noreferrer">reComputer R1100</a>, powered by Raspberry Pi CM4, is an adaptable edge IoT gateway with AI capabilities.
It features comprehensive industrial interfaces (2x Ethernet, 2xUSB, 2xRS485, 2xRS232, 2xDI and 2xDO) and flexible wireless connectivity options (4G, LoRa®, Wi-Fi/BLE), making it ideal for diverse industrial applications

It has extensive applications in the IoT field. It can be used in aspects such as data acquisition and process monitoring, automation and robot control, intelligent manufacturing, and industrial communication and networking.
With its small size, flexibility, low cost, and programmability, it provides strong support for automation & IoT system and more.

## Integration with ThingsBoard Edge

### Prerequisites

To continue with this guide, we will require the following:
* reComputer R1100
* Modbus Controller (in our case, <a href="https://www.siemens.com/ua/uk/produkty/avtomatyzatsiya-promyslovosti/systemy-avtomatyzatsiyi/systemy-promyslovoyi-avtomatyzatsiyi-simatic/plc-kontrolery-simatic/lohichnyy-modul-logo.html" target="_blank" rel="noopener noreferrer">Siemens LOGO!</a>)
* <a href="https://docs.docker.com/engine/install/" target="_blank" rel="noopener noreferrer">Docker</a> installed
* **ThingsBoard Community Edition** <a href="/docs/user-guide/install/installation-options/" target="_blank" rel="noopener noreferrer">server</a>.

### Starting Edge on the reComputer R1100



<Steps>

1. Log in to the **ThingsBoard Community Edition instance** and go to the **Edge Management > Instances** section. Click the **“+”** icon in the top right corner and select the **“Add new edge”** option.
2. Enter a name for your Edge in the **“Name”** field and click the **“Add”** button to confirm the addition of your new Edge.
3. Your new Edge should now appear at the top of the list, as entries are sorted by creation time by default.

</Steps>

<ImageGallery images={[{"src":"/images/edge/user-guide/edge-install/1-instance-section.webp","alt":"Log in to the **ThingsBoard Community Edition instance** and go to the **Edge Management > Instances** section. Click the **“+”** icon in the top right corner and select the **“Add new edge”** option.","caption":"Log in to the **ThingsBoard Community Edition instance** and go to the **Edge Management > Instances** section. Click the **“+”** icon in the top right corner and select the **“Add new edge”** option."},{"src":"/images/edge/user-guide/edge-install/2-add-new-edge.webp","alt":"Enter a name for your Edge in the **“Name”** field and click the **“Add”** button to confirm the addition of your new Edge.","caption":"Enter a name for your Edge in the **“Name”** field and click the **“Add”** button to confirm the addition of your new Edge."},{"src":"/images/edge/user-guide/edge-install/3-done.webp","alt":"Your new Edge should now appear at the top of the list, as entries are sorted by creation time by default.","caption":"Your new Edge should now appear at the top of the list, as entries are sorted by creation time by default."}]} />

Proceed with the installation of the **Edge instance** on the **reComputer R1100**:

* To initiate an **SSH (Secure Shell)** connection to the **reComputer R1100**, open the terminal and execute the following command:

```bash
ssh recomputer@ip_address #Enter the actual IP address
```

**ip_address:** The IP address of the reComputer R1100. Enter the actual IP address instead of _ip_address_.<br />
**Password:** Terminal requests the password. _The **default password** for the reComputer R1100 is: 12345678_

* Once connected, you can follow the installation instructions below. Start by creating a new directory:

```bash
mkdir tb_edge
```

* Open this directory:

```bash
cd /home/recomputer/tb_edge
```

Create a docker compose file for the **ThingsBoard Edge** service within this directory:

```bash
nano docker-compose.yml
```

Then, you need to fill this file with the configuration lines.
Once the **Edge** instance has been created, the preset installation instructions will be available for use.
They contain important credentials, such as **Edge Secret**, **Edge Key**, etc. To access these configurations:



<Steps>

1. Go to the **Edge management > Instances** section of your **ThingsBoard Community Edition** instance, and click on the **Instance**. Then, click the **"Install & Connect Instructions"** button.
2. On the **"Install & Connect Instructions"** pop-up window, select the **"Docker"** tab and **copy the configuration lines**.

</Steps>

<ImageGallery images={[{"src":"/images/edge/user-guide/edge-install/4-install-instructions.webp","alt":"Go to the **Edge management > Instances** section of your **ThingsBoard Community Edition** instance, and click on the **Instance**. Then, click the **\"Install & Connect Instructions\"** button.","caption":"Go to the **Edge management > Instances** section of your **ThingsBoard Community Edition** instance, and click on the **Instance**. Then, click the **\"Install & Connect Instructions\"** button."},{"src":"/images/edge/user-guide/edge-install/5-docker.webp","alt":"On the **\"Install & Connect Instructions\"** pop-up window, select the **\"Docker\"** tab and **copy the configuration lines**.","caption":"On the **\"Install & Connect Instructions\"** pop-up window, select the **\"Docker\"** tab and **copy the configuration lines**."}]} />

<Steps>

1. Paste the copied lines into the **docker-compose.yml** file and press **CTRL+S** to save it. To close the file press **CTRL+X**.

2. Execute the following commands:

</Steps>

```bash
docker compose up -d
docker compose logs -f mytbedge
```

<Aside type="note">

The command must be executed in the **same directory** in which the docker-compose.yml file was saved.

</Aside>

* To set up a local port forwarding via SSH, open **another terminal tab** and execute the following command:

```bash
ssh -N -L 8080:127.0.0.1:8080 recomputer@ip_address #Enter the actual IP address
```

Any connection to **localhost:8080** on your local machine will be forwarded to **127.0.0.1:8080** on the **reComputer R1100**.

* The **ThingsBoard Edge** instance is available at <a href="http://127.0.0.1:8080" target="_blank" rel="noopener noreferrer">**http://127.0.0.1:8080**</a>. Use your credentials to log in.

### Provisioning the ThingsBoard IoT Gateway on Edge

To deploy the **ThingsBoard Gateway**:



<Steps>

1. Log in to the **ThingsBoard Community Edition** instance and go to the **Edge Management > Instances** section, then click the **“Manage dashboards”** button.
2. On the **“Edge Dashboards”** page, click the **“+”** icon to assign the **“ThingsBoard IoT Gateways”** dashboard to the Edge instance. Click the **“Assign”** button. The **"ThingsBoard IoT Gateways"** dashboard is one of the pre-created, out-of-the-box dashboards available.

</Steps>

<ImageGallery images={[{"src":"/images/edge/user-guide/edge-install/6-instance-section-manage-dashboard.webp","alt":"Log in to the **ThingsBoard Community Edition** instance and go to the **Edge Management > Instances** section, then click the **“Manage dashboards”** button.","caption":"Log in to the **ThingsBoard Community Edition** instance and go to the **Edge Management > Instances** section, then click the **“Manage dashboards”** button."},{"src":"/images/edge/user-guide/edge-install/7-assign.webp","alt":"On the **“Edge Dashboards”** page, click the **“+”** icon to assign the **“ThingsBoard IoT Gateways”** dashboard to the Edge instance. Click the **“Assign”** button. The **\"ThingsBoard IoT Gateways\"** dashboard is one of the pre-created, out-of-the-box dashboards available.","caption":"On the **“Edge Dashboards”** page, click the **“+”** icon to assign the **“ThingsBoard IoT Gateways”** dashboard to the Edge instance. Click the **“Assign”** button. The **\"ThingsBoard IoT Gateways\"** dashboard is one of the pre-created, out-of-the-box dashboards available."}]} />



<Steps>

1. Open your **Edge instance**, navigate to the **Dashboards** section and open the **“ThingsBoard IoT Gateways”** dashboard.
2. Click the **“+”** icon in the upper right corner to add a new gateway. Enter the gateway name in the **“Name”** field, and select the **“default”** device profile. Click the **“Create”** button.

</Steps>

<ImageGallery images={[{"src":"/images/edge/user-guide/edge-install/8-dashboards-on-edge.webp","alt":"Open your **Edge instance**, navigate to the **Dashboards** section and open the **“ThingsBoard IoT Gateways”** dashboard.","caption":"Open your **Edge instance**, navigate to the **Dashboards** section and open the **“ThingsBoard IoT Gateways”** dashboard."},{"src":"/images/edge/user-guide/edge-install/9-add-gw.webp","alt":"Click the **“+”** icon in the upper right corner to add a new gateway. Enter the gateway name in the **“Name”** field, and select the **“default”** device profile. Click the **“Create”** button.","caption":"Click the **“+”** icon in the upper right corner to add a new gateway. Enter the gateway name in the **“Name”** field, and select the **“default”** device profile. Click the **“Create”** button."}]} />

## Configuring the ModBus Connector

The new **IoT Gateway device** will be featured at the top of the **“ThingsBoard IoT Gateways”** dashboard list, allowing us to add a **ModBus Connector**. For example, we can use the ModBus Connector to fetch temperature data from the to fetch temperature data from the [Siemens LOGO!]() device:



<Steps>

1. On the **“ThingsBoard IoT Gateways”** dashboards page, click the **“Connectors”** button.
2. To add a **Connector**, click the **“+”** button. In the pop-up window, select the **MODBUS** option in the **“Type”** field and enter the  name of the connector in the **“Name”** field. Click the **“Add”** button to proceed.
3. To make further adjustments, click on the newly added **Connector** and select the **“Advanced”** tab and the **“Configuration”** sub-tab on the right side of the **"MODBUS Configuration"** screen.

</Steps>

<ImageGallery images={[{"src":"/src/assets/devices-library/edge/recomputer-r1100/10-connector.webp","alt":"On the **“ThingsBoard IoT Gateways”** dashboards page, click the **“Connectors”** button.","caption":"On the **“ThingsBoard IoT Gateways”** dashboards page, click the **“Connectors”** button."},{"src":"/src/assets/devices-library/edge/recomputer-r1000/6-add-connector.webp","alt":"To add a **Connector**, click the **“+”** button. In the pop-up window, select the **MODBUS** option in the **“Type”** field and enter the  name of the connector in the **“Name”** field. Click the **“Add”** button to proceed.","caption":"To add a **Connector**, click the **“+”** button. In the pop-up window, select the **MODBUS** option in the **“Type”** field and enter the  name of the connector in the **“Name”** field. Click the **“Add”** button to proceed."},{"src":"/src/assets/devices-library/edge/recomputer-r1000/7-advanced-config.webp","alt":"To make further adjustments, click on the newly added **Connector** and select the **“Advanced”** tab and the **“Configuration”** sub-tab on the right side of the **\"MODBUS Configuration\"** screen.","caption":"To make further adjustments, click on the newly added **Connector** and select the **“Advanced”** tab and the **“Configuration”** sub-tab on the right side of the **\"MODBUS Configuration\"** screen."}]} />

* Then, please insert the following configuration code. Then, click the **“Save”** button.

```cpp maxLines=15 collapsible
{
  "master": {
    "slaves": [
      {
        "host": "192.168.1.200",
        "port": 510,
        "type": "tcp",
        "method": "socket",
        "timeout": 35,
        "byteOrder": "BIG",
        "wordOrder": "BIG",
        "retries": true,
        "retryOnEmpty": true,
        "retryOnInvalid": true,
        "pollPeriod": 3000,
        "unitId": 1,
        "deviceName": "LOGO!",
        "deviceType": "siemens",
        "connectAttemptTimeMs": 5000,
        "connectAttemptCount": 5,
        "waitAfterFailedAttemptsMs": 300000,
        "attributes": [],
        "timeseries": [
          {
            "tag": "temp",
            "type": "16int",
            "functionCode": 4,
            "objectsCount": 1,
            "address": 2,
            "divider": 10
          },
          {
            "tag": "Q1",
            "type": "bit",
            "functionCode": 1,
            "objectsCount": 1,
            "address": 8193
          },
          {
            "tag": "Q2",
            "type": "16int",
            "functionCode": 3,
            "objectsCount": 2,
            "address": 507
          }
        ],
        "rpc": [
          {
            "tag": "getTemp",
            "type": "16int",
            "functionCode": 4,
            "objectsCount": 1,
            "address": 2
          }
        ],
        "attributeUpdates": [
          {
            "tag": "randNumber",
            "type": "16int",
            "functionCode": 6,
            "objectsCount": 1,
            "address": 507
          }
        ],
        "reportStrategy": {
          "type": "ON_REPORT_PERIOD",
          "reportPeriod": 3000
        }
      }
    ]
  },
  "slave": {}
}
```

## Installing the ThingsBoard IoT Gateway on the reComputer R1100

Start installation of the **IoT Gateway** on the **reComputer R1100**:



<Steps>

1. Go back to the **“ThingsBoard IoT Gateways”** dashboard page and click on the newly added **Gateway device** (reComputer R1100).
2. On the **Gateway device** page, click the **“Launch command”** button and download **docker-compose.yml** for your gateway.

</Steps>

<ImageGallery images={[{"src":"/src/assets/devices-library/edge/recomputer-r1100/11-gw-list.webp","alt":"Go back to the **“ThingsBoard IoT Gateways”** dashboard page and click on the newly added **Gateway device** (reComputer R1100).","caption":"Go back to the **“ThingsBoard IoT Gateways”** dashboard page and click on the newly added **Gateway device** (reComputer R1100)."},{"src":"/src/assets/devices-library/edge/recomputer-r1100/12-launch-command.webp","alt":"On the **Gateway device** page, click the **“Launch command”** button and download **docker-compose.yml** for your gateway.","caption":"On the **Gateway device** page, click the **“Launch command”** button and download **docker-compose.yml** for your gateway."}]} />

* Then, open another tab in the terminal and initiate the **SSH** connection to the **reComputer R1100**:

```bash
ssh recomputer@ip_address
```

**ip_address:** The IP address of the reComputer R1100. Enter the actual IP address instead of _ip_address_.<br />
**Password:** Terminal requests the password. _The **default password** for the reComputer R1100 is: 12345678_

* Create the directory for the Gateway service:

```bash
mkdir gateway_service
```

* Open this directory:

```bash
cd /home/recomputer/gateway_service
```

* Create the docker compose file for your **IoT Gateway**:

```bash
nano docker-compose.yml
```

<Steps>

1. Copy the content of the previously downloaded **docker-compose.yml** file, and paste it into the new one via the terminal:

   <DocImage src="/src/assets/devices-library/edge/recomputer-r1100/copy-paste-configs.webp" alt="image" />

2. Save the **docker-compose.yml** file and press **CTRL+S** to save it. To close the file press **CTRL+X**.

3. Start the **Gateway** by executing the following command in the terminal:

</Steps>

```bash
docker compose up -d
docker compose logs -f
```

<Aside type="note">

The command must be executed in the same directory in which the docker-compose.yml file was saved.

</Aside>

## Visualize Incoming Data with the Dashboard

<DocImage src="/src/assets/devices-library/edge/recomputer-r1100/r1100-logo.webp" alt="image" />
Once the **ThingsBoard Edge** and **IoT Gateway** are running on the **Computer R1100** and the **ModBus connector** transfers data, you can visualize it on the **Dashboard** on your **Edge instance**:



<Steps>

1. Go to the **Dashboards** section, click the **“+”** icon and select **"Create new dashboard"** option.
2. In the pop-up window, enter the dashboard **title**. Other fields are optional. Click the **"Add"** button to proceed.
3. Once you have created the dashboard, it will be automatically opened. Click the **"Add widget"** button and select the widget you require. For example, open the **“Charts”** widget bundle and elect the **Line chart**.
4. In the **"Add Widget"** pop-up window, select the **Device** (in our case, Siemens LOGO!) as the **Datasource**. Please verify that the **Series Key** is the same as the one entered in the **Advanced Configuration** settings of the connector. As an example, we have utilized the designation **"temp"**. Consequently, the series key must be **"temp"** as well. Click the **“Add”** button..
5. You will now be able to see the real-time data on the dashboard.

</Steps>

<ImageGallery images={[{"src":"/src/assets/devices-library/edge/recomputer-r1000/11-create-new-dashboard.webp","alt":"Go to the **Dashboards** section, click the **“+”** icon and select **\"Create new dashboard\"** option.","caption":"Go to the **Dashboards** section, click the **“+”** icon and select **\"Create new dashboard\"** option."},{"src":"/src/assets/devices-library/edge/recomputer-r1000/12-enter-name.webp","alt":"In the pop-up window, enter the dashboard **title**. Other fields are optional. Click the **\"Add\"** button to proceed.","caption":"In the pop-up window, enter the dashboard **title**. Other fields are optional. Click the **\"Add\"** button to proceed."},{"src":"/src/assets/devices-library/edge/recomputer-r1000/13-configure-dashboard.webp","alt":"Once you have created the dashboard, it will be automatically opened. Click the **\"Add widget\"** button and select the widget you require. For example, open the **“Charts”** widget bundle and elect the **Line chart**.","caption":"Once you have created the dashboard, it will be automatically opened. Click the **\"Add widget\"** button and select the widget you require. For example, open the **“Charts”** widget bundle and elect the **Line chart**."},{"src":"/src/assets/devices-library/edge/recomputer-r1000/14-widget-configuration.webp","alt":"In the **\"Add Widget\"** pop-up window, select the **Device** (in our case, Siemens LOGO!) as the **Datasource**. Please verify that the **Series Key** is the same as the one entered in the **Advanced Configuration** settings of the connector. As an example, we have utilized the designation **\"temp\"**. Consequently, the series key must be **\"temp\"** as well. Click the **“Add”** button..","caption":"In the **\"Add Widget\"** pop-up window, select the **Device** (in our case, Siemens LOGO!) as the **Datasource**. Please verify that the **Series Key** is the same as the one entered in the **Advanced Configuration** settings of the connector. As an example, we have utilized the designation **\"temp\"**. Consequently, the series key must be **\"temp\"** as well. Click the **“Add”** button.."},{"src":"/src/assets/devices-library/edge/recomputer-r1000/15-dashboard.webp","alt":"You will now be able to see the real-time data on the dashboard.","caption":"You will now be able to see the real-time data on the dashboard."}]} />

</PlatformContent>

<PlatformContent variant="pe">

## Introduction

The <a href="https://www.seeedstudio.com/blog/2024/11/20/just-launched-recomputer-r11-a%EF%BC%84179-edge-iot-gateway-controller-could-be-the-most-adapatble-and-cost-effective-edge-iot-gateway-controller-in-your-equipment-cabinets/?srsltid=AfmBOopWn625P_n59sPhbUJavUGYus7l3ah1NTd0-90w51unAIVVTgRp" target="_blank" rel="noopener noreferrer">reComputer R1100</a>, powered by Raspberry Pi CM4, is an adaptable edge IoT gateway with AI capabilities.
It features comprehensive industrial interfaces (2x Ethernet, 2xUSB, 2xRS485, 2xRS232, 2xDI and 2xDO) and flexible wireless connectivity options (4G, LoRa®, Wi-Fi/BLE), making it ideal for diverse industrial applications

It has extensive applications in the IoT field. It can be used in aspects such as data acquisition and process monitoring, automation and robot control, intelligent manufacturing, and industrial communication and networking.
With its small size, flexibility, low cost, and programmability, it provides strong support for automation & IoT system and more.

## Integration with ThingsBoard Edge

### Prerequisites

<Tabs>
<TabItem label="Professional Edition<br /><small>(North America)</small>">

To continue with this guide, we will require the following:
* reComputer R1100
* Modbus Controller (in our case, <a href="https://www.siemens.com/ua/uk/produkty/avtomatyzatsiya-promyslovosti/systemy-avtomatyzatsiyi/systemy-promyslovoyi-avtomatyzatsiyi-simatic/plc-kontrolery-simatic/lohichnyy-modul-logo.html" target="_blank" rel="noopener noreferrer">Siemens LOGO!</a>)
* <a href="https://docs.docker.com/engine/install/" target="_blank" rel="noopener noreferrer">Docker</a> installed
* <a href="https://thingsboard.cloud/signup" target="_blank" rel="noopener noreferrer">ThingsBoard Cloud (North America)</a> or the **ThingsBoard Community Edition** <a href="/docs/user-guide/install/installation-options/" target="_blank" rel="noopener noreferrer">local server</a> as an alternative.

</TabItem>
<TabItem label="Professional Edition<br /><small>(Europe)</small>">

To continue with this guide, we will require the following:
* reComputer R1100
* Modbus Controller (in our case, <a href="https://www.siemens.com/ua/uk/produkty/avtomatyzatsiya-promyslovosti/systemy-avtomatyzatsiyi/systemy-promyslovoyi-avtomatyzatsiyi-simatic/plc-kontrolery-simatic/lohichnyy-modul-logo.html" target="_blank" rel="noopener noreferrer">Siemens LOGO!</a>)
* <a href="https://docs.docker.com/engine/install/" target="_blank" rel="noopener noreferrer">Docker</a> installed
* <a href="https://eu.thingsboard.cloud/signup" target="_blank" rel="noopener noreferrer">ThingsBoard Cloud (Europe)</a> or the **ThingsBoard Community Edition** <a href="/docs/user-guide/install/installation-options/" target="_blank" rel="noopener noreferrer">local server</a> as an alternative.

</TabItem>
</Tabs>

### Starting Edge on the reComputer R1100


<Steps>

1. Log in to the **ThingsBoard Professional Edition instance** and go to the **Edge Management > Instances** section. Click the **“+”** icon in the top right corner and select the **“Add new edge”** option.
2. Enter a name for your Edge in the **“Name”** field and click the **“Add”** button to confirm the addition of your new Edge.
3. Your new edge should now appear at the top of the list, as entries are sorted by creation time by default.

</Steps>

<ImageGallery images={[{"src":"/images/edge/user-guide/edge-install/1-instance-section-pe.webp","alt":"Log in to the **ThingsBoard Professional Edition instance** and go to the **Edge Management > Instances** section. Click the **“+”** icon in the top right corner and select the **“Add new edge”** option.","caption":"Log in to the **ThingsBoard Professional Edition instance** and go to the **Edge Management > Instances** section. Click the **“+”** icon in the top right corner and select the **“Add new edge”** option."},{"src":"/images/edge/user-guide/edge-install/2-add-new-edge-pe.webp","alt":"Enter a name for your Edge in the **“Name”** field and click the **“Add”** button to confirm the addition of your new Edge.","caption":"Enter a name for your Edge in the **“Name”** field and click the **“Add”** button to confirm the addition of your new Edge."},{"src":"/images/edge/user-guide/edge-install/3-done-pe.webp","alt":"Your new edge should now appear at the top of the list, as entries are sorted by creation time by default.","caption":"Your new edge should now appear at the top of the list, as entries are sorted by creation time by default."}]} />

Proceed with the installation of the **Edge instance** on the **reComputer R1100**:

* To initiate an **SSH (Secure Shell)** connection to the **reComputer R1100**, open the terminal and execute the following command:

```bash
ssh recomputer@ip_address #Enter the actual IP address
```

**ip_address:** The IP address of the reComputer R1100. Enter the actual IP address instead of _ip_address_.<br />
**Password:** Terminal requests the password. _The **default password** for the reComputer R1100 is: 12345678_

* Once connected, you can follow the installation instructions below. Start by creating a new directory:

```bash
mkdir tb_edge
```

* Open this directory:

```bash
cd /home/recomputer/tb_edge
```

Create a docker compose file for the **ThingsBoard Edge** service within this directory:

```bash
nano docker-compose.yml
```

Then, you need to fill this file with the configuration lines.
Once the **Edge** instance has been created, the preset installation instructions will be available for use.
They contain important credentials, such as **Edge Secret**, **Edge Key**, etc. To access these configurations:


<Steps>

1. Go to the **Edge management > Instances** section of your **ThingsBoard Professional Edition** instance, and click on the **Instance**. Then, click the **"Install & Connect Instructions"** button.
2. On the **"Install & Connect Instructions"** pop-up window, select the **"Docker"** tab and **copy the configuration lines**.

</Steps>

<ImageGallery images={[{"src":"/images/edge/user-guide/edge-install/4-install-instructions-pe.webp","alt":"Go to the **Edge management > Instances** section of your **ThingsBoard Professional Edition** instance, and click on the **Instance**. Then, click the **\"Install & Connect Instructions\"** button.","caption":"Go to the **Edge management > Instances** section of your **ThingsBoard Professional Edition** instance, and click on the **Instance**. Then, click the **\"Install & Connect Instructions\"** button."},{"src":"/images/edge/user-guide/edge-install/5-docker-pe.webp","alt":"On the **\"Install & Connect Instructions\"** pop-up window, select the **\"Docker\"** tab and **copy the configuration lines**.","caption":"On the **\"Install & Connect Instructions\"** pop-up window, select the **\"Docker\"** tab and **copy the configuration lines**."}]} />

<Steps>

1. Paste the copied lines into the **docker-compose.yml** file and press **CTRL+S** to save it. To close the file press **CTRL+X**.

2. Execute the following commands:

</Steps>

```bash
docker compose up -d
docker compose logs -f mytbedge
```

<Aside type="note">

The command must be executed in the **same directory** in which the docker-compose.yml file was saved.

</Aside>

* To set up a local port forwarding via SSH, open **another terminal tab** and execute the following command:

```bash
ssh -N -L 8080:127.0.0.1:8080 recomputer@ip_address #Enter the actual IP address
```

Any connection to **localhost:8080** on your local machine will be forwarded to **127.0.0.1:8080** on the **reComputer R1100**.

* The **ThingsBoard Edge** instance is available at <a href="http://127.0.0.1:8080" target="_blank" rel="noopener noreferrer">**http://127.0.0.1:8080**</a>. Use your credentials to log in.

### Provisioning the ThingsBoard IoT Gateway on Edge

To deploy the **ThingsBoard Gateway**:


<Steps>

1. Log in to the **ThingsBoard Professional Edition** instance and go to the **Dashboards** section and select the **"Group"** tab. Click the **"+"** icon to add a new group. In the **"Add entity group"** pop-up window, enter the group name in the **"Name"** field and click the **"Add"** button.
2. Then, select the **"All"** tab and find the **"ThingsBoard IoT Gateways"** dashboard. The **"ThingsBoard IoT Gateways"** dashboard is one of the pre-created, out-of-the-box dashboards available. Click the **"Dashboard details"** button.
3. On the **"Dashboard details"** page click the **"Manage owner and groups"** button.
4. In the **"Manage owner and groups"** pop-up window, select the newly created group from the **"Groups"** drop-down menu. Click the **"Update"** button.
5. Navigate to the **Edge Management > Instances** section, then click the **“Manage edge dashboard groups”** button.
6. On the **“Dashboard groups”** page, click the **“+”** icon to assign the newly created group to the Edge instance. Click the **“Assign”** button. The group and all dashboard within it will be assigned to the Edge instance.

</Steps>

<ImageGallery images={[{"src":"/images/edge/user-guide/edge-install/6-dashboard-group-pe.webp","alt":"Log in to the **ThingsBoard Professional Edition** instance and go to the **Dashboards** section and select the **\"Group\"** tab. Click the **\"+\"** icon to add a new group. In the **\"Add entity group\"** pop-up window, enter the group name in the **\"Name\"** field and click the **\"Add\"** button.","caption":"Log in to the **ThingsBoard Professional Edition** instance and go to the **Dashboards** section and select the **\"Group\"** tab. Click the **\"+\"** icon to add a new group. In the **\"Add entity group\"** pop-up window, enter the group name in the **\"Name\"** field and click the **\"Add\"** button."},{"src":"/images/edge/user-guide/edge-install/7-all-dashboards-pe.webp","alt":"Then, select the **\"All\"** tab and find the **\"ThingsBoard IoT Gateways\"** dashboard. The **\"ThingsBoard IoT Gateways\"** dashboard is one of the pre-created, out-of-the-box dashboards available. Click the **\"Dashboard details\"** button.","caption":"Then, select the **\"All\"** tab and find the **\"ThingsBoard IoT Gateways\"** dashboard. The **\"ThingsBoard IoT Gateways\"** dashboard is one of the pre-created, out-of-the-box dashboards available. Click the **\"Dashboard details\"** button."},{"src":"/images/edge/user-guide/edge-install/8-manage-owner-and-groups-pe.webp","alt":"On the **\"Dashboard details\"** page click the **\"Manage owner and groups\"** button.","caption":"On the **\"Dashboard details\"** page click the **\"Manage owner and groups\"** button."},{"src":"/images/edge/user-guide/edge-install/9-add-group-pe.webp","alt":"In the **\"Manage owner and groups\"** pop-up window, select the newly created group from the **\"Groups\"** drop-down menu. Click the **\"Update\"** button.","caption":"In the **\"Manage owner and groups\"** pop-up window, select the newly created group from the **\"Groups\"** drop-down menu. Click the **\"Update\"** button."},{"src":"/images/edge/user-guide/edge-install/10-instance-secton-manage-dashboard-pe.webp","alt":"Navigate to the **Edge Management > Instances** section, then click the **“Manage edge dashboard groups”** button.","caption":"Navigate to the **Edge Management > Instances** section, then click the **“Manage edge dashboard groups”** button."},{"src":"/images/edge/user-guide/edge-install/11-assign-pe.webp","alt":"On the **“Dashboard groups”** page, click the **“+”** icon to assign the newly created group to the Edge instance. Click the **“Assign”** button. The group and all dashboard within it will be assigned to the Edge instance.","caption":"On the **“Dashboard groups”** page, click the **“+”** icon to assign the newly created group to the Edge instance. Click the **“Assign”** button. The group and all dashboard within it will be assigned to the Edge instance."}]} />


<Steps>

1. Open your **Edge instance**, navigate to the **Dashboards** section and open the **“ThingsBoard IoT Gateways”** dashboard.
2. Click the **“+”** icon in the upper right corner to add a new gateway. Enter the gateway name in the **“Name”** field, and select the **“default”** device profile. Click the **“Create”** button.

</Steps>

<ImageGallery images={[{"src":"/images/edge/user-guide/edge-install/12-dashboards-on-edge-pe.webp","alt":"Open your **Edge instance**, navigate to the **Dashboards** section and open the **“ThingsBoard IoT Gateways”** dashboard.","caption":"Open your **Edge instance**, navigate to the **Dashboards** section and open the **“ThingsBoard IoT Gateways”** dashboard."},{"src":"/images/edge/user-guide/edge-install/13-add-gw-pe.webp","alt":"Click the **“+”** icon in the upper right corner to add a new gateway. Enter the gateway name in the **“Name”** field, and select the **“default”** device profile. Click the **“Create”** button.","caption":"Click the **“+”** icon in the upper right corner to add a new gateway. Enter the gateway name in the **“Name”** field, and select the **“default”** device profile. Click the **“Create”** button."}]} />

## Configuring the ModBus Connector

The new **IoT Gateway device** will be featured at the top of the **“ThingsBoard IoT Gateways”** dashboard list, allowing us to add a **ModBus Connector**. For example, we can use the ModBus Connector to fetch temperature data from the to fetch temperature data from the [Siemens LOGO!]() device:


<Steps>

1. On the **“ThingsBoard IoT Gateways”** dashboards page, click the **“Connectors”** button.
2. To add a **Connector**, click the **“+”** button. In the pop-up window, select the **MODBUS** option in the **“Type”** field and enter the  name of the connector in the **“Name”** field. Click the **“Add”** button to proceed.
3. To make further adjustments, click on the newly added **Connector** and select the **“Advanced”** tab and the **“Configuration”** sub-tab on the right side of the **"MODBUS Configuration"** screen.

</Steps>

<ImageGallery images={[{"src":"/src/assets/devices-library/edge/recomputer-r1100/14-connector-pe.webp","alt":"On the **“ThingsBoard IoT Gateways”** dashboards page, click the **“Connectors”** button.","caption":"On the **“ThingsBoard IoT Gateways”** dashboards page, click the **“Connectors”** button."},{"src":"/src/assets/devices-library/edge/recomputer-r1000/6-add-connector-pe.webp","alt":"To add a **Connector**, click the **“+”** button. In the pop-up window, select the **MODBUS** option in the **“Type”** field and enter the  name of the connector in the **“Name”** field. Click the **“Add”** button to proceed.","caption":"To add a **Connector**, click the **“+”** button. In the pop-up window, select the **MODBUS** option in the **“Type”** field and enter the  name of the connector in the **“Name”** field. Click the **“Add”** button to proceed."},{"src":"/src/assets/devices-library/edge/recomputer-r1000/7-advanced-config-pe.webp","alt":"To make further adjustments, click on the newly added **Connector** and select the **“Advanced”** tab and the **“Configuration”** sub-tab on the right side of the **\"MODBUS Configuration\"** screen.","caption":"To make further adjustments, click on the newly added **Connector** and select the **“Advanced”** tab and the **“Configuration”** sub-tab on the right side of the **\"MODBUS Configuration\"** screen."}]} />

* Then, please insert the following configuration code. Then, click the **“Save”** button.

```cpp maxLines=15 collapsible
{
  "master": {
    "slaves": [
      {
        "host": "192.168.1.200",
        "port": 510,
        "type": "tcp",
        "method": "socket",
        "timeout": 35,
        "byteOrder": "BIG",
        "wordOrder": "BIG",
        "retries": true,
        "retryOnEmpty": true,
        "retryOnInvalid": true,
        "pollPeriod": 3000,
        "unitId": 1,
        "deviceName": "LOGO!",
        "deviceType": "siemens",
        "connectAttemptTimeMs": 5000,
        "connectAttemptCount": 5,
        "waitAfterFailedAttemptsMs": 300000,
        "attributes": [],
        "timeseries": [
          {
            "tag": "temp",
            "type": "16int",
            "functionCode": 4,
            "objectsCount": 1,
            "address": 2,
            "divider": 10
          },
          {
            "tag": "Q1",
            "type": "bit",
            "functionCode": 1,
            "objectsCount": 1,
            "address": 8193
          },
          {
            "tag": "Q2",
            "type": "16int",
            "functionCode": 3,
            "objectsCount": 2,
            "address": 507
          }
        ],
        "rpc": [
          {
            "tag": "getTemp",
            "type": "16int",
            "functionCode": 4,
            "objectsCount": 1,
            "address": 2
          }
        ],
        "attributeUpdates": [
          {
            "tag": "randNumber",
            "type": "16int",
            "functionCode": 6,
            "objectsCount": 1,
            "address": 507
          }
        ],
        "reportStrategy": {
          "type": "ON_REPORT_PERIOD",
          "reportPeriod": 3000
        }
      }
    ]
  },
  "slave": {}
}
```

## Installing the ThingsBoard IoT Gateway on the reComputer R1100

Start installation of the **IoT Gateway** on the **reComputer R1100**:


<Steps>

1. Go back to the **“ThingsBoard IoT Gateways”** dashboard page and click on the newly added **Gateway device** (reComputer R1100).
2. On the **Gateway device** page, click the **“Launch command”** button and download **docker-compose.yml** for your gateway.

</Steps>

<ImageGallery images={[{"src":"/src/assets/devices-library/edge/recomputer-r1100/15-gw-list-pe.webp","alt":"Go back to the **“ThingsBoard IoT Gateways”** dashboard page and click on the newly added **Gateway device** (reComputer R1100).","caption":"Go back to the **“ThingsBoard IoT Gateways”** dashboard page and click on the newly added **Gateway device** (reComputer R1100)."},{"src":"/src/assets/devices-library/edge/recomputer-r1100/16-launch-command.webp","alt":"On the **Gateway device** page, click the **“Launch command”** button and download **docker-compose.yml** for your gateway.","caption":"On the **Gateway device** page, click the **“Launch command”** button and download **docker-compose.yml** for your gateway."}]} />

* Then, open another tab in the terminal and initiate the **SSH** connection to the **reComputer R1100**:

```bash
ssh recomputer@ip_address
```

**ip_address:** The IP address of the reComputer R1100. Enter the actual IP address instead of _ip_address_.<br />
**Password:** Terminal requests the password. _The **default password** for the reComputer R1100 is: 12345678_

* Create the directory for the Gateway service:

```bash
mkdir gateway_service
```

* Open this directory:

```bash
cd /home/recomputer/gateway_service
```

* Create the docker compose file for your **IoT Gateway**:

```bash
nano docker-compose.yml
```

<Steps>

1. Copy the content of the previously downloaded **docker-compose.yml** file, and paste it into the new one via the terminal:

   <DocImage src="/src/assets/devices-library/edge/recomputer-r1100/copy-paste-configs.webp" alt="image" />

2. Save the **docker-compose.yml** file and press **CTRL+S** to save it. To close the file press **CTRL+X**.

3. Start the **Gateway** by executing the following command in the terminal:

</Steps>

```bash
docker compose up -d
docker compose logs -f
```

<Aside type="note">

The command must be executed in the same directory in which the docker-compose.yml file was saved.

</Aside>

## Visualize Incoming Data with the Dashboard

<DocImage src="/src/assets/devices-library/edge/recomputer-r1100/r1100-logo.webp" alt="image" />
Once the **ThingsBoard Edge** and **IoT Gateway** are running on the **Computer R1100** and the **ModBus connector** transfers data, you can visualize it on the **Dashboard** on your **Edge instance**:


<Steps>

1. Go to the **Dashboards** section, click the **“+”** icon and select **"Create new dashboard"** option.
2. In the pop-up window, enter the dashboard **title**. Other fields are optional. Click the **"Add"** button to proceed.
3. Once you have created the dashboard, it will be automatically opened. Click the **"Add widget"** button and select the widget you require. For example, open the **“Charts”** widget bundle and elect the **Line chart**.
4. In the **"Add Widget"** pop-up window, select the **Device** (in our case, Siemens LOGO!) as the **Datasource**. Please verify that the **Series Key** is the same as the one entered in the **Advanced Configuration** settings of the connector. As an example, we have utilized the designation **"temp"**. Consequently, the series key must be **"temp"** as well. Click the **“Add”** button.
5. You will now be able to see the real-time data on the dashboard.

</Steps>

<ImageGallery images={[{"src":"/src/assets/devices-library/edge/recomputer-r1000/11-create-new-dashboard-pe.webp","alt":"Go to the **Dashboards** section, click the **“+”** icon and select **\"Create new dashboard\"** option.","caption":"Go to the **Dashboards** section, click the **“+”** icon and select **\"Create new dashboard\"** option."},{"src":"/src/assets/devices-library/edge/recomputer-r1000/12-enter-name-pe.webp","alt":"In the pop-up window, enter the dashboard **title**. Other fields are optional. Click the **\"Add\"** button to proceed.","caption":"In the pop-up window, enter the dashboard **title**. Other fields are optional. Click the **\"Add\"** button to proceed."},{"src":"/src/assets/devices-library/edge/recomputer-r1000/13-configure-dashboard-pe.webp","alt":"Once you have created the dashboard, it will be automatically opened. Click the **\"Add widget\"** button and select the widget you require. For example, open the **“Charts”** widget bundle and elect the **Line chart**.","caption":"Once you have created the dashboard, it will be automatically opened. Click the **\"Add widget\"** button and select the widget you require. For example, open the **“Charts”** widget bundle and elect the **Line chart**."},{"src":"/src/assets/devices-library/edge/recomputer-r1000/14-widget-configuration-pe.webp","alt":"In the **\"Add Widget\"** pop-up window, select the **Device** (in our case, Siemens LOGO!) as the **Datasource**. Please verify that the **Series Key** is the same as the one entered in the **Advanced Configuration** settings of the connector. As an example, we have utilized the designation **\"temp\"**. Consequently, the series key must be **\"temp\"** as well. Click the **“Add”** button.","caption":"In the **\"Add Widget\"** pop-up window, select the **Device** (in our case, Siemens LOGO!) as the **Datasource**. Please verify that the **Series Key** is the same as the one entered in the **Advanced Configuration** settings of the connector. As an example, we have utilized the designation **\"temp\"**. Consequently, the series key must be **\"temp\"** as well. Click the **“Add”** button."},{"src":"/src/assets/devices-library/edge/recomputer-r1000/15-dashboard-pe.webp","alt":"You will now be able to see the real-time data on the dashboard.","caption":"You will now be able to see the real-time data on the dashboard."}]} />

</PlatformContent>
