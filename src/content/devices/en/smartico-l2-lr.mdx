---
title: Smartico L2-LR
description: Connect Smartico L2-LR to ThingsBoard
platforms: ['ThingsBoard']
hasBranching: false
vendor: Smartico
deviceImageFileName: smartico-l2-lr.png
hardwareType: Sensors
connectivity:
  - LoRaWAN
industry:
  - Energy Management
  - Smart Cities
  - Smart Buildings
useCase:
  - Smart Energy
  - Smart Metering
category: Sensors
---

import ImageGallery from '~/components/ImageGallery.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';

## Introduction

The <a href="https://smartico.biz/en/leaks-detector/" target="_blank" rel="noopener noreferrer">Smartico L2-LR</a> is a leaks detector used in industry, utilities, and automation for remote data collection and leak detection via LoRaWAN networks. It implements two-zone control using passive leak sensors, providing high energy efficiency. The waterproof housing allows outdoor installation. Data is transmitted wirelessly over LoRaWAN in the license-free frequency range.

This guide describes how to receive telemetry from the Smartico L2-LR over LoRaWAN using ChirpStack and a ThingsBoard MQTT integration, and visualize the data on a dashboard.

<ImageGallery images={[
  {
    src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-dashboard.png',
    alt: 'Smartico L2-LR leaks detector dashboard',
    caption: 'Dashboard showing processed telemetry from the Smartico L2-LR leaks detector',
  },
]} />

## Prerequisites

- Smartico L2-LR — 1×
- <a href="https://www.chirpstack.io/" target="_blank" rel="noopener noreferrer">ChirpStack</a> LoRaWAN Network Server with the device registered and connected
- <a href="https://www.chirpstack.io/docs/chirpstack/integrations/mqtt.html" target="_blank" rel="noopener noreferrer">ChirpStack MQTT integration</a> configured with a Mosquitto MQTT broker
- ThingsBoard PE instance: use <a href="/installations/choose-region/" target="_blank" rel="noopener noreferrer">ThingsBoard Cloud</a> or install a <a href="/docs/pe/installation/" target="_blank" rel="noopener noreferrer">local PE server</a>

## Configure ChirpStack

Register the Smartico L2-LR in ChirpStack and verify the device is transmitting. The device should appear on the **Applications** page with its serial number (like `13123`).

<ImageGallery images={[
  {
    src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-lorawan-devices.png',
    alt: 'ChirpStack Applications page showing registered Smartico L2-LR device',
    caption: 'ChirpStack: registered Smartico L2-LR device type visible on the Applications page',
  },
  {
    src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-lorawan-data.png',
    alt: 'ChirpStack device data page showing live frames from device 13123',
    caption: 'ChirpStack: live data flow from device 13123 confirming correct LoRaWAN connectivity',
  },
]} />

## Create the uplink data converter

The uplink data converter decodes the Base64-encoded payload from the L2-LR into ThingsBoard telemetry format.

<Steps>
1. Download <a href="/device-library/smartico-l2-lr/resources/uplink_leaks_detector.json" download="uplink_leaks_detector.json">uplink_leaks_detector.json</a>.
2. Go to **Integrations center &#8702; Data converters** and click **+ Add data converter > Import converter**.
3. Drag and drop the downloaded `uplink_leaks_detector.json` file.
4. Click **Import**.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-uplink-converter-1.png',
    alt: 'Import data converter dialog',
    caption: 'Click <strong>Add data converter</strong>, then upload the JSON file to import the uplink data converter.',
  },
  {
    src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-uplink-converter-2.png',
    alt: 'Uplink data converter details',
    caption: 'Open the imported converter to review and edit the uplink decoding configuration.',
  }
]} />

<Aside type="note">
  Decoding function provided for **JS** type
</Aside>

```js maxLines=10 collapsible
/** Decoder **/

var payloadStr = decodeToString(payload);
var data = JSON.parse(payloadStr);
var deviceName = data.deviceName;
var deviceType = 'Leak Sensor';
var res = _base64ToArrayBuffer(data.data);
var dateTime = GetCurrentDateTime();

var fraud1_dight = null;
var fraud2_dight = null;
var fraud1 = GetFraud(res[2], 1);
var fraud2 = GetFraud(res[2], 2);

var flg_err_pulse1 = GetErrPulse(res[2], 1);
var flg_err_pulse2 = GetErrPulse(res[2], 2);
var low_bat = GetFlagError(res[0], 1);
var motion_detect = GetFlagError(res[0], 2);
var magnet_detect = GetFlagError(res[0], 3);
var tamper_detect = GetFlagError(res[0], 4);
var power_on = GetFlagError(res[0], 5);
var power_bat = GetFlagError(res[0], 6);
var err_time = GetFlagError(res[0], 7);
var cfg_done = GetFlagError(res[0], 8);

var result = {
   deviceName: deviceName,
   deviceType: deviceType,
   attributes: {
       integrationName: metadata['integrationName'],
   },
   telemetry: {
       NAME_DEV: "Leaks Detector LoRaWAN 'Smartico L2-LR'",
       SN: data.deviceName,
       REAL_TIME: dateTime,
       FRAUD_1: fraud1,
       FRAUD_2: fraud2,
       FRAUD_1D: fraud1_dight,
       FRAUD_2D: fraud2_dight,
       FLG_ERR_PULSE_1: flg_err_pulse1,
       FLG_ERR_PULSE_2: flg_err_pulse2,
       FLG_LOW_BAT: low_bat,
       FLG_MOTION_DETECT: motion_detect,
       FLG_MAGNET_DETECT: magnet_detect,
       FLG_TAMPER_DETECT: tamper_detect,
       FLG_POWER_ON: power_on,
       FLG_POWER_BAT: power_bat,
       FLG_ERR_TIME: err_time,
       FLG_CFG_DONE: cfg_done
   }
};

/** Helper functions **/

function decodeToString(payload) {
   return String.fromCharCode.apply(String, payload);
}

function decodeToJson(payload) {
   var str = decodeToString(payload);
   var data = JSON.parse(str);
   return data;
}

function _base64ToArrayBuffer(base64) {
    var binary_string = atob(base64);
    var len = binary_string.length;
    var bytes = new Uint8Array(len);
    for (var i = 0; i < len; i++) {
        bytes[i] = binary_string.charCodeAt(i);
    }
    return bytes;
}

function GetCurrentDateTime(){
    var a = new Date();
    var year = a.getFullYear().toString();
    var month = (a.getMonth()+1).toString();
    if(month.length==1) month="0"+month;
    var date = a.getDate().toString();
    if(date.length==1) date="0"+date;
    var hour = a.getHours().toString();
    if(hour.length==1) hour="0"+hour;
    var min = a.getMinutes().toString();
    if(min.length==1) min="0"+min;
    var sec = a.getSeconds().toString();
    if(sec.length==1) sec="0"+sec;
    return date + '.' + month + '.' + year + ' ' + hour + ':' + min + ':' + sec;
}

function GetFraud(byte1, num) {
    var value1 = parseInt(byte1, 10).toString(2);
    while(value1.length<8){value1='0'+value1;}

    if(num==1) {
        var res = value1[5]+value1[6]+value1[7];
        if(parseInt(res,2)===6) {
            fraud1_dight = 2;
            return "Discovered";
        } else {
            fraud1_dight = 1;
            return "Not discovered";
        }
    }

    if(num==2) {
        var res1 = value1[2]+value1[3]+value1[4];
        if(parseInt(res1,2)===6) {
            fraud2_dight = 2;
            return "Discovered";
        } else {
            fraud2_dight = 1;
            return "Not discovered";
        }
    }

    return null;
}

function GetErrPulse(byte1, num) {
    var value1 = parseInt(byte1, 10).toString(2);
    while(value1.length<8){value1='0'+value1;}
    if(num==1) return parseInt(value1[1],2);
    if(num==2) return parseInt(value1[0],2);
    return null;
}

function GetFlagError(byte1, num) {
    var value = parseInt(byte1, 10).toString(2);
    while(value.length<8){value='0'+value;}

    if(num==1) return Number(value[7]);
    if(num==2) return Number(value[6]);
    if(num==3) return Number(value[5]);
    if(num==4) return Number(value[4]);
    if(num==5) return Number(value[3]);
    if(num==6) return Number(value[2]);
    if(num==7) return Number(value[1]);
    if(num==8) return Number(value[0]);

    return null;
}

return result;
```

You can test the converter before connecting the device. Open the **Uplink Leaks Detector** converter, enter edit mode and click the **Test decoder function** tab.
Paste the sample input below into the **Payload content** field and press **Test**.

<details>
  <summary>Sample input payload (Base64-encoded)</summary>

  ```json
  {
    "applicationID": "7",
    "applicationName": "Smartico_leak_sensor",
    "deviceName": "13123",
    "devEUI": "02aaaa0100003343",
    "rxInfo": [{
      "gatewayID": "647fdafffe00d228",
      "uplinkID": "4e9ed6ae-b658-4e11-b686-1ecae882c807",
      "name": "tectelic_micro_lite_TECH",
      "rssi": -68,
      "loRaSNR": 4.8,
      "location": {
        "latitude": 48.44229794818326,
        "longitude": 35.014479160308845,
        "altitude": 144
      }
    }],
    "txInfo": {
      "frequency": 868500000,
      "dr": 0
    },
    "adr": true,
    "fCnt": 2220,
    "fPort": 7,
    "data": "YgAtAAAAAAAAAAA="
  }
  ```

</details>

<details>
  <summary>Expected decoded output</summary>

  ```json
  {
    "deviceName": "13123",
    "deviceType": "Leak Sensor",
    "attributes": {
      "integrationName": "Leaks Detector"
    },
    "telemetry": {
      "NAME_DEV": "Leaks Detector LoRaWAN 'Smartico L2-LR'",
      "SN": "13123",
      "REAL_TIME": "27.08.2020 10:46:25",
      "FRAUD_1": "Not discovered",
      "FRAUD_2": "Not discovered",
      "FRAUD_1D": 1,
      "FRAUD_2D": 1,
      "FLG_ERR_PULSE_1": 0,
      "FLG_ERR_PULSE_2": 0,
      "FLG_LOW_BAT": 0,
      "FLG_MOTION_DETECT": 1,
      "FLG_MAGNET_DETECT": 0,
      "FLG_TAMPER_DETECT": 0,
      "FLG_POWER_ON": 0,
      "FLG_POWER_BAT": 1,
      "FLG_ERR_TIME": 1,
      "FLG_CFG_DONE": 0
    }
  }
  ```

</details>

<ImageGallery images={[{
  src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-verify-uplink.png',
  alt: 'Uplink converter Test decoder function showing decoded output for the L2-LR payload',
  caption: 'Test decoder function: decoded telemetry output for the Smartico L2-LR payload',
}]} />

## Create the integration

For more information about MQTT integrations in ThingsBoard, see the <a href="/docs/pe/user-guide/integrations/mqtt/" target="_blank" rel="noopener noreferrer">MQTT integration documentation</a>.

<Steps>
  <ol start="1">
    <li>Go to <strong>Integrations center &#8702; Integrations</strong> and click <strong>+ Add integration</strong>.</li>
    <li>
      <span><strong>Basic settings:</strong></span>
      <ul>
        <li>Set <strong>Integration type</strong> to <strong>MQTT</strong>.</li>
        <li><strong>Enable integration</strong> and <strong>Allow create devices or assets</strong> are on by default.</li>
        <li>Click <strong>Next</strong>.</li>
      </ul>
    </li>
    <li>
      <span><strong>Uplink data converter:</strong></span>
      <ul>
        <li><strong>Select existing</strong> — choose the previously created <code>Uplink Leaks Detector</code> from the list.</li>
        <li>Click <strong>Next</strong>.</li>
      </ul>
    </li>
    <li>
      <span><strong>Downlink data converter:</strong></span>
      <ul>
        <li>Click <strong>Skip</strong> — only needed for RPC; can be added later.</li>
      </ul>
    </li>
    <li>
      <span><strong>Connection:</strong></span>
      <ul>
        <li>Set your <strong>MQTT broker Host</strong> and <strong>Port</strong>.</li>
        <li>Select the <strong>Credentials type</strong> (e.g. <strong>Anonymous</strong>).</li>
        <li>Add at least one <strong>topic filter</strong> with <strong>QoS</strong> (e.g. <code>application/7/device/+/rx</code>, QoS <code>0 — At most once</code>).</li>
      </ul>
    </li>
    <li>Click <strong>Add</strong> to complete the integration setup.</li>
  </ol>
</Steps>

<MultiProductImageGallery product={props.product} images={[
  {
    src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-integration-1.png',
    alt: 'Add MQTT integration - basic settings',
    caption: 'Click <strong>Add integration</strong>, select <strong>MQTT</strong>, enter a name, and click <strong>Next</strong>.',
  },
  {
    src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-integration-2.png',
    alt: 'Select uplink data converter',
    caption: 'On the <strong>Uplink data converter</strong> step, select the previously created converter and click <strong>Next</strong>.',
  },
  {
    src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-integration-3.png',
    alt: 'Skip downlink data converter',
    caption: 'On the <strong>Downlink data converter</strong> step, click <strong>Skip</strong> if not required.',
  },
  {
    src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-integration-4.png',
    alt: 'Configure MQTT connection settings',
    caption: 'Configure the MQTT broker host, port, credentials, and topic filters, then click <strong>Add</strong>.',
  },
]} />

## Verify data

After the L2-LR transmits its first packet, a new device named after its serial number (like `13123`) appears automatically in **Entities > Devices**. You can also verify input and output data in **Data converters > Uplink Leaks Detector > Events**.

<ImageGallery images={[{
  src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-verify-device.png',
  alt: 'ThingsBoard Devices page showing auto-created device 13123',
  caption: 'Devices: device 13123 created automatically after the first LoRaWAN transmission',
}]} />

## Create an asset

To display data on the dashboard, create an asset and add the device as a relation.

<Steps>
1. Go to **Entities &#8702; Assets** and create a new asset named `Leaks Detector` with asset type `leaks-detector`.

   <ImageGallery images={[{
     src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-create-asset.png',
     alt: 'ThingsBoard Assets page creating a new leaks detector asset',
     caption: 'Assets: creating the Leaks Detector asset',
   }]} />

2. Open the asset, go to the **Relations** tab, and add a relation to the device.

   <ImageGallery images={[{
     src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-asset-relations.png',
     alt: 'Asset Relations tab with device linked to the leaks detector asset',
     caption: 'Asset relations: linking the device to the Leaks Detector asset',
   }]} />
</Steps>

## Import the rule chain

In addition to leak status, the device reports flags for low battery, tamper detection, and magnetic field exposure. These are displayed in an Alarm widget. Import the rule chain first to enable alarm processing.

<Steps>
1. Download <a href="/device-library/smartico-l2-lr/resources/alarms_leaks_detector.json" download="alarms_leaks_detector.json">alarms_leaks_detector.json</a> and import it in **Rule chains**.

   <ImageGallery images={[{
     src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-rule-chain-import.png',
     alt: 'ThingsBoard Rule chains page with the Alarms Leaks Detector rule chain imported',
     caption: 'Rule chains: importing the Alarms Leaks Detector rule chain',
   }]} />

2. Open the **Root Rule Chain**, add a link to the imported **Alarms Leaks Detector** rule chain, and save.

   <ImageGallery images={[{
     src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-root-rule-chain.png',
     alt: 'Root Rule Chain with Alarms Leaks Detector node added',
     caption: 'Root Rule Chain: linking the Alarms Leaks Detector rule chain for alarm processing',
   }]} />
</Steps>

## Import the dashboard

<Steps>
1. Download <a href="/device-library/smartico-l2-lr/resources/dashboard_leaks_detector.json" download="dashboard_leaks_detector.json">dashboard_leaks_detector.json</a> and import it in **Dashboards**. See the <a href="/docs/user-guide/dashboards/#import-dashboard" target="_blank" rel="noopener noreferrer">dashboard import instructions</a>.

   <ImageGallery images={[{
     src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-import-dashboard.png',
     alt: 'ThingsBoard Dashboards page with the Smartico L2-LR dashboard imported',
     caption: 'Dashboards: importing the Leaks Detector dashboard',
   }]} />

2. After importing, edit the dashboard <a href="/docs/user-guide/aliases/" target="_blank" rel="noopener noreferrer">alias</a> and set it to the **Leaks Detector** asset created in the previous step.

   <ImageGallery images={[{
     src: '/src/assets/devices-library/smartico-l2-lr/smartico-l2lr-dashboard-alias.png',
     alt: 'Dashboard alias configuration linking the widget to the Leaks Detector asset',
     caption: 'Dashboard alias: linking the widget datasource to the Leaks Detector asset',
   }]} />
</Steps>

The dashboard now displays live leak status and device health data from the Smartico L2-LR.
