---
title: COMFORT v2 LoRaWAN® Leak Detection Sensor
description: Connect COMFORT v2 LoRaWAN® Leak Detection Sensor to ThingsBoard    
platforms: ['ThingsBoard']
hasBranching: false
vendor: TEKTELIC
deviceImageFileName: comfort-v2-lorawan-leak-detection-sensor.png
hardwareType: Sensors
connectivity:
  - LoRaWAN
industry:
  - Smart Buildings
  - Smart Cities
  - Industrial Manufacturing
useCase:
  - Environment Monitoring
  - Smart Office
category: Sensors
---

import ImageGallery from '~/components/ImageGallery.astro';
import { Steps } from '@astrojs/starlight/components';

## Introduction

The <a href="https://tektelic.com/wp-content/uploads/TEKTELIC_Comfort_Vivid_v2_UG.pdf" target="_blank" rel="noopener noreferrer">COMFORT v2 LoRaWAN® Leak Detection Sensor</a> from TEKTELIC is a compact indoor sensor that monitors water leaks via a wired probe and reports events over LoRaWAN. It is deployed using the TEKTELIC KONA Core Network Server, which has a built-in ThingsBoard integration that forwards sensor data to ThingsBoard via the MQTT Gateway API — automatically creating devices and telemetry entries on first uplink.

This guide walks through registering the gateway and sensor in the KONA Network Server, creating the ThingsBoard integration, and verifying received data.

<ImageGallery images={[{
  src: '/src/assets/devices-library/comfort-v2-lorawan-leak-detection-sensor/comfort-v2-setup-scheme.png',
  alt: 'Setup scheme showing COMFORT v2 sensor connected through KONA gateway and Network Server to ThingsBoard',
  caption: 'Overall setup: the COMFORT v2 sends LoRaWAN packets through the KONA gateway and Network Server to ThingsBoard.',
}]} />

## Prerequisites

- TEKTELIC COMFORT v2 LoRaWAN® Leak Detection Sensor
- <a href="https://tektelic.com/products/gateways/kona-micro-iot-gateway/" target="_blank" rel="noopener noreferrer">KONA Micro IoT Gateway</a>
- Customer account on the <a href="https://lorawan-ns-eu.tektelic.com" target="_blank" rel="noopener noreferrer">TEKTELIC KONA Core Network Server</a> (EU instance) — contact <a href="mailto:support@tektelic.com">TEKTELIC support</a> to obtain one
- ThingsBoard PE instance: use <a href="/installations/choose-region/" target="_blank" rel="noopener noreferrer">ThingsBoard Cloud</a> or install a <a href="/docs/pe/installation/" target="_blank" rel="noopener noreferrer">local PE server</a>

## Step 1: Register the gateway in the Network Server

<Steps>
1. Log in to the KONA Core Network Server. Create a new gateway group, then add a new gateway in the group.

   Use the **GW-ID** from the sticker on the gateway body. For credentials, the default username is `TEK00XXYY` (where `XXYY` is the last 4 digits of the MAC address) and the default password is the 9-digit serial number.

   The Network Server provides a predefined list of gateway models — select **Kona Micro EU GW** or create a new model and set the frequency band and channel count to match your hardware.

2. Once the gateway is powered on and connected to the internet, its status changes to **Online**. The **Statistics** tab updates periodically with sent and received packet counts.

   <ImageGallery images={[{
     src: '/src/assets/devices-library/comfort-v2-lorawan-leak-detection-sensor/comfort-v2-gateway-statistics.png',
     alt: 'KONA Network Server gateway Statistics tab showing packet counters',
     caption: 'Gateway Statistics tab: confirms the gateway is online and receiving radio packets.',
   }]} />
</Steps>

## Step 2: Register the COMFORT v2 sensor

<Steps>
1. Create a new application in the Network Server (for example, *Leak Sensors*). Create a new device in the application.

   The COMFORT v2 ships with a QR code sticker containing the Device EUI and Application EUI required for provisioning.

   <ImageGallery images={[
     {
       src: '/src/assets/devices-library/comfort-v2-lorawan-leak-detection-sensor/comfort-v2-sensor-qr.png',
       alt: 'COMFORT v2 QR code sticker with Device EUI and Application EUI',
       caption: 'QR code sticker on the sensor body: contains Device EUI and Application EUI for registration.',
     },
     {
       src: '/src/assets/devices-library/comfort-v2-lorawan-leak-detection-sensor/comfort-v2-qr-scan-result.png',
       alt: 'Scanned QR code showing Device EUI and Application EUI values',
       caption: 'Scanned QR result: Device EUI and Application EUI extracted for the registration form.',
     },
   ]} />

2. Enter the Device EUI and Application EUI when adding the device. Then add the sensor to the application.

3. Power on the sensor by removing the protective film from the battery contact. The sensor sends a join request, and the Network Server shows join and uplink packets in the **Real-Time Packets** tab.

   <ImageGallery images={[
     {
       src: '/src/assets/devices-library/comfort-v2-lorawan-leak-detection-sensor/comfort-v2-sensor-packets.png',
       alt: 'KONA Network Server Real-Time Packets tab showing join and uplink packets from the sensor',
       caption: 'Real-Time Packets: join request and uplink packets after powering on the sensor.',
     },
     {
       src: '/src/assets/devices-library/comfort-v2-lorawan-leak-detection-sensor/comfort-v2-sensor-activation-tab.png',
       alt: 'Sensor Activation tab showing Application and Network session keys',
       caption: 'Activation tab: Application and Network session keys (change after each rejoin).',
     },
   ]} />
</Steps>

## Step 3: Create a gateway device in ThingsBoard

The KONA Network Server uses the ThingsBoard MQTT Gateway API to forward data. You must create a **gateway device** in ThingsBoard and provide its access token to the Network Server.

<Steps>
1. In ThingsBoard, go to **Entities &#8702; Devices** and create a new device. Set any name (for example, `NS Integration`) and enable the **Is gateway** checkbox. Click **Add**.

   <ImageGallery images={[{
     src: '/src/assets/devices-library/comfort-v2-lorawan-leak-detection-sensor/comfort-v2-tb-gateway.png',
     alt: 'ThingsBoard new device dialog with Is gateway checkbox enabled',
     caption: 'Creating the gateway device in ThingsBoard: enable <strong>Is gateway</strong> to allow the MQTT Gateway API.',
   }]} />

2. Open the created device and click **Copy access token**. Save the token — you will need it in the next step.
</Steps>

## Step 4: Configure the ThingsBoard integration in the Network Server

<Steps>
1. In the KONA Network Server, open your application (e.g. *Leak Sensors*) and click **Manage Integrations**. Click **Add integration** (+) and fill in the following fields:

   | Field | Value |
   |-------|-------|
   | Name | Any name (e.g. `ThingsBoard`) |
   | Type | `ThingsBoard` |
   | Data Converter | Tektelic Home Sensor (or the appropriate converter for your sensor model) |
   | Application Address | ThingsBoard host without `http://` or `https://` prefix |
   | Token | The access token copied in Step 3 |

   Leave all remaining fields at their defaults and save.

   <ImageGallery images={[{
     src: '/src/assets/devices-library/comfort-v2-lorawan-leak-detection-sensor/comfort-v2-ns-integration.png',
     alt: 'KONA Network Server integration configuration showing Type, Application Address, and Token fields',
     caption: 'KONA integration config: set Type to ThingsBoard, enter the ThingsBoard host and access token.',
   }]} />
</Steps>

## Verify data in ThingsBoard

After the next uplink from the COMFORT v2, the Network Server forwards data via MQTT Gateway API. ThingsBoard automatically creates two child devices under the gateway device: one for the LoRa gateway and one for the sensor.

<Steps>
1. In ThingsBoard, open the gateway device created in Step 3. Go to the **Relations** tab and select **From** outbound relations. Two child devices appear: the LoRa gateway (type `Gateway`) and the COMFORT v2 sensor (type `CLASS_A`).

   <ImageGallery images={[{
     src: '/src/assets/devices-library/comfort-v2-lorawan-leak-detection-sensor/comfort-v2-tb-relations.png',
     alt: 'ThingsBoard Relations tab showing the auto-created gateway and sensor devices',
     caption: 'Relations tab: auto-created devices — the LoRa gateway (Gateway type) and the COMFORT v2 sensor (CLASS_A type).',
   }]} />

2. Open the sensor device and click **Latest telemetry**. It contains the telemetry decoded by the Network Server converter — LoRaWAN metadata (`nsGateway`, `nsRssi`, etc.) and sensor-specific fields like `humidity`, `temperature`, and leak state.
</Steps>

## Import the dashboard

Download <a href="/device-library/comfort-v2-lorawan-leak-detection-sensor/resources/network_server_dashboard.json" download="network_server_dashboard.json">network_server_dashboard.json</a> and import it in **Dashboards**. See <a href="/docs/user-guide/dashboards/#import-dashboard" target="_blank" rel="noopener noreferrer">dashboard import instructions</a>.
