---
title: Agent self-upgrade
sidebar:
  label: Self-upgrade
description: "Upgrade Remote Agent from the ThingsBoard UI: the agent replaces its own container with a new image, hands the host over, and keeps serving if the handover does not complete."
---
import PageComponent from '@includes/docs/user-guide/agents/self-upgrade.mdx'
import { Products } from '~/models/site.models'

<PageComponent product={Products.PE}/>
