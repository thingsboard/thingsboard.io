import DocLink from '@components/DocLink.astro';
import { Steps } from '@astrojs/starlight/components';
import Checklist from '~/components/Checklist.astro';
import Box from '~/components/tutorial/Box.astro';
import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import { Products } from '~/models/site.models';

In this step, you will learn how telemetry flows from Edge to your ThingsBoard account, how entities created on Edge are synchronized to the server, and how to assign server-side entities to an Edge instance.

---

## Step 4.1 Push data from Edge to server

The **Edge Root Rule Chain** controls how Edge processes and forwards data locally. It includes a **Push to Cloud** node that transmits telemetry and attributes to your ThingsBoard account after storing them in the local Edge database.

To view the rule chain, go to **Rule Chains** in the Edge UI and open **Edge Root Rule Chain**. The **Push to Cloud** node is connected to the **Post telemetry** and **Post attributes** message types.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/edge/getting-started/step-3-item-1.webp', alt: 'Edge Root Rule Chain with Push to Cloud node highlighted', caption: 'The Push to Cloud node forwards telemetry to the ThingsBoard server.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-3-item-2.webp', alt: 'Push to Cloud rule node configuration panel', caption: 'The node triggers on Post telemetry and Post attributes message types.' },
]} />

To create or modify rule chain templates for Edge instances, go to **Edge Management → Rule Chain Templates** in your ThingsBoard account.

---

## Step 4.2 Provision entities from Edge to server

Entities created on an Edge instance — devices, assets, and dashboards — are synchronized to your ThingsBoard account automatically.

To verify:

<Steps>
1. In your ThingsBoard account, go to **Entities → Devices**. The devices you created on Edge appear here.

2. Open `sensor-01` and go to the **Relations** tab. Switch the direction from **From** to **To** to see the relationship with the Edge instance.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-3.webp', alt: 'Devices list in ThingsBoard account showing sensor-01 synchronized from Edge', caption: 'Devices created on Edge appear in the ThingsBoard account automatically.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-4.webp', alt: 'Relations tab on sensor-01 showing the Edge instance relationship', caption: 'The Relations tab confirms the device belongs to the Edge instance.' },
]} />

---

## Step 4.3 Provision entities from server to Edge

You can assign entities created in your ThingsBoard account — like dashboards, device profiles, or rule chains — to an Edge instance. Once assigned, they synchronize to Edge automatically.

To assign a dashboard to Edge:

<Steps>
1. In your ThingsBoard account, go to **Edge Management → Instances**.

2. Click **Manage dashboards** for your Edge instance.

3. Select the dashboard and click **Assign**. The dashboard synchronizes to Edge.

4. In the Edge UI, go to **Dashboards** to confirm the dashboard appears with the same widgets and configuration.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/edge/getting-started/step-5-item-1.webp', alt: 'Edge Instances page with Manage dashboards action', caption: 'Open Manage dashboards for the Edge instance.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-5-item-2.webp', alt: 'Dashboard assignment dialog showing available dashboards', caption: 'Select the dashboard to assign to Edge.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-5-item-3.webp', alt: 'Dashboard visible in Edge UI after synchronization', caption: 'The assigned dashboard appears in Edge.' },
]} />

---

<Box icon="check-list">

### Checklist

<Checklist>
- [ ] I can see the Push to Cloud node in the Edge Root Rule Chain.
- [ ] Devices created on Edge appear in my ThingsBoard account.
- [ ] I assigned an entity from the server to the Edge instance.
</Checklist>

</Box>
