import DocLink from '@components/DocLink.astro';
import { Steps } from '@astrojs/starlight/components';
import Checklist from '~/components/Checklist.astro';
import Box from '~/components/tutorial/Box.astro';
import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import { Products } from '~/models/site.models';

In this step, you will learn how telemetry flows from Edge to your ThingsBoard account, how entities created on Edge are synchronized to the server, and how to assign server-side entities to an Edge instance.

---

## Step 4.1 Push data from Edge to server

The **Edge Root Rule Chain** controls how Edge processes and forwards data locally. It includes a **Push to Cloud** node that transmits telemetry and attributes to your ThingsBoard account after storing them in the local Edge database.

<Steps>
1. Go to **Rule Chains** in the Edge UI to create or modify rule chains.

2. Open the **Edge Root Rule Chain** — you will see that the **Push to Cloud** node is connected to the **Post telemetry** and **Post attributes** message types.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-1.webp', alt: 'Edge Root Rule Chain with Push to Cloud node highlighted', caption: 'The Push to Cloud node forwards telemetry to the ThingsBoard server.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-2.webp', alt: 'Push to Cloud rule node configuration panel', caption: 'The node triggers on Post telemetry and Post attributes message types.' },
]} />

---

## Step 4.2 Provision entities from Edge to server

Entities created on an Edge instance — devices, assets, and dashboards — are synchronized to your ThingsBoard account automatically.

To verify:

<Steps>
1. In your ThingsBoard account, go to **Entities → Devices**. The devices you created on Edge appear here.

2. Open the device and go to the **Relations** tab. Switch the direction from **From** to **To** to see the relationship with the Edge instance.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-3.webp', alt: 'Devices list in ThingsBoard account showing sensor-01 synchronized from Edge', caption: 'Devices created on Edge appear in the ThingsBoard account automatically.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-4.webp', alt: 'Relations tab on sensor-01 showing the Edge instance relationship', caption: 'The Relations tab confirms the device belongs to the Edge instance.' },
]} />

---

## Step 4.3 Provision entities from server to Edge

You can assign entities created in your ThingsBoard account — like dashboards, device profiles, or rule chains — to an Edge instance. Once assigned, they synchronize to Edge automatically. The steps below demonstrate this using a dashboard assignment as an example.

{props.product === Products.EDGE_PE ? (
<Steps>
<ol>
<li>In your ThingsBoard account, go to <strong>Dashboards</strong> and create a new dashboard. Once configured and saved, click the <strong>Dashboard details</strong> icon (pencil) to open it.</li>
<li>Click <strong>Manage owner and groups</strong>.</li>
<li>In the pop-up window, in the <strong>Groups</strong> field, enter a group name and click <strong>Create a new one!</strong> in the dropdown menu.</li>
<li>Click <strong>Add</strong>.</li>
<li>Click <strong>Update</strong>.</li>
<li>The dashboard is now assigned to the new group, visible in the <strong>Groups</strong> column.</li>
<li>Go to <strong>Edge Management → Instances</strong> and click the <strong>Manage edge dashboard groups</strong> icon for your Edge instance.</li>
<li>On the <strong>Edge Dashboard Groups</strong> page, click <strong>Assign to Edge</strong> (+), select the new group from the dropdown, and click <strong>Assign</strong>.</li>
<li>Open the Edge UI (<a href="http://localhost:8080" target="_blank" rel="noopener noreferrer">http://localhost:8080</a>), go to <strong>Dashboards</strong> — the assigned dashboard is now available on Edge.</li>
</ol>
</Steps>
) : (
<Steps>
<ol>
<li>In your ThingsBoard account, go to <strong>Dashboards</strong> and create a new dashboard. Once configured and saved, go to <strong>Edge Management → Instances</strong>.</li>
<li>Click <strong>Manage dashboards</strong> for your Edge instance.</li>
<li>Select the dashboard and click <strong>Assign</strong>. The dashboard synchronizes to Edge.</li>
<li>In the Edge UI, go to <strong>Dashboards</strong> to confirm the dashboard appears with the same widgets and configuration.</li>
</ol>
</Steps>
)}

{props.product === Products.EDGE_PE ? (
<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-5.webp', alt: 'Dashboards section with a new dashboard created on the ThingsBoard server', caption: 'Create a new dashboard on the server and click the pencil icon to open Dashboard details.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-6.webp', alt: 'Dashboard details page with Manage owner and groups button', caption: 'Click Manage owner and groups.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-7.webp', alt: 'Manage owner and groups pop-up with Groups field and Create a new one option', caption: 'Enter a group name and click Create a new one! in the dropdown.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-8.webp', alt: 'Manage owner and groups dialog with Add button', caption: 'Click Add.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-9.webp', alt: 'Manage owner and groups dialog with Update button', caption: 'Click Update.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-10.webp', alt: 'Dashboards list showing the dashboard assigned to the new group in the Groups column', caption: 'The dashboard now appears in the new group.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-11.webp', alt: 'Edge Management Instances page with Manage edge dashboard groups icon', caption: 'Go to Edge Management → Instances and click Manage edge dashboard groups.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-12.webp', alt: 'Edge Dashboard Groups page with Assign to Edge button and group dropdown', caption: 'Click Assign to Edge (+), select the group, and click Assign.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-13.webp', alt: 'Edge UI Dashboards section showing the assigned dashboard available on Edge', caption: 'Open the Edge UI and go to Dashboards — the assigned dashboard is now available on Edge.' },
]} />
) : (
<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-5.webp', alt: 'Edge Instances page with Manage dashboards action', caption: 'Open Manage dashboards for the Edge instance.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-6.webp', alt: 'Dashboard assignment dialog showing available dashboards', caption: 'Select the dashboard to assign to Edge.' },
  { src: '/src/assets/images/docs/edge/getting-started/step-4-item-7.webp', alt: 'Dashboard visible in Edge UI after synchronization', caption: 'The assigned dashboard appears in Edge.' },
]} />
)}

---

<Box icon="check-list">

### Checklist

<Checklist>
- [ ] I understand how data and entities flow between Edge and the ThingsBoard server.
</Checklist>

</Box>
