import DocLink from '@components/DocLink.astro';
import { Aside } from '@astrojs/starlight/components';
import Checklist from '~/components/Checklist.astro';
import Box from '~/components/tutorial/Box.astro';
import Lede from '~/components/tutorial/Lede.astro';
import { Products } from '~/models/site.models';

<Lede>By completing this guide, you will learn how to install ThingsBoard Edge and connect it to a server, add a device and publish telemetry, and create and configure real-time dashboards.</Lede>

:::tip
Use the checkboxes at the bottom of each page to track your progress.
All data is stored in your browser and is not sent to ThingsBoard.
:::


---

<h2>Prerequisites</h2>

Before starting, you need:

- An active ThingsBoard account with Edge functionality enabled (<DocLink product={props.product === Products.EDGE_PE ? Products.PE : Products.CE} path="installation" target="_blank">self-hosted</DocLink>{props.product === Products.EDGE_PE && <> or <DocLink product={Products.PE} path="installation" target="_blank">ThingsBoard Cloud</DocLink></>}).
- A Linux-based machine or Docker environment (minimum 1 GB RAM).
- Port `7070` open between the Edge host and your ThingsBoard instance

---

<Box icon="check-list">

### Checklist

<Checklist>
- [ ] I have an active ThingsBoard account with Edge functionality enabled.
</Checklist>

</Box>
