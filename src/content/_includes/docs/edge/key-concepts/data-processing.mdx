import DocLink from '@components/DocLink.astro';
import ImageGallery from '@components/ImageGallery.astro';
import { Products } from '~/models/site.models';

ThingsBoard Edge runs a full rule engine locally. Every message from a connected device passes through the rule engine before it is stored, forwarded, or acted upon — whether the Edge is online or offline. This is what makes Edge functional independently of the server: decisions, alarms, and data routing all happen at the edge of the network.

## How the rule engine works

The Edge rule engine mirrors the server rule engine in architecture and available node types. It uses an actor model: each rule chain and rule node runs as a separate actor, and messages flow between them as immutable objects.

```plaintext
  ┌─────────────────────────────────────────────────┐
  │  Device (MQTT / HTTP / CoAP)                    │
  └──────────────────────┬──────────────────────────┘
                         │
  ┌──────────────────────▼──────────────────────────┐
  │  Transport layer                                │
  │  (validates credentials, parses protocol)       │
  └──────────────────────┬──────────────────────────┘
                         │
  ┌──────────────────────▼──────────────────────────┐
  │  Rule engine                                    │
  │                                                 │
  │  ┌─────────────┐   ┌──────────────────────┐     │
  │  │ Save to DB  │   │  Generate alarm       │     │
  │  └─────────────┘   └──────────────────────┘     │
  │  ┌─────────────┐   ┌──────────────────────┐     │
  │  │ Push to     │   │  Send RPC to device   │     │
  │  │ server      │   └──────────────────────┘     │
  │  └─────────────┘                                │
  └─────────────────────────────────────────────────┘
```

The same node categories available on the server are available on Edge — filter, enrichment, transformation, action, and external nodes. The key addition is two Edge-specific action nodes: <DocLink product={props.product === Products.EDGE_PE ? Products.PE : Products.CE} path="reference/rule-engine/nodes/action/push-to-cloud" target="_blank">push to cloud</DocLink> and <DocLink product={props.product === Products.EDGE_PE ? Products.PE : Products.CE} path="reference/rule-engine/nodes/action/push-to-edge" target="_blank">push to edge</DocLink>, which control data synchronization with the server. For details on how synchronization works, see <DocLink product={props.product === Products.EDGE_PE ? Products.EDGE_PE : Products.EDGE} path="key-concepts/telemetry-synchronization" target="_blank">Telemetry synchronization</DocLink>.

## Default root rule chain

Edge ships with a preconfigured **Root Rule Chain** that handles the most common scenarios without any additional setup:

- Saves device telemetry and attributes to the local database.
- Generates alarms based on device profile alarm rules.
- Forwards telemetry and attribute updates to the server via the **push to cloud** node.

You can extend the Root Rule Chain by adding nodes before or after existing ones, or replace it entirely with a custom chain. The Root Rule Chain is the entry point for all device messages — every message starts here unless a device is configured to use a different rule chain via its device profile.

## Rule chain templates

A **rule chain template** is a rule chain created on the server specifically for deployment to Edge instances. The template does not run on the server — it runs on the Edge after it is assigned. This lets you design and version processing logic centrally, then deploy it consistently across many Edge instances.

```plaintext
  ┌──────────────────────────────────────┐
  │  Server                              │
  │  Edge management → Rule chain        │
  │  templates                           │
  │                                      │
  │  [ Template A ]  [ Template B ]      │
  └──────────────┬───────────────────────┘
                 │  assign to Edge instance
                 ▼
  ┌──────────────────────────────────────┐
  │  Edge instance                       │
  │                                      │
  │  Rule chains (running locally)       │
  │  [ Template A ]  [ Template B ]      │
  └──────────────────────────────────────┘
```

Starting with Edge 4.0, rule chains can also be created and edited directly on the Edge instance — without going through server templates first. Changes made on Edge propagate back to the server automatically when the connection is available.

### Create a rule chain template on the server

Go to **Edge management → Rule chain templates** on the server and click **Add** (+). Enter a name and click **Add**. Open the new template and configure your rule nodes and connections. Click **Apply changes** when done. The template is not yet active on any Edge until you assign it.

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/create-rule-chain-template-step-1.webp',
      alt: 'Edge management → Rule chain templates section with Add button — CE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/create-rule-chain-template-step-2.webp',
      alt: 'Dialog to enter a name for the new rule chain template — CE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/create-rule-chain-template-step-3.webp',
      alt: 'New template listed in the Rule chain templates section — CE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/create-rule-chain-template-step-4.webp',
      alt: 'Rule chain editor with nodes configured and Apply changes button — CE',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/create-rule-chain-template-step-1.webp',
      alt: 'Edge management → Rule chain templates section with Add button — PE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/create-rule-chain-template-step-2.webp',
      alt: 'Dialog to enter a name for the new rule chain template — PE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/create-rule-chain-template-step-3.webp',
      alt: 'New template listed in the Rule chain templates section — PE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/create-rule-chain-template-step-4.webp',
      alt: 'Rule chain editor with nodes configured and Apply changes button — PE',
    },
  ]} />
)}

### Assign a template to an Edge instance

Go to **Edge management → Instances**, open the target Edge instance, and click **Manage rule chains**. Click **Add** (+). Select the template from the list and click **Assign**. The template is pushed to the Edge instance and becomes active immediately.

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/assign-rule-chain-template-step-1.webp',
      alt: 'Edge instance details with Manage rule chains button — CE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/assign-rule-chain-template-step-2.webp',
      alt: 'Rule chains list for the Edge instance with Add button — CE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/assign-rule-chain-template-step-3.webp',
      alt: 'Dialog to select a rule chain template to assign — CE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/assign-rule-chain-template-step-4.webp',
      alt: 'Assigned template now listed in the Edge instance rule chains — CE',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/assign-rule-chain-template-step-1.webp',
      alt: 'Edge instance details with Manage rule chains button — PE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/assign-rule-chain-template-step-2.webp',
      alt: 'Rule chains list for the Edge instance with Add button — PE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/assign-rule-chain-template-step-3.webp',
      alt: 'Dialog to select a rule chain template to assign — PE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/assign-rule-chain-template-step-4.webp',
      alt: 'Assigned template now listed in the Edge instance rule chains — PE',
    },
  ]} />
)}

### Create a rule chain directly on Edge

Go to **Rule chains** on the Edge instance and click **Add** (+). Choose **Create new rule chain**. Enter a name and click **Add**. Configure your rule nodes and click **Apply changes**.

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/1-edge-rule-chain.webp',
      alt: 'Rule chains section on Edge with Add button — CE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/2-create-rule-chain-template.webp',
      alt: 'Dialog to name the new rule chain on Edge — CE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/3-in-the-list.webp',
      alt: 'New rule chain listed in the Edge Rule chains section — CE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/4-configure-and-safe.webp',
      alt: 'Rule chain editor on Edge with Apply changes button — CE',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/1-edge-rule-chain.webp',
      alt: 'Rule chains section on Edge with Add button — PE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/2-create-rule-chain-template.webp',
      alt: 'Dialog to name the new rule chain on Edge — PE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/3-in-the-list.webp',
      alt: 'New rule chain listed in the Edge Rule chains section — PE',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/4-configure-and-safe.webp',
      alt: 'Rule chain editor on Edge with Apply changes button — PE',
    },
  ]} />
)}

## Common processing patterns

These four patterns cover the majority of Edge data processing use cases. They can be combined in a single rule chain.

**Filter:** Use a script or message type filter node to pass only relevant messages downstream. For example, route only telemetry with a `temperature` key to the push to cloud node and discard the rest. Unmatched messages are still saved locally.

**Transform:** Use a transformation node to reshape or enrich a message before forwarding it. For example, strip sensitive fields before pushing to the server, or add entity attributes as metadata to a telemetry message.

**Act:** Use action nodes to respond to device data in real time. For example, when an alarm is created, use an RPC call node to send a command back to the device that triggered it — like switching off a machine or adjusting a setpoint.

**Route:** Use a switch node or script node to split message traffic by device type, message content, or entity attribute, and send each path to a different downstream chain or output node.

## Offline operation

The rule engine continues to process messages when the Edge is disconnected from the server. Devices are processed, alarms are raised, and data is written to the local database without interruption. Events destined for the server accumulate in the cloud event queue and are sent once the connection is restored. For details, see <DocLink product={props.product === Products.EDGE_PE ? Products.EDGE_PE : Products.EDGE} path="key-concepts/telemetry-synchronization" target="_blank">Telemetry synchronization</DocLink>.
