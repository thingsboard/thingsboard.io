import CircuitpythonClientSdk from '@includes/docs/reference/circuitpython-client-sdk.mdx'
import { Aside } from '@astrojs/starlight/components';

<Aside type="note">
These examples connect directly to the Edge node. Point the SDK to your Edge host instead of the ThingsBoard Server. Data is stored locally on Edge and synchronized to the ThingsBoard Server via the root rule chain when the connection is available.
</Aside>

<CircuitpythonClientSdk product={props.product}/>
