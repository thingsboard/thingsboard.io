import DocLink from '@components/DocLink.astro';
import { Aside, Steps } from '@astrojs/starlight/components';
import ImageGallery from '~/components/ImageGallery.astro';
import { Products } from '~/models/site.models';

A public dashboard allows anyone with the URL to view it without authentication.

On ThingsBoard Edge, public dashboards work the same as on ThingsBoard Server, with these differences:

- Public dashboards remain accessible on the local network even when internet access is unavailable or restricted.
- The public URL uses the Edge's local address: `http://<Edge-IP>:8080/dashboards/<dashboard-id>`
- If the connection to ThingsBoard Server is lost, the dashboard continues to show real-time device data from the Edge.
- Any changes are synchronized with ThingsBoard Server once connectivity is restored.

For dashboard creation and management, see <DocLink product={props.product} path="user-guide/dashboards" target="_blank">Dashboards</DocLink>.

## Making Dashboard Public

<Aside type="note">
To access data from devices, assets, and entity views through the public link, make those entities public as well.
</Aside>

{props.product === Products.EDGE_PE && (
  <p>Dashboard groups are managed on ThingsBoard Server. The dashboard group must first be created on ThingsBoard Server and then assigned to the ThingsBoard Edge instance.</p>
)}

<Steps>
1. Go to **Dashboards** and open the dashboard menu. Click **Make public**.
2. The dashboard is now public. Click **Copy public link** to share it.
3. The public link is also available in the **Dashboard details** window.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/user-guide/public-dashboard/1-dashboard-section.webp',
    alt: 'Make public button in the Dashboards section',
  },
  {
    src: '/src/assets/images/edge/user-guide/public-dashboard/2-copy-the-link.webp',
    alt: 'Copy public link button',
  },
  {
    src: '/src/assets/images/edge/user-guide/public-dashboard/3-dashboard-details.webp',
    alt: 'Dashboard details window showing the public link',
  },
]} />

### Making Dashboard Private

<Steps>
1. Go to **Dashboards** and open the dashboard menu. Click **Make private**.
2. Click **Yes** to confirm.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/edge/user-guide/public-dashboard/4-dashboard-section.webp',
    alt: 'Make private button in the Dashboards section',
  },
  {
    src: '/src/assets/images/edge/user-guide/public-dashboard/5-confirm-going-private.webp',
    alt: 'Confirmation dialog for making dashboard private',
  },
]} />
