import DocLink from '@components/DocLink.astro';
import ImageGallery from '@components/ImageGallery.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';

A rule chain template is a rule chain created on the server under **Edge management → Rule chain templates**. It does not run on the server — it is a blueprint that gets pushed to one or more Edge instances when assigned. On Edge 4.0 and later, you can also create and edit rule chains directly on the Edge instance.

## Create a template on the server

Use this approach when you want to deploy the same rule chain logic to multiple Edge instances from one place.

<Steps>
1. On the server, go to **Edge management → Rule chain templates** and click **Add** (+).
2. Enter a name and click **Add**.
3. Open the new template, configure your rule nodes and connections, and click **Apply changes**.
</Steps>

The template is not active on any Edge until you assign it.

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/create-rule-chain-template-step-1.webp',
      alt: 'Edge management → Rule chain templates section with Add button',
      caption: 'Step 1: Go to Edge management → Rule chain templates and click Add.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/create-rule-chain-template-step-2.webp',
      alt: 'Dialog to enter the new rule chain template name',
      caption: 'Step 2: Enter a name and click Add.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/create-rule-chain-template-step-3.webp',
      alt: 'New template listed in the Rule chain templates section',
      caption: 'Step 3: Open the template to start editing.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/create-rule-chain-template-step-4.webp',
      alt: 'Rule chain editor with nodes configured and Apply changes button',
      caption: 'Step 3: Configure rule nodes and click Apply changes.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/create-rule-chain-template-step-1.webp',
      alt: 'Edge management → Rule chain templates section with Add button (PE)',
      caption: 'Step 1: Go to Edge management → Rule chain templates and click Add.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/create-rule-chain-template-step-2.webp',
      alt: 'Dialog to enter the new rule chain template name (PE)',
      caption: 'Step 2: Enter a name and click Add.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/create-rule-chain-template-step-3.webp',
      alt: 'New template listed in the Rule chain templates section (PE)',
      caption: 'Step 3: Open the template to start editing.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/create-rule-chain-template-step-4.webp',
      alt: 'Rule chain editor with nodes configured and Apply changes button (PE)',
      caption: 'Step 3: Configure rule nodes and click Apply changes.',
    },
  ]} />
)}

## Assign a template to an Edge instance

<Steps>
1. On the server, go to **Edge management → Instances** and open the target Edge instance context menu. Select **Manage rule chains**.
2. Click **Add** (+), select the template from the list, and click **Assign**.
</Steps>

The template is pushed to the Edge instance and becomes active on the next synchronization.

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/assign-rule-chain-template-step-1.webp',
      alt: 'Edge instance context menu with Manage rule chains option',
      caption: 'Step 1: Open the Edge instance context menu and select Manage rule chains.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/assign-rule-chain-template-step-2.webp',
      alt: 'Rule chains list for the Edge instance with Add button',
      caption: 'Step 2: Click Add to assign a template.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/assign-rule-chain-template-step-3.webp',
      alt: 'Dialog to select a rule chain template to assign',
      caption: 'Step 2: Select the template and click Assign.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/assign-rule-chain-template-step-4.webp',
      alt: 'Assigned template now listed in the Edge instance rule chains',
      caption: 'The assigned template appears in the rule chains list for this Edge instance.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/assign-rule-chain-template-step-1.webp',
      alt: 'Edge instance context menu with Manage rule chains option (PE)',
      caption: 'Step 1: Open the Edge instance context menu and select Manage rule chains.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/assign-rule-chain-template-step-2.webp',
      alt: 'Rule chains list for the Edge instance with Add button (PE)',
      caption: 'Step 2: Click Add to assign a template.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/assign-rule-chain-template-step-3.webp',
      alt: 'Dialog to select a rule chain template to assign (PE)',
      caption: 'Step 2: Select the template and click Assign.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/assign-rule-chain-template-step-4.webp',
      alt: 'Assigned template now listed in the Edge instance rule chains (PE)',
      caption: 'The assigned template appears in the rule chains list for this Edge instance.',
    },
  ]} />
)}

## Create a rule chain directly on Edge

Available from Edge 4.0. Rule chains created on Edge are automatically propagated to the server when the connection is available. Use this approach for Edge-specific logic that does not need to be shared across multiple instances.

<Steps>
1. On the Edge instance, go to **Rule chains** and click **Add** (+).
2. Select **Create new rule chain**, enter a name, and click **Add**.
3. Configure your rule nodes and click **Apply changes**.
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/1-edge-rule-chain.webp',
      alt: 'Rule chains section on Edge with Add button',
      caption: 'Step 1: Go to Rule chains on Edge and click Add.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/2-create-rule-chain-template.webp',
      alt: 'Dialog to name the new rule chain on Edge',
      caption: 'Step 2: Enter a name and click Add.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/3-in-the-list.webp',
      alt: 'New rule chain listed in the Edge Rule chains section',
      caption: 'The new rule chain appears in the list. Click it to start editing.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/4-configure-and-safe.webp',
      alt: 'Rule chain editor on Edge with Apply changes button',
      caption: 'Step 3: Configure rule nodes and click Apply changes.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/1-edge-rule-chain.webp',
      alt: 'Rule chains section on Edge with Add button (PE)',
      caption: 'Step 1: Go to Rule chains on Edge and click Add.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/2-create-rule-chain-template.webp',
      alt: 'Dialog to name the new rule chain on Edge (PE)',
      caption: 'Step 2: Enter a name and click Add.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/3-in-the-list.webp',
      alt: 'New rule chain listed in the Edge Rule chains section (PE)',
      caption: 'The new rule chain appears in the list. Click it to start editing.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/4-configure-and-safe.webp',
      alt: 'Rule chain editor on Edge with Apply changes button (PE)',
      caption: 'Step 3: Configure rule nodes and click Apply changes.',
    },
  ]} />
)}

## Verify a rule chain on Edge

After assigning a template or creating a rule chain on Edge, confirm it is active and configured correctly.

<Steps>
1. Log in to the Edge instance and go to **Rule chains**.
2. Open the rule chain and verify the node configuration matches what you defined.
</Steps>

{props.product === Products.EDGE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/verify-rule-chain-template-step-1.webp',
      alt: 'Rule chains section on Edge showing the assigned rule chain',
      caption: 'Step 1: Open the rule chain on Edge.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/verify-rule-chain-template-step-2.webp',
      alt: 'Rule chain editor showing the nodes and connections as configured',
      caption: 'Step 2: Verify the node configuration is accurate.',
    },
  ]} />
)}

{props.product === Products.EDGE_PE && (
  <ImageGallery images={[
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/verify-rule-chain-template-step-1.webp',
      alt: 'Rule chains section on Edge showing the assigned rule chain (PE)',
      caption: 'Step 1: Open the rule chain on Edge.',
    },
    {
      src: '/src/assets/images/docs/edge/rule-engine/pe/verify-rule-chain-template-step-2.webp',
      alt: 'Rule chain editor showing the nodes and connections as configured (PE)',
      caption: 'Step 2: Verify the node configuration is accurate.',
    },
  ]} />
)}
