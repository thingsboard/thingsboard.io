---
---
import { Aside } from '@astrojs/starlight/components';
import ImageGallery from '@components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';

ThingsBoard Edge supports two complementary automation mechanisms: **Scheduler events** and **Rule Chains**. Understanding when to use each one helps you build reliable, efficient automation on the Edge.

<Aside type="note">
  Scheduler is a Professional Edition feature.
</Aside>

**Scheduler events** perform periodic tasks automatically — telemetry and attribute updates, RPC commands, and more — triggered by time. See the <DocLink product={props.product} path="pe/user-guide/scheduler" target="_blank">Platform Scheduler</DocLink> and <DocLink product={props.product} path="edge/pe/user-guide/scheduler" target="_blank">Edge Scheduler</DocLink> documentation for details.

**Rule Chains** perform automated tasks in response to incoming data or events. <DocLink product={props.product} path="pe/user-guide/rule-engine" target="_blank">Rule nodes</DocLink> are the building blocks — for example, when a device sends temperature data above a threshold, the Rule Chain processes it and creates an alarm. See the <DocLink product={props.product} path="edge/pe/user-guide/rule-chain-templates" target="_blank">Rule Chain Templates</DocLink> documentation for Edge-specific details.

## Edge-specific considerations

| Topic | Detail |
|---|---|
| Rule Chain creation | Prior to Edge 4.0, Rule Chain templates had to be configured on the Platform and synchronized to Edge. Starting with **Edge 4.0**, Rule Chains can be created and edited directly on the Edge. |
| Scheduler creation | Scheduler events must always be configured on the Platform and assigned to Edge. |
| Execution | Once synchronized, both Scheduler events and Rule Chains run entirely on the Edge, independent of internet connectivity. |

## Comparison matrix

<ImageGallery images={[
  {
    src: '/src/assets/images/docs/edge/user-guide/scheduler/comparison-matrix.webp',
    alt: 'Scheduler vs Rule Chain comparison matrix',
    caption: 'Feature comparison between Scheduler events and Rule Chain templates.',
  },
]} />

## When to use each

**Use Scheduler when:**
- A time-based trigger is required.
- The automation is periodic or scheduled, independent of incoming device data.

**Use Rule Chains when:**
- Real-time reaction to events or telemetry is needed.
- Complex processing logic, branching, or external API calls are required.
- You need to process local data — attribute updates, alarms, incoming messages.

## Using both together

Combining Scheduler events and Rule Chain templates produces the most flexible automation on Edge.

<ImageGallery images={[
  {
    src: '/src/assets/images/docs/edge/user-guide/scheduler/both-feature-logic.webp',
    alt: 'Scheduler and Rule Chain used together for automation logic',
    caption: 'Scheduler events and Rule Chains complement each other for complex automation.',
  },
]} />

### Use case: pump shutdown with delayed status check

To turn off pumps every night at 7 PM and raise an alarm if a pump is still running 10 minutes later, configure two Scheduler events and a Rule Chain template.

<ImageGallery images={[
  {
    src: '/src/assets/images/docs/edge/user-guide/scheduler/first-event.webp',
    alt: 'First scheduler event — stop pump at 7 PM',
    caption: 'First event: sends {"action": "stop_pump"} via RPC to stop the pump every night at 7 PM.',
  },
  {
    src: '/src/assets/images/docs/edge/user-guide/scheduler/second-event.webp',
    alt: 'Second scheduler event — check pump status at 7:10 PM',
    caption: 'Second event: sends {"action": "check_pump_status"} as a custom message at 7:10 PM.',
  },
  {
    src: '/src/assets/images/docs/edge/user-guide/scheduler/rule-chain-template.webp',
    alt: 'Rule Chain template processing both scheduler events',
    caption: 'The Rule Chain routes the first event to an RPC call node, and the second event checks pumpStatus telemetry — raising an alarm if the pump is still ON.',
  },
]} />
