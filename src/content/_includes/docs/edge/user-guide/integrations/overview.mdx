---
---
import DocImage from '@components/DocImage.astro';
import ImageGallery from '@components/ImageGallery.astro';
import { Aside } from '@astrojs/starlight/components';

Edge Integrations let you connect ThingsBoard Edge to external brokers and servers — MQTT, HTTP, CoAP, OPC-UA, TCP, UDP, and more — and process incoming data on the Edge before syncing it to the Cloud.

They work like Platform Integrations but with one key difference: instead of creating integrations directly, you create **Integration templates** and **Converter templates** in the Cloud and then assign them to one or more Edge instances.

## Deployment options

Each integration can run in one of two modes.

### Embedded integrations

An embedded integration runs inside the main ThingsBoard Edge process.

<DocImage src="/src/assets/images/docs/edge/user-guide/integrations/embeded-integrations-overview.svg" alt="Embedded integration architecture: external data source connects directly to ThingsBoard Edge, which forwards data to the ThingsBoard Server" width="700px" />

**Advantages:**
- No extra packages to install — the integration deploys in a few clicks from the UI
- Minimal message delivery latency

**Trade-offs:**
- Shares CPU, memory, and network connections with the Edge process
- Lower isolation from the main Edge process

### Remote integrations

A remote integration runs as a separate process on the local network and streams data to the Edge over the network.

<DocImage src="/src/assets/images/docs/edge/user-guide/integrations/remote-integrations-overview.svg" alt="Remote integration architecture: external data source connects to a remote integration process on the local network, which streams data to ThingsBoard Edge, which forwards to ThingsBoard Server" width="700px" />

This is useful when the broker or server (e.g. a local MQTT broker or OPC-UA server) has no external IP address and cannot be reached by the Edge directly. The remote integration is deployed on the same local network as the broker, pulls data from it, and streams it to the Edge whenever a connection is available.

**Advantages:**
- Works with servers that have no external IP address
- Fully isolated from the Edge process

**Trade-offs:**
- Requires installing and maintaining a separate package

## Converter templates

Converter templates transform raw integration payloads into ThingsBoard telemetry and attribute data. They are created by a tenant administrator in the Cloud under **Edge management > Converter templates**.

<ImageGallery images={[
  {
    src: '/src/assets/images/docs/edge/user-guide/integrations/create-converter-step-1.webp',
    alt: 'Create Converter template — step 1',
    caption: 'Create Converter template',
  },
  {
    src: '/src/assets/images/docs/edge/user-guide/integrations/create-converter-step-2.webp',
    alt: 'Converter template configuration',
    caption: 'Converter template configuration',
  },
]} />

<Aside type="note">
  You do not need to assign converter templates to an Edge directly. When an integration template is assigned to an Edge, its Uplink and Downlink Converters are automatically propagated to that Edge.
</Aside>

## Integration templates

Once your Converter template is ready, create an Integration template under **Edge management > Integration templates** and link the converter to it. Integration templates are assigned to Edge instances — not run in the Cloud.

<ImageGallery images={[
  {
    src: '/src/assets/images/docs/edge/user-guide/integrations/create-integration-step-1.webp',
    alt: 'Create Integration template — step 1',
    caption: 'Create Integration template',
  },
  {
    src: '/src/assets/images/docs/edge/user-guide/integrations/create-integration-step-2.webp',
    alt: 'Integration template configuration',
    caption: 'Integration template configuration',
  },
]} />

Any changes made to an Integration or Converter template in the Cloud are automatically propagated to all Edges that have it assigned. **Templates cannot be edited on the Edge.**

### Configuration placeholders

If the same Integration template is used across multiple Edges but each Edge needs a different value for a specific field (such as a host address or base URL), you can use **placeholders**.

Add an attribute to the Edge instance, then reference it in the Integration template using `${attributeKey}` syntax. When the integration is deployed to that Edge, the placeholder is replaced with the attribute's value.

The example below shows an HTTP integration configured with a unique **Base URL** per Edge using the `ipAddress` attribute:

<ImageGallery images={[
  {
    src: '/src/assets/images/docs/edge/user-guide/integrations/placeholder-feature-step-1.webp',
    alt: 'Add ipAddress attribute to the Edge instance',
    caption: 'Go to Edge management > Instances, open the Edge, select the Attributes tab, and add the ipAddress attribute.',
  },
  {
    src: '/src/assets/images/docs/edge/user-guide/integrations/placeholder-feature-step-2.webp',
    alt: 'Add the ${ipAddress} placeholder to the Integration template configuration',
    caption: 'In the Integration template, enter ${ipAddress} in the Base URL field.',
  },
  {
    src: '/src/assets/images/docs/edge/user-guide/integrations/placeholder-feature-step-3.webp',
    alt: 'Open Manage Integrations for the Edge',
    caption: 'Go to Edge management > Instances and click the Manage Integrations button for the Edge.',
  },
  {
    src: '/src/assets/images/docs/edge/user-guide/integrations/placeholder-feature-step-4.webp',
    alt: 'Assign the Integration template to the Edge',
    caption: 'Click "Assign to edge" and select the integration from the drop-down, then confirm.',
  },
  {
    src: '/src/assets/images/docs/edge/user-guide/integrations/placeholder-feature-step-5.webp',
    alt: 'Verify the placeholder is resolved on the Edge',
    caption: 'On the Edge, go to Integration center > Integrations and open the integration to confirm the Base URL shows the resolved attribute value.',
  },
]} />

If an Edge is missing the attribute referenced by a placeholder, the platform will warn you during assignment or when the integration configuration is updated:

<ImageGallery images={[
  {
    src: '/src/assets/images/docs/edge/user-guide/integrations/missing-placeholder.webp',
    alt: 'Warning shown when a placeholder attribute is missing on the Edge',
    caption: 'The platform warns you when a required placeholder attribute is not set on the Edge.',
  },
]} />

## Edge limitations

In the current release, the Edge cannot create customers or entity groups. This affects Uplink Data Converter behavior:

- If a device type referenced in the converter does not exist on the Edge, the `default` device type is used.
- If the target Customer is not available on the Edge, the device is assigned to the Tenant.
- If the target entity group does not exist on the Edge, the `All` group is used.
