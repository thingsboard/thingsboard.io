import DocLink from '@components/DocLink.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import { Products } from '~/models/site.models';

ThingsBoard Version Control service lets you export and restore ThingsBoard entities using Git.
As a tenant administrator, configure access to a Git repository through the UI or REST API.
As a platform user, export single or multiple entities, browse version history, and restore entities to a specific version.

This feature improves collaboration when multiple engineers work on the same <DocLink product={props.product} path="user-guide/rule-engine">rule chain</DocLink> or <DocLink product={props.product} path="user-guide/dashboards">dashboard</DocLink> and simplifies CI/CD.
It also lets you clone solutions between tenants or platform instances.

Available since ThingsBoard **3.4**.

## Architecture

### Entity External ID

Every ThingsBoard entity has an `id` field — the unique identifier of the entity within a particular ThingsBoard environment.
Every exportable entity also has an `externalId` field, used to identify the same entity when importing and exporting between multiple environments.
Both fields are [UUIDs](https://en.wikipedia.org/wiki/Universally_unique_identifier).

The `externalId` is also used to automatically substitute entity IDs in rule chains (rule nodes) and dashboards (aliases and widget actions).
If you import a rule chain that references specific devices or assets, make sure you have exported and imported the corresponding devices or assets as well.

### Exportable entities

{props.product === Products.PE ? (
  <p>Supported entities: Device, Asset, Entity View, Customer, Dashboard, Widget Bundle, Rule Chain, Entity Group, Role, Converter, and Integration.</p>
) : (
  <p>Supported entities: Device, Asset, Entity View, Customer, Dashboard, Widget Bundle, and Rule Chain.</p>
)}

<Aside type="note">
  The User entity is intentionally excluded — user emails are unique across the platform instance, and exporting credentials to Git would be a security concern.
</Aside>

When exporting an entity, the system stores its JSON representation in Git. You can also export entity attributes, relations, and credentials (devices only).

### Repository structure

When you first export an entity, the entity `id` is used to name the file inside the Git repository.
When you import entities from Git back to ThingsBoard, the `id` from the file name becomes the `externalId` of the entity.
The `externalId` is unique within a tenant, so you can import and export between tenants of the same instance or between different instances.
Every subsequent export or import uses the `externalId` to find the right entity to update.

**Example:** export a dashboard named "Dashboard 1" with id `4864b750-da7d-11ec-a496-97fa2815d2fe` from a development instance. The repository will contain:

```bash
dashboard/4864b750-da7d-11ec-a496-97fa2815d2fe.json
```

Import this dashboard into a production instance — it will receive a new `id`, but its `externalId` will be set to `4864b750-da7d-11ec-a496-97fa2815d2fe`.

{props.product === Products.PE && (
  <Aside type="note">
    Customer hierarchy is stored in the <code>hierarchy</code> folder (recursive, mirroring the Customer hierarchy page in the UI).
    Entity groups are stored in the <code>groups</code> folder. Each group has an <code>id.json</code> file for the group entity and an <code>id_entities.json</code> file listing entity IDs in the group.
    The reserved "All" group does not contain <code>id_entities.json</code> because it includes all entities automatically.
  </Aside>
)}

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/version-control/version-control-4.png', alt: 'Git repository structure' },
]} />

### Sync strategy

The platform supports two sync strategies for export:

| Strategy | Description |
|----------|-------------|
| **Merge** (default) | Appends selected entities to the repository without deleting other files. Use when saving specific files. |
| **Overwrite** | Completely rewrites the corresponding repository files. Use when you want to fully synchronize a set of entities (e.g., all Dashboards) between your instance and Git. Entities previously saved but no longer present in your instance will be deleted from Git in the corresponding commit. |

### Scalability

The Version Control service runs as part of a monolithic ThingsBoard instance or as a separate microservice for horizontal scalability.
Each service instance handles synchronization for specific tenant partition(s) in the cluster.

Each "commit" API call may take some time. Concurrent commits within the same tenant are not supported — if a new commit request arrives while one is already in progress, the system cancels the in-progress call.

## Usage

### Git settings

Navigate to the **Version control** page in the **Advanced features** section. Configure the Git repository URL, default branch name, and authentication settings.
Provide a URL to an **empty** Git repository.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/version-control/version-control-1.png', alt: 'Configure Git settings' },
]} />

### Export to Git

#### Single entity export

Navigate to an entity's details and open the **Version control** tab.
Rule chains and dashboards have a built-in version control button and popup in their respective editors.

**Device:**

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/version-control/version-control-devices-1.png', alt: 'Device entity export' },
  { src: '/src/assets/images/user-guide/version-control/version-control-devices-2.png', alt: 'Device version history' },
]} />

**Rule chain:**

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/version-control/version-control-rule-chain-1.png', alt: 'Rule chain export' },
  { src: '/src/assets/images/user-guide/version-control/version-control-rule-chain-2.png', alt: 'Rule chain version history' },
]} />

**Dashboard:**

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/version-control/version-control-dashboard-1.png', alt: 'Dashboard export' },
  { src: '/src/assets/images/user-guide/version-control/version-control-dashboard-2.png', alt: 'Dashboard version history' },
]} />

#### Multiple entity export

Navigate to the **Version control** page. Select one or more entity types to export — by default, all entity types are selected.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/version-control/version-control-2.png', alt: 'Select entity types for export' },
  { src: '/src/assets/images/user-guide/version-control/version-control-3.png', alt: 'Entity version history' },
]} />

#### Auto-commit

Auto-commit automatically saves dashboards and rule chains to Git whenever you save the entity through the UI or REST API.
The commit happens asynchronously to keep the UI responsive.

<Aside type="note">
  Auto-commit does not trigger when you assign an entity to a customer (change entity owner).
  In that case, commit all entities of the specific type using the **Overwrite** sync strategy.
</Aside>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/version-control/auto-commit-settings-1.png', alt: 'Auto-commit settings' },
]} />

### Restore from Git

Navigate to the **Version control** page. Select the commit and specify restore settings.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/version-control/version-control-5.png', alt: 'Restore specific version from Git' },
]} />
