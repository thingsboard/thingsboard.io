import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import ImageGallery from '~/components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';
import { Products } from '~/models/site.models';
import { Steps, Aside } from '@astrojs/starlight/components';
import ConditionalHeading from '~/components/ConditionalHeading.astro';

**Dashboards** are configurable pages for visualizing and interacting with your IoT data. They are built from widgets — charts, tables, maps, controls, and more — and can display data from any combination of devices, assets, or other entities. 

Before diving in, review the <DocLink product={props.product} path="user-guide/data-visualization">key concepts</DocLink> — states, aliases, layouts, actions, and time windows — that make up every dashboard.

<Aside type="tip" title="IoT solution templates">
  ThingsBoard ships with ready-made IoT solution templates to reduce time-to-market. Each template includes pre-built dashboards, processing logic, sample devices, users, and all related entities — a complete PoC/MVP out of the box.
</Aside>

---

## Create a new dashboard

<Steps>
  <ol>
    <li>From the sidebar, navigate to the <strong>Dashboards</strong>.</li>
    <li>Click the <strong>+ Add dashboard &#8702; Create new dashboard</strong> in the top right corner.</li>
    <li>Enter a dashboard <strong>title</strong> (all other settings are optional).</li>
    {props.product === Products.PE && <><li>If needed: In <strong>Owner and groups</strong>, change the dashboard owner or add it to a group.</li></>}
    <li>Click <strong>Add</strong> — the dashboard opens automatically.</li>
    <li>Click <strong>Save</strong> in the upper-right corner to store it.</li>
  </ol>
</Steps>

---

## Edit mode

In the dashboard **edit mode**, you can add widgets, configure settings, modify the layout, etc.

To switch to dashboard edit mode, click the **Edit mode** button in the top-right corner.  

After making changes, click **Save** to apply them or **Cancel** to discard them.

---

## Dashboard toolbar

The dashboard toolbar provides tools for adding widgets, exporting dashboard, and managing states, layouts, aliases, filters, time window, and other settings.  
Some controls are visible only in edit mode; others are available in view mode. 

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/dashboard-toolbar-view-mode.png', alt: 'View mode toolbar', caption: 'Dashboard toolbar in view mode.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-toolbar-edit-mode.png', alt: 'Edit mode toolbar', caption: 'Dashboard toolbar in edit mode.' },
]} />

<Aside type="note" title="Toolbar settings">
  The visible tools can be configured in <strong>dashboard &#8702; edit mode &#8702;</strong> toolbar <strong>settings</strong>.
</Aside>

---

### Time window

The dashboard **time window** defines the time interval for working with telemetry data. It is used by all time series and alarm widgets unless they are configured to use their own widget time window.

See <DocLink product={props.product} path="user-guide/time-window" target="_blank">time window</DocLink> for complete details.

### Filters

Filters narrow which data appears on the dashboard. 

Each filter checks one or more key conditions against an attribute or telemetry key — for example, show only devices where `model = "DHT22"` and `battery < 20`.

Multiple key filters are combined with `and`; within a single key, complex expressions with `and`/`or` like `(A > 0 and A < 20) or (A = 50 or A = 60)` are supported.

<Aside type="caution">
  Filters apply only to the **latest** value of an attribute or time series key. Do not use filters to exclude historical time series values.
</Aside>

<h4>Add a filter</h4>

<Steps>
1. Enter edit mode and click **Filters** in the toolbar.
2. Click **Add filter**.
3. Enter the filter name and click **Add key filter**.
4. Configure the key filter:
    - Select the **key type**.
    - Enter the **key name**.
    - Choose the **value type**.
    - Define the **filter condition**: **operation** and **value** (*static* or *dynamic*).
    - Add additional key filters if needed.
5. Click **Add** (bottom-right) to save the key filter.
6. Click **Add** to create the filter.
7. To save and apply the filter click **Save**.
</Steps>

<Aside type="note">
  Use the slider **Editable** next to the filter name to control whether end users can edit the filter.
</Aside>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/dashboard-filters-1.png', alt: 'Add filter', caption: 'Add a dashboard filter.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-filters-2.png', alt: 'Filter configuration', caption: 'Configure the filter.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-filters-3.png', alt: 'Key filter', caption: 'Define a key filter with conditions.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-filters-4.png', alt: 'Filter expression', caption: 'Configure filter expressions.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-filters-5.png', alt: 'Filter applied', caption: 'The filter is applied.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-filters-6.png', alt: 'Filter result', caption: 'Dashboard data filtered.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-filters-7.png', alt: 'User edit toggle', caption: 'Toggle user ability to edit filters.' },
]} />

### Entity aliases

Aliases define data sources for widgets. Two basic types: Static alias (fixed entity) and Dynamic alias (resolved at runtime based on device type, user context, etc.).

See <DocLink product={props.product} path="user-guide/aliases" bold={true} target="_blank">entity aliases</DocLink> for the full list of alias types and configuration options.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/dashboard-aliases-1.png', alt: 'Entity aliases', caption: 'Open the entity aliases dialog.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-aliases-2.png', alt: 'Static alias', caption: 'Configure a static alias.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-aliases-3.png', alt: 'Dynamic alias', caption: 'Configure a dynamic alias.' },
]} />

### Settings

Configure dashboard appearance and behavior — state controller, title, logo, toolbar visibility, layout, and advanced CSS.  
Click the **gear** icon in the edit mode toolbar.

<h4>State controller</h4>

Determines how dashboard states are driven. Default: **entity** — state is bound to the currently selected entity context.

<h4>Title</h4>

Enable **display dashboard title** to show the title in the top-left corner. Adjust color and transparency with the color picker.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/dashboard-settings-1.png', alt: 'Dashboard settings', caption: 'Open dashboard settings.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-settings-2.png', alt: 'Title settings', caption: 'Configure the dashboard title.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-settings-3.png', alt: 'Title result', caption: 'Dashboard with customized title.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-settings-title-color-1.png', alt: 'Title color', caption: 'Customize the title color.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-settings-title-color-2.png', alt: 'Title color result', caption: 'Dashboard with colored title.' },
]} />

<h4>Logo</h4>

Enable **Display logo in dashboard fullscreen mode** and upload an image. The logo appears in the top-left corner when the dashboard is in fullscreen.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/dashboard-logo-settings-2.png', alt: 'Logo settings', caption: 'Configure the dashboard logo.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-logo-settings-3.png', alt: 'Upload logo', caption: 'Upload a logo image.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-logo-settings-4.png', alt: 'Logo displayed', caption: 'Logo displayed in fullscreen mode.' },
]} />

<h4>Toolbar settings</h4>

The checkboxes control the visibility of the corresponding options in the dashboard toolbar.

| Toggle | Description |
|--------|-------------|
| **Hide toolbar** | Entirely hides the toolbar; only the **Enter edit mode** (pencil) icon is displayed in the top-right corner. |
| **Keep toolbar opened** | Keeps the toolbar always expanded; when off, it collapses into a three-dots menu. |
| **Display dashboards selection** | Shows/hides the dashboards selector in the toolbar. |
| **Display entities selection** | Shows/hides the entities selector in the toolbar. |
| **Display filters** | Shows/hides the filters button (only if filters are defined). |
| **Display time window** | Shows/hides the time window control. |
| **Display export** | Shows/hides the export button. |
| **Display update dashboard image** | Shows/hides the update dashboard image button. |

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/toolbar-settings-1.png', alt: 'Toolbar settings', caption: 'Open toolbar settings.' },
  { src: '/src/assets/images/user-guide/dashboards/toolbar-settings-2.png', alt: 'Toolbar toggles', caption: 'Toggle toolbar controls.' },
  { src: '/src/assets/images/user-guide/dashboards/toolbar-settings-3.png', alt: 'Hide toolbar', caption: 'Option to hide the entire toolbar.' },
  { src: '/src/assets/images/user-guide/dashboards/toolbar-settings-4.png', alt: 'Toolbar hidden', caption: 'Dashboard with hidden toolbar.' },
  { src: '/src/assets/images/user-guide/dashboards/toolbar-settings-5.png', alt: 'Keep toolbar open', caption: 'Keep the toolbar open by default.' },
  { src: '/src/assets/images/user-guide/dashboards/toolbar-settings-6.png', alt: 'Toolbar open result', caption: 'Dashboard with toolbar always open.' },
]} />

<h4>Advanced settings</h4>

**Dashboard CSS** — apply custom CSS scoped to the dashboard.

<h4>Layout settings</h4>

Configure the default layout grid and appearance:

- **Columns count** — number of columns in the dashboard grid (default: 24).
- **Minimum layout width** — minimum width in columns before the layout scrolls horizontally.
- **Margin between widgets** — spacing between widgets in pixels.
- **Apply margin to the sides of the layout** — adds outer padding around the widget grid.
- **Auto fill layout height** — stretches the layout to fill the full page height.
- **Background color** — sets a solid background color for the dashboard.
- **Background image** — sets a background image (browse from gallery or set a link); configure **Background size mode** (e.g. Fit width).

<h4>Mobile layout settings</h4>

- **Auto fill layout height** — applies auto-fill to the mobile layout.
- **Mobile row height** — height of each row in the mobile layout (default: 70 px).

{props.product !== Products.CE && (
  <>
    <ConditionalHeading level={3} id="generate-report" showFor="pe">Generate a report</ConditionalHeading>
    <p>
      A Tenant Administrator or Customer User can generate a report from the currently opened dashboard in PDF, PNG, or JPEG format.<br/><br/> 
      Click the <strong>Export dashboard</strong> button on the right side of the toolbar, then select the desired format. The report is automatically downloaded.
    </p>
    <MultiProductImageGallery product={props.product} images={[
      { src: '/src/assets/images/user-guide/dashboards/generate-report-from-dashboard-1.png', alt: 'Generate report', caption: 'Click Export Dashboard and select a format.', products: [Products.PE] },
      { src: '/src/assets/images/user-guide/dashboards/generate-report-from-dashboard-2.png', alt: 'Report generating', caption: 'Report generation starts automatically.', products: [Products.PE] },
      { src: '/src/assets/images/user-guide/dashboards/generate-report-from-dashboard-3.png', alt: 'Report downloaded', caption: 'The report is downloaded to your PC.', products: [Products.PE] },
    ]} />
    <p>
      Report generation can also be automated on a schedule using the <DocLink product={props.product} path="user-guide/scheduler">event scheduler</DocLink> with the "Generate Dashboard Report" event type.
    </p>
  </>
)}

## Layouts

ThingsBoard uses a grid-based layout system to organize widgets within a dashboard.  
A single dashboard can include multiple layouts (states) to support advanced navigation scenarios and different visual configurations for various use cases and screen sizes.

See <DocLink product={props.product} path="user-guide/layouts">Layouts</DocLink> for full details.

## States

States create a layered hierarchy in your dashboard — for example, a root state listing all devices and a detail state showing data for a selected device. 

To access states management, in edit mode, click the **States** icon (**Manage dashboard states**) on the left of the toolbar.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-1.png', alt: 'Add state', caption: 'Enter \"edit mode\", then click \"States\" icon (\"Manage dashboard states\") in the upper-left corner of the dashboard. This opens the dashboard states configuration window.' },
]} />

<h3>Add a state</h3>

<Steps>
1. In edit mode, click **Manage dashboard states** in the toolbar.
2. Click the **+** icon in the right upper of the manage dashboard states window.
2. Enter a state name (the State ID is auto-generated but editable). Use `${entityName}` for dynamic naming.
3. Click **Add**, then **Save**.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-2.png', alt: 'State name', caption: 'To add a new state, click the "+" icon in the right upper of the manage dashboard states window.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-4.png', alt: 'State created', caption: 'Enter a state name, State ID will form automatically according to the name, but you can change it. Use ${entityName} for dynamic naming. Click "Add".' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-5.png', alt: 'Configure action', caption: 'After adding states, save all changes.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-6.png', alt: 'Action type', caption: 'You can navigate between states through a drop-down menu.' },
]} />

**Root state**

Every dashboard starts with a single **Root state**. To change which state is the root, edit the state and enable the **Root state** checkbox.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-root-1.png', alt: 'Root state', caption: 'You can change a root state by clicking the \"pencil\" icon (\"Edit dashboard state\").' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-root-2.png', alt: 'Root state configuration', caption: 'Tick the \"Root state\" checkbox, and save changes.' },
]} />

### Navigate between states via widget actions

Configuring the appropriate widget action allows you to navigate between dashboard states.

In this example, the <DocLink product={props.product} path='user-guide/actions/#navigate-to-new-dashboard-state' bold={true} target="_blank">Navigate to new dashboard state</DocLink> action type is used.

<Aside type="note" title="Widget actions">
  For detailed information about available action types and configuration options, refer to the corresponding <DocLink product={props.product} path='user-guide/actions/' bold={true} target="_blank">documentation section</DocLink>.
</Aside>

**Preparation**
1. Prepare a **table widget** that displays multiple devices (or other entities).
2. Add a new **dashboard state**. Use `${entityName}` as the state name to dynamically generate the state name based on the selected entity.

**Configure widget action**

<Steps>
1. Open the dashboard in **Edit mode**.
2. Open widget settings by clicking the **edit** (pencil) icon in the top-right corner of the widget.  
3. Navigate to the **Actions** tab.
4. Click **+ Add action**.
5. Configure the action parameters: 
    - **Action source:** *On row click* 
    - **Name:** enter a descriptive name
    - **Action type:** *Navigate to new dashboard state* 
    - **Target state:** select the previously created state
    - Click **Add**.
4. Click **Apply** to save widget changes.
5. Click **Save** to save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-7.png', alt: 'Action target', caption: 'Open widget settings by clicking the \"pencil\" icon in the top-right corner of the widget.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-8.png', alt: 'Action saved', caption: 'Go to the \"Actions\" tab and click \"+\" (\"Add action\").' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-9.png', alt: 'Navigation test', caption: 'Select \"On row click\" as \"action source\", enter the \"action name\", set **action type:** - *Navigate to new dashboard state* and specify the state to which you will be transferred. Then click "Add".' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-10.png', alt: 'Detail state', caption: 'Click Apply to save widget changes. Then, save the dashboard.' },
]} />

**Test navigation**

After saving the dashboard, click a row in the table widget to trigger the action. 

The dashboard switches to the selected state associated with the clicked entity.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-11.png', alt: 'State hierarchy', caption: 'Click on the device row to activate the action.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-states-12.png', alt: 'State widget', caption: 'After clicking, we immediately transfer to the chosen state.' },
]} />

## Add a new widget

**Widgets** are the core building blocks of a dashboard — including charts, tables, maps, controls, alarms, and other visualization components.  
Widgets provide configurable settings and parameters that allow users to customize behavior and appearance according to specific use cases. 

For detailed information, see <DocLink product={props.product} path="user-guide/widgets" target="_blank">Widgets</DocLink>.

## Dashboard management

[//]: # (Manage owner and groups PE)
{props.product !== Products.CE && (
<>
  <ConditionalHeading level={3} id="manage-owner-and-groups" showFor="pe">Manage owner and groups</ConditionalHeading>
  <p>
    Reassign the dashboard to a different owner or move it into a group. Only users associated with the selected owner will have access.
  </p>
  <Steps>
    <ol>
      <li>Go to the <strong>Dashboards</strong> page.</li>
      <li>Click the <strong>Manage owner and groups</strong> icon next to the dashboard.</li>
      <li>In the <strong>Owner</strong> field, select a new owner from the dropdown.</li>
      <li>Optionally, select an existing dashboard group or create a new one.</li>
      <li>Click <strong>Update</strong>.</li>
    </ol>
  </Steps>
  <Aside type="note">
    A Tenant Administrator always has the right to reassign or revoke dashboard ownership.
  </Aside>
  <ImageGallery images={[
    { src: '/src/assets/images/user-guide/dashboards/dashboard-manage-owner-and-groups-1-pe.png', alt: 'Manage owner', caption: 'Open dashboard details.' },
    { src: '/src/assets/images/user-guide/dashboards/dashboard-manage-owner-and-groups-2-pe.png', alt: 'Select owner', caption: 'Click <strong>Manage owner and groups</strong>.' },
    { src: '/src/assets/images/user-guide/dashboards/dashboard-manage-owner-and-groups-3-pe.png', alt: 'Add to group', caption: 'Select a new owner. Add to a dashboard group. Click <strong>Update</strong>.' },
    { src: '/src/assets/images/user-guide/dashboards/dashboard-manage-owner-and-groups-4-pe.png', alt: 'Owner updated', caption: 'Owner and groups updated.' },
  ]} />
  <Aside type="tip" title="Include customer entities">
    This option controls whether customer-owned dashboards are displayed in the list.<br/>
    <ul>
      <li><strong>Enabled (default):</strong> shows all dashboards, including customer-owned.</li>
      <li><strong>Disabled:</strong> shows only your own dashboards.</li>
    </ul>
    <ImageGallery images={[
      { src: '/src/assets/images/user-guide/dashboards/include-customer-dashboards-1-pe.png', alt: 'Include customer toggle', caption: 'Toggle <strong>Include customer entities</strong>.' },
      { src: '/src/assets/images/user-guide/dashboards/include-customer-dashboards-2-pe.png', alt: 'Customer entities shown', caption: 'Customer-owned dashboards are shown or hidden.' },
    ]} />
  </Aside>
</>
)}

[//]: # (### Share dashboard group PE)
{props.product !== Products.CE && (
  <>
    <ConditionalHeading level={3} id="share-dashboard-group" showFor="pe">Share dashboard group</ConditionalHeading>
    <p>
      In ThingsBoard PE, dashboards cannot be shared individually. A dashboard must be shared as part of a <strong>dashboard group</strong> that contains the required dashboard.
    </p>
    <p>When sharing, choose a permission level:</p>
    <ul>
      <li><strong>Read</strong> — the customer can view the dashboards but cannot modify them.</li>
      <li><strong>Write</strong> — the customer can view and edit the dashboards.</li>
      <li><strong>Role</strong> — apply a previously created custom Role with fine-grained permissions.</li>
    </ul>
    <Aside type="tip">
      A <strong>Role</strong> is a named set of permissions (e.g. read, write, delete) that can be reused across multiple entity groups. See <DocLink product={props.product} path="user-guide/roles">Role-Based Access Control</DocLink> for details.
    </Aside>
    <Steps>
      <ol>
        <li>Go to the <strong>Dashboards &#8702; Groups</strong>.</li>
        <li>Click the <strong>Share</strong> icon next to the dashboard group.</li>
        <li>Select the customer(s) to share with.</li>
        <li>Set the permission: <strong>Read</strong>, <strong>Write</strong>, or select a previously created <strong>Role</strong>.</li>
        <li>Click <strong>Share</strong>.</li>
      </ol>
    </Steps>
    <ImageGallery images={[
      { src: '/src/assets/images/user-guide/dashboards/share-dashboard-group-1-pe.png', alt: 'Share group', caption: 'Click the <strong>Share</strong> icon next to the dashboard group.' },
      { src: '/src/assets/images/user-guide/dashboards/share-dashboard-group-2-pe.png', alt: 'Select customer', caption: 'Select a customer and set permissions. Click <strong>Share</strong>.' },
    ]} />
    <Aside type="note">
      Sharing grants access to the dashboard only — not to the data it displays. The customer must also have access to the associated devices or entities for widgets to show data.
    </Aside>
  </>
)}

[//]: # (Make dashboard group public PE)
{props.product !== Products.CE && (
<>
  <ConditionalHeading level={3} id="make-dashboard-group-public" showFor="pe">Make dashboard group public</ConditionalHeading>
  <p>
    Make a dashboard group and all its dashboards <strong>public</strong> to allow access without authentication.<br/>
    This is useful for providing read-only access to external users without a ThingsBoard account, displaying dashboards on public screens, or embedding them into external websites.
  </p>
  <Steps>
    <ol>
      <li>Go to the <strong>Dashboards &#8702; Groups</strong>.</li>
      <li>Click the <strong>Make public</strong> icon next to the dashboard group.</li>
      <li>Confirm making the group public.</li>
    </ol>  
  </Steps>
  <ImageGallery images={[
    { src: '/src/assets/images/user-guide/dashboards/make-public-dashboard-1-pe.png', alt: 'Make group public', caption: 'Click <strong>Make public</strong> on the dashboard group.' },
    { src: '/src/assets/images/user-guide/dashboards/make-public-dashboard-2-pe.png', alt: 'Confirm public', caption: 'Confirm making the group public. The dashboard group is now public. ' },
  ]} />
  <p>
    Share a link to a public dashboard.
  </p>
  <Steps>  
    <ol>
      <li>Open the now-public group and click the <strong>Public dashboard link</strong> icon next to the desired dashboard.</li>
      <li>Click <strong>Copy public link</strong> and share it.</li>
    </ol>
  </Steps>
  <ImageGallery images={[
    { src: '/src/assets/images/user-guide/dashboards/make-public-dashboard-3-pe.png', alt: 'Group is public', caption: 'Open public dashboards group.' },
    { src: '/src/assets/images/user-guide/dashboards/make-public-dashboard-4-pe.png', alt: 'Copy public link', caption: 'Copy the public link for individual dashboard and share it.' },
  ]} />
  <Aside type="note">
    To display data on a public dashboard, the associated device or asset groups must also be made public.
  </Aside>
  <p>
    To revert, click the <strong>Make private</strong> icon next to the dashboard group and confirm.
  </p>
  <ImageGallery images={[
    { src: '/src/assets/images/user-guide/dashboards/make-public-dashboard-5-pe.png', alt: 'Make private', caption: 'Click Make private to revoke public access.' },
    { src: '/src/assets/images/user-guide/dashboards/make-public-dashboard-6-pe.png', alt: 'Group is private', caption: 'Confirm the action. The dashboard group is private again.' },
  ]} />
</>
)}

<br/>

[//]: # (Make dashboard public CE)
{props.product === Products.CE && (
<>
  <ConditionalHeading level={3} id="make-dashboard-public" showFor="ce">Make dashboard public</ConditionalHeading>
  <p>
    Make the dashboard <strong>public</strong> to allow access without authentication.<br/>
    This is useful for providing read-only access to external users without a ThingsBoard account, displaying dashboards on public screens, or embedding them into external websites.
  </p>
  <Steps>
    <ol>
      <li>Go to the <strong>Dashboards</strong> page.</li>
      <li>Click the <strong>Make dashboard public</strong> icon next to the dashboard.</li>
      <li>In the dialog, click <strong>Copy public link</strong> to get the shareable URL.</li>
      <li>Share the link — anyone with it can view the dashboard without logging in.</li>
    </ol>
  </Steps>
  <ImageGallery images={[
    { src: '/src/assets/images/user-guide/dashboards/make-public-dashboard-1-ce.png', alt: 'Make public toggle', caption: 'Click <strong>Make dashboard public</strong>.' },
    { src: '/src/assets/images/user-guide/dashboards/make-public-dashboard-2-ce.png', alt: 'Copy public link', caption: 'Copy the public link. Share the link — anyone with it can view the dashboard without logging in.' },
    { src: '/src/assets/images/user-guide/dashboards/make-public-dashboard-3-ce.png', alt: 'Public dashboard', caption: 'The dashboard is now public.' },
  ]} />
  <Aside type="note">
    To display data on a public dashboard, the associated entities (devices or assets) must also be made public individually.
  </Aside>
  <p>
    To make the dashboard private again, click the <strong>Make dashboard private</strong> icon next to the dashboard and confirm.
  </p>
  <ImageGallery images={[
    { src: '/src/assets/images/user-guide/dashboards/make-public-dashboard-4-ce.png', alt: 'Make private', caption: 'Click <strong>Make dashboard private</strong> to revoke public access.' },
    { src: '/src/assets/images/user-guide/dashboards/make-public-dashboard-5-ce.png', alt: 'Dashboard private', caption: 'Confirm the action. The dashboard is private again.' },
  ]} />
</>
)}

[//]: # (Manage assigned customers CE)
{props.product === Products.CE && (
  <>
    <ConditionalHeading level={3} id="manage-assigned-customers" showFor="ce">Manage assigned customers</ConditionalHeading>
    <p>
      Assign a dashboard to one or more customers. Assigned customers can <strong>view</strong> the dashboard but cannot modify it.
    </p>
    <Steps>
      <ol>
        <li>Go to the <strong>Dashboards</strong> page.</li>
        <li>Click the <strong>Manage assigned customers</strong> icon next to the dashboard.</li>
        <li>Select the customer(s) to assign.</li>
        <li>Click <strong>Update</strong>.</li>
      </ol>
    </Steps>
  <ImageGallery images={[
    { src: '/src/assets/images/user-guide/dashboards/manage-assigned-customers-1-ce.png', alt: 'Manage customers icon', caption: 'Click the <strong>Manage assigned customers</strong> icon.' },
    { src: '/src/assets/images/user-guide/dashboards/manage-assigned-customers-2-ce.png', alt: 'Select customers', caption: 'Select customers to assign. Click <strong>Update</strong> to assign.' },
    { src: '/src/assets/images/user-guide/dashboards/manage-assigned-customers-3-ce.png', alt: 'Click Update', caption: 'The dashboard assigned to the selected customer.' },
  ]} />
    <p>
      To unassign, open <strong>Manage assigned customers</strong> again, remove the customer from the list, and click <strong>Update</strong>.
    </p>
    <ImageGallery images={[
      { src: '/src/assets/images/user-guide/dashboards/manage-assigned-customers-1-ce.png', alt: 'Customers assigned', caption: 'Click the <strong>Manage assigned customers</strong> icon.' },
      { src: '/src/assets/images/user-guide/dashboards/manage-assigned-customers-5-ce.png', alt: 'Unassign customer', caption: 'Remove the customer(s) from the assigned customer list. Click <strong>Update</strong>.' },
      { src: '/src/assets/images/user-guide/dashboards/manage-assigned-customers-6-ce.png', alt: 'Bulk assign', caption: 'The list of clients to whom the selected dashboards were assigned has been updated.' },
    ]} />
  </>
)}

### Edit dashboard

Edit the dashboard title and description through the dashboard details panel.

<Steps>
1. Go to the **Dashboards**.
2. Click the **Dashboard details** icon next to the dashboard.  
2. In the dashboard details window, click the **pencil** icon to enter edit mode.
3. Make the necessary changes.
4. Click **Apply changes** to save.
</Steps>

## Export and Import

### Export dashboard

Export a dashboard configuration as JSON from the dashboard list or toolbar. Click the **Export dashboard** icon to download.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/export-dashboard-1.png', alt: 'Export dashboard', caption: 'Click the <strong>Export dashboard</strong> icon to download dashboard as JSON.' },
  { src: '/src/assets/images/user-guide/dashboards/dashboard-toolbar-export-1.png', alt: 'Toolbar export', caption: 'Export from the dashboard toolbar.' },
]} />

{props.product !== Products.CE && (
<p>In ThingsBoard Professional Edition, dashboards can also be exported in PDF, PNG, and JPEG formats.</p>
)}

### Import dashboard

Upload a JSON configuration file to import a dashboard.

<Steps>
  1. Go to the **Dashboards**.
  2. Click **+ Add dashboard &#8702; Import dashboard**.
  3. Upload the `.json` file and click **Import**.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/import-dashboard-1.png', alt: 'Import dashboard', caption: 'Click <strong>+ Add dashboard &#8702; Import dashboard</strong>' },
  { src: '/src/assets/images/user-guide/dashboards/import-dashboard-2.png', alt: 'Upload JSON', caption: 'Upload the dashboard <strong>JSON file</strong>. Click <strong>Import</strong>.' },
  { src: '/src/assets/images/user-guide/dashboards/import-dashboard-3.png', alt: 'Click Import', caption: 'The dashboard is imported. Open it.' },
  { src: '/src/assets/images/user-guide/dashboards/import-dashboard-4.png', alt: 'Dashboard imported', caption: 'The dashboard has been imported along with all of its widgets.' },
]} />

## Delete dashboard

Click the **trash** icon next to the dashboard and confirm deleting, or  
open the dashboard details page and click **Delete dashboard**.
