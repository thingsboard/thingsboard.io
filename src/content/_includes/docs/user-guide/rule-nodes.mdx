import DocLink from '@components/DocLink.astro';
import { Products } from '~/models/site.models';
import RuleNodeCardGrid from '~/components/RuleNodeCardGrid.astro';

Rule Nodes are the building blocks of
<DocLink product={props.product} path="user-guide/rule-engine">Rule Chains</DocLink>.
Each node processes one incoming message at a time and routes the result to connected nodes via
named relations (`Success`, `Failure`, `True`, `False`, or custom names).

```plaintext
  incoming message
        │
        ▼
  ┌─────────────┐  Success  ┌─────────────┐
  │  Rule Node  │──────────▶│  Next Node  │
  └─────────────┘           └─────────────┘
        │ Failure
        ▼
  ┌─────────────┐
  │  Next Node  │  (or unhandled → message marked as Failed)
  └─────────────┘
```

Nodes fall into seven categories:

<RuleNodeCardGrid items={[
  { href: `/docs/${props.product === Products.CE ? '' : 'pe/'}reference/rule-engine/nodes/filter/`, title: 'Filter', description: 'Route messages based on conditions — no data modification', icon: '/src/assets/images/user-guide/rule-nodes/node-filter.svg' },
  { href: `/docs/${props.product === Products.CE ? '' : 'pe/'}reference/rule-engine/nodes/enrichment/`, title: 'Enrichment', description: 'Add attributes, telemetry, or entity details to message metadata', icon: '/src/assets/images/user-guide/rule-nodes/node-enrichment.svg' },
  { href: `/docs/${props.product === Products.CE ? '' : 'pe/'}reference/rule-engine/nodes/transformation/`, title: 'Transformation', description: 'Modify payload, metadata, type, or originator', icon: '/src/assets/images/user-guide/rule-nodes/node-transform.svg' },
  { href: `/docs/${props.product === Products.CE ? '' : 'pe/'}reference/rule-engine/nodes/action/`, title: 'Action', description: 'Perform system operations — persist data, manage alarms, send RPC', icon: '/src/assets/images/user-guide/rule-nodes/node-action.svg' },
  { href: `/docs/${props.product === Products.CE ? '' : 'pe/'}reference/rule-engine/nodes/external/`, title: 'External', description: 'Publish data to external systems (Kafka, MQTT, REST, cloud, email)', icon: '/src/assets/images/user-guide/rule-nodes/node-external.svg' },
  { href: `/docs/${props.product === Products.CE ? '' : 'pe/'}reference/rule-engine/nodes/flow/`, title: 'Flow', description: 'Control routing between rule chains and queues', icon: '/src/assets/images/user-guide/rule-nodes/node-flow.svg' },
  { href: `/docs/${props.product === Products.CE ? '' : 'pe/'}reference/rule-engine/nodes/analytics/`, title: 'Analytics', description: 'Aggregate data streams and compute statistics', icon: '/src/assets/images/user-guide/rule-nodes/node-analytics.svg', badge: 'PE only', badgeVariant: 'success' },
]} />
