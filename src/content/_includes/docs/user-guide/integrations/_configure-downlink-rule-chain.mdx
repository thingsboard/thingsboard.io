import DocLink from '@components/DocLink.astro';
import { Steps, Aside } from '@astrojs/starlight/components';

To send downlinks through the integration, modify the **Root Rule Chain**:

<Steps>
1. Go to **Rule chains** and open the **Root Rule Chain**.
2. Find the **integration downlink** node in the node library panel on the left. Drag it onto the canvas.
3. In the node configuration dialog, enter a name (e.g. "Downlink to {props.integrationName}") and select your **{props.integrationName}** integration. Click **Add**.
4. Connect the **message type switch** node to the new **integration downlink** node using the **Attributes Updated** link type. Apply changes.
</Steps>

<Aside type="tip">
You can also connect the **Post attributes** link type if you want to forward attribute post messages as downlinks.
</Aside>
