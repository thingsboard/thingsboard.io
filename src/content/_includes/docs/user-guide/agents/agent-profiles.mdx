import DocLink from '@components/DocLink.astro';
import ImageGallery from '@components/ImageGallery.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';

An **agent profile** defines how a fleet of agents is provisioned and what applications those agents run. Instead of creating each agent by hand, you create one profile, copy its provisioning command, and run that same command on every target machine. ThingsBoard registers each machine as a new agent and, depending on the provisioning strategy, installs applications on it automatically.

Agent profiles are also where you run <DocLink product={props.product} path='user-guide/agents/bulk-actions'>bulk actions</DocLink>: fleet-wide restarts, updates, upgrades, and deletes of every application that matches an assigned application profile.

<ImageGallery images={[
  {
    src: '/src/assets/schemas/agent-profile-provisioning.svg',
    alt: 'Agent profile provisioning: an agent profile with the One application per type strategy generates one provisioning command with the provision key and secret; the same docker run command runs on every remote host, each host registers as a new agent, and the agent installs the Edge and Gateway applications defined by the provisioning strategy',
    caption: 'One agent profile, one command: every host that runs it registers as an agent and receives its applications automatically.',
  },
]} />

## Provisioning strategies

Each agent profile has a provisioning strategy that controls what happens when a new agent registers with the profile's credentials:

| Strategy | Behavior |
|---|---|
| **Disabled** | The profile rejects provisioning requests. Agents can only be created manually. |
| **Provision agents only** | New agents are registered, but no applications are installed. |
| **One application per type** (default) | New agents are registered, and at most one application of each type (Edge, Gateway) is installed from the assigned application profiles. |
| **One application per assigned profile** | New agents are registered, and one application is installed for every assigned application profile. |

## Create an agent profile

Creating an agent profile is one dialog: enter the name and pick a **setup**. The setup determines the [provisioning strategy](#provisioning-strategies) and the assigned <DocLink product={props.product} path='user-guide/agents/application-profiles'>application profiles</DocLink>: reusable configurations that define what actually runs on a host, like a specific ThingsBoard Edge or IoT Gateway version. Pick a preset for the common cases, or **Advanced** for full control:

| Setup | Use for |
|---|---|
| **Edge** | Hosts that run <DocLink product={Products.EDGE_PE} path=''>ThingsBoard Edge</DocLink>: local data processing and rule chains that stay in sync with the platform. |
| **Gateway** | Hosts that run <DocLink product={Products.GW} path=''>IoT Gateway</DocLink>: connects local devices (Modbus, OPC-UA, MQTT, etc.) to the platform. |
| **Edge + Gateway** | Hosts that run both: an IoT Gateway connecting local devices alongside a ThingsBoard Edge. |
| **Advanced** | Custom setups: choose the provisioning strategy yourself and assign or create any application profiles. |

<Steps>
  <ol>
    <li>
      Go to **Edge management > Profiles > Agent profiles** and click **Add agent profile**.
    </li>
    <li>
      Enter the profile name and pick a setup: an **Edge**, **Gateway**, or **Edge + Gateway** preset, or **Advanced**.
    </li>
  </ol>
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/agent/user-guide/agent-profiles/1-agent-profiles-list.webp',
    alt: 'The Agent profiles tab of the Profiles page with the Add agent profile button highlighted',
    caption: 'Go to <strong>Edge management > Profiles > Agent profiles</strong> and click <strong>Add agent profile</strong>.',
  },
  {
    src: '/src/assets/images/agent/user-guide/agent-profiles/2-agent-profile-presets.webp',
    alt: 'The Add agent profile dialog with the profile name field, the Edge, Gateway, Edge + Gateway, and Advanced setup cards with descriptions, and a summary of what the selected preset creates',
    caption: 'Pick a preset or <strong>Advanced</strong>.',
  },
]} />

### Presets

The **Edge**, **Gateway**, and **Edge + Gateway** presets need no configuration. Each assigns the latest predefined application profiles for the selected types and sets the **One application per type** provisioning strategy: a new agent provisioned with this profile installs the assigned applications on its host automatically, unless an application with a matching image name already runs there; the name is compared without the tag, like `thingsboard/thingsboard-edge`. The dialog summarizes what the selected preset creates. Click **Add**. ThingsBoard creates the profile right away.

### Advanced setup

**Advanced** lets you configure the profile yourself. Click **Configure** and complete two steps:

<Steps>
  <ol>
    <li>
      Enter the profile name and select the [provisioning strategy](#provisioning-strategies).
    </li>
    <li>
      Assign <DocLink product={props.product} path='user-guide/agents/application-profiles'>application profiles</DocLink> and click **Add**. Pick predefined or existing profiles, or create a new one inline.
    </li>
  </ol>
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/agent/user-guide/agent-profiles/3-agent-profile-advanced.webp',
    alt: 'The Profile details step of the advanced setup with the profile name field and the provisioning strategy selector',
    caption: 'Enter the profile name and select the provisioning strategy.',
  },
  {
    src: '/src/assets/images/agent/user-guide/agent-profiles/4-assign-application-profiles.webp',
    alt: 'The Application profiles step of the advanced setup with predefined and existing profiles in the dropdown and the Create new application profile button',
    caption: 'Assign predefined or existing application profiles, or create one inline.',
  },
]} />

### Provisioning command and profile details

Both setups end with the **Agent profile created** dialog, which shows the provisioning command for this profile: a `docker run` command with the provision key and secret already filled in.

<ImageGallery images={[
  {
    src: '/src/assets/images/agent/user-guide/agent-profiles/5-agent-profile-created.webp',
    alt: 'The Agent profile created dialog with a docker run command containing the provision key and secret, a summary of the chosen provisioning strategy, and the Go to profile button',
    caption: 'The <strong>Agent profile created</strong> dialog shows the provisioning command.',
  },
]} />

The profile details page shows the provisioning strategy, the generated **Docker run command**, and the assigned application profiles. Its **Application profiles & bulk actions** tab is also where you run <DocLink product={props.product} path='user-guide/agents/bulk-actions'>bulk actions</DocLink> on the fleet. You can mark one profile as the default for the tenant.

<ImageGallery images={[
  {
    src: '/src/assets/images/agent/user-guide/agent-profiles/6-agent-profile-details.webp',
    alt: 'The agent profile details page with the Application profiles & bulk actions tab open, listing the assigned application profile and its recent bulk actions',
    caption: 'The <strong>Application profiles & bulk actions</strong> tab of the profile details page.',
  },
]} />

## Auto-provision an agent

<Steps>
  <ol>
    <li>
      Go to **Edge management > Agents** and click **Auto-provision**.
    </li>
    <li>
      Select an existing agent profile or create a new one, then go to the **Provisioning script** step.
    </li>
    <li>
      Copy the generated command and run it on the target machine:

      ```bash
      docker run -d \
        --name=tb-agent \
        --restart=always \
        -v /var/run/docker.sock:/var/run/docker.sock:ro \
        -v tb-agent-data:/root/.tb-agent \
        -v /:/host:ro \
        -e TB_SERVER_ADDR=<thingsboard-host>:7070 \
        -e AUTO_PROVISION=true \
        -e TB_PROVISION_KEY=<provision-key> \
        -e TB_PROVISION_SECRET=<provision-secret> \
        thingsboard/tb-remote-agent:1.0.0
      ```
    </li>
    <li>
      Verify the result. A new agent named `Agent-<first 8 characters of the routing key>` appears in the Agents list, and the applications defined by the provisioning strategy start installing.
    </li>
  </ol>
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/agent/user-guide/agent-profiles/7-auto-provision-menu.webp',
    alt: 'The Agents page with the Add agent dropdown open and the Auto-provision option highlighted',
    caption: 'Go to <strong>Edge management > Agents</strong> and click <strong>Auto-provision</strong>.',
  },
  {
    src: '/src/assets/images/agent/user-guide/agent-profiles/8-auto-provision-select-profile.webp',
    alt: 'The Agent profile step of the Auto-provision agent dialog with the profile list open and the Create profile button',
    caption: 'Select an agent profile or create a new one.',
  },
  {
    src: '/src/assets/images/agent/user-guide/agent-profiles/9-auto-provision-ready.webp',
    alt: 'The Auto-provision agent dialog with the selected profile and a note confirming that the profile has provisioning enabled',
    caption: 'The note confirms that the profile allows provisioning.',
  },
  {
    src: '/src/assets/images/agent/user-guide/agent-profiles/10-auto-provision-script.webp',
    alt: 'The Provisioning script step of the Auto-provision agent dialog with a docker run command containing the provision key and secret',
    caption: 'Copy the command from the <strong>Provisioning script</strong> step and run it on the target machine.',
  },
  {
    src: '/src/assets/images/agent/user-guide/agent-profiles/11-auto-provisioned-agent.webp',
    alt: 'The Agents list with a new auto-provisioned agent shown as Online and its agent profile applied',
    caption: 'The provisioned agent registers itself and appears as <strong>Online</strong>.',
  },
]} />

The same command works on every machine in the fleet: the provision key and secret identify the profile, not an individual agent.

### How provisioning works

<ImageGallery images={[
  {
    src: '/src/assets/schemas/agent-provisioning-flow.svg',
    alt: 'Remote Agent provisioning flow: the agent sends a provision request, the server validates it against the agent profile, creates the agent entity, and returns routing credentials; the agent persists them, reconnects, and receives application installs per the provisioning strategy',
    caption: 'The agent contacts the server with the profile provision key and secret, receives its own routing credentials, and reconnects; the server then installs applications per the provisioning strategy.',
  },
]} />

The agent stores the received credentials in its data volume (`credentials.json`, file mode `0600`) and uses them directly on every subsequent start; no provisioning request is repeated. To force re-provisioning, delete the file from the `tb-agent-data` volume or recreate the volume.

## Assign application profiles

The **Application profiles & bulk actions** tab of the agent profile lists the assigned <DocLink product={props.product} path='user-guide/agents/application-profiles'>application profiles</DocLink>. For each assignment you can:

- **Relate on Auto-Discovery:** Automatically assigns this application profile to applications that agents discover on their hosts, when the application's Edge or Gateway image matches the profile's template. Use it to bring an existing fleet under profile management. See <DocLink product={props.product} path='user-guide/agents/application-profiles'>Application profiles and templates</DocLink> for the matching rules and cautions.
- **Run bulk actions:** Restart, update, upgrade, or delete every matching application across the fleet. See <DocLink product={props.product} path='user-guide/agents/bulk-actions'>Bulk actions</DocLink>.

<ImageGallery images={[
  {
    src: '/src/assets/images/agent/user-guide/agent-profiles/12-relate-on-auto-discovery.webp',
    alt: 'The Application profiles & bulk actions tab with the Assign profile button and the Relate on Auto-Discovery checkbox of an assigned application profile highlighted',
    caption: 'Assign application profiles and toggle <strong>Relate on Auto-Discovery</strong> per assignment.',
  },
]} />

<Aside type="danger" title="Relate on Auto-Discovery overrides the host configuration">
With **Relate on Auto-Discovery** enabled, relating happens immediately: as soon as a discovered application's main Edge or Gateway image matches the profile's template version (like a host running `thingsboard-edge:4.3` and a profile whose template version is `thingsboard-edge:4.3`), the profile's Docker Compose configuration **overrides** the Docker Compose project on the host right away. Before enabling the toggle, make sure the profile matches the running deployments exactly, including environment variables and volume names.
</Aside>

An application profile can be assigned to several agent profiles. This supports staged rollouts: assign one Edge application profile to a staging and a production agent profile, <DocLink product={props.product} path='user-guide/agents/bulk-actions#bulk-upgrade'>bulk upgrade</DocLink> the staging fleet first, verify the result and adjust the configuration, then bulk upgrade production.

<ImageGallery images={[
  {
    src: '/src/assets/schemas/agent-staged-upgrade.svg',
    alt: 'Staged fleet upgrade: the Factory Edge application profile with a new template version is assigned to a staging and a production agent profile, each with agents managing one ThingsBoard Edge instance; a bulk upgrade runs on the staging fleet first, the result is verified and the profile configuration adjusted, then a bulk upgrade runs on the production fleet',
    caption: 'One application profile assigned to two agent profiles: bulk upgrade staging first, verify, then bulk upgrade production.',
  },
]} />

<Aside type="tip">
Assign application profiles before machines come online. With the **One application per assigned profile** strategy, each new host receives its full application set the moment the agent connects.
</Aside>
