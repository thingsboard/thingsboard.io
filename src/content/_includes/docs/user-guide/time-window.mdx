import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import { Aside } from '@astrojs/starlight/components';
import YouTubeVideo from '~/components/YouTubeVideo.astro';
import DocLink from '@components/DocLink.astro';

The **Time window** defines the temporal scope and aggregation rules applied to time-series telemetry displayed on dashboards.

Time window parameters can be configured at:
- **Dashboard level** — global time window applied to all supported widgets.
- **Widget level** — local override for a specific widget.

<Aside type="note">
  Time window settings are applied only to widgets that support **time-series data** and **alarms**.
</Aside>

## Video tutorial

Watch this short video to learn how to configure Time Window settings, switch between Realtime and History modes, and adjust aggregation and grouping intervals.

<YouTubeVideo
  videoId="3xRWm1W1IM4"
  title="How to configure Time Window in ThingsBoard"
/>

## Configuration levels

### Dashboard time window

The dashboard time window is configured in **edit mode** via the *Edit time window* dialog and saved as part of the dashboard configuration.

Changes made in **view mode** are temporary and reset to the last saved configuration after page reload or reopening the dashboard.

<Aside type="note" title="Save current settings as default time window">
  <strong>Save current settings as default time window</strong> option — saves the current time window configuration (set in view mode) as the dashboard default.
</Aside>

### Widget time window

Each time-series widget can use either the **dashboard time window** or its own configuration.

To configure a custom time window, enable **Use widget time window** in the widget settings.

Widget-level configuration allows:
- Defining a custom time window
- Independent aggregation and grouping interval settings

When a widget-level time window is enabled, the widget ignores the dashboard-level configuration.

## Mode selection

The time window dialog supports two data retrieval modes:

- **Real-time** — widgets continuously receive updates from the server for the current time range.
- **History** — widgets load data once for a fixed period; no live updates.

### Interval types

| Type         | Description                                              | Mode Availability | Behavior                                                                                             | Example                             |
| ------------ | -------------------------------------------------------- | ----------------- | ---------------------------------------------------------------------------------------------------- | ----------------------------------- |
| **Last**     | Rolling interval counted backward from the current time. | Realtime, History | In Realtime mode, continuously shifts forward. In History mode, fixed period ending at current time. | Last 5 minutes, Last 1 hour         |
| **Range**    | Explicit start and end timestamps.                       | History only      | Fixed interval. Does not update automatically.                                                       | 2026-03-01 08:00 – 2026-03-01 12:00 |
| **Relative** | Calendar-aligned interval.                               | Realtime, History | Aligned to calendar boundaries.                                                                      | Current day, Previous month         |


<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/timewindow-real-time-mode-1.png', alt: 'Real-time mode', caption: 'Real-time mode for continuous updates.' },
  { src: '/src/assets/images/user-guide/dashboards/timewindow-history-mode-1.png', alt: 'History mode', caption: 'History mode for a selected time range.' },
  { src: '/src/assets/images/user-guide/dashboards/time-window-key-parameters-1.png', alt: 'Key parameters', caption: 'Time window key parameters: Last, Range, Relative.' },
  { src: '/src/assets/images/user-guide/dashboards/displaying-data-for-desired-time-period-1.png', alt: 'Time period selection', caption: 'Select the desired time period.' },
  { src: '/src/assets/images/user-guide/dashboards/displaying-data-for-desired-time-period-2.png', alt: 'Time period result', caption: 'Data displayed for the selected period.' },
]} />

## Aggregation

Defines how telemetry values are processed within <DocLink product={props.product} path='user-guide/time-window/#grouping-interval' bold={true}>grouping intervals</DocLink>.

| Function | Description                |
| -------- | -------------------------- |
| None     | Raw telemetry values       |
| Min      | Minimum value per interval |
| Max      | Maximum value per interval |
| Average  | Arithmetic mean            |
| Sum      | Sum of values              |
| Count    | Number of records          |

<Aside type="note" title="Notes">
  - Applies only to time-series telemetry
  - When None, raw values are returned (subject to Max values limit)
  - When aggregation is enabled, data is grouped before applying the function

  Aggregation reduces data volume and improves performance.
</Aside>

## Grouping interval

Defines the size of time buckets used for aggregation. Each bucket produces one aggregated value.

*Example:* with a time window of Last 1 hour, a grouping interval of 10 minutes, and Average aggregation — the widget receives 6 aggregated values (one per 10-minute bucket).

- Smaller intervals &#8702; higher granularity, more data points.
- Larger intervals &#8702; smoother trends, reduced load.

The grouping interval may be automatically calculated based on the selected window and widget resolution, or explicitly defined in widget configuration.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/timewindow-aggregation-1.png', alt: 'Aggregation functions', caption: 'Available aggregation functions.' },
  { src: '/src/assets/images/user-guide/dashboards/timewindow-grouping-interval-1.png', alt: 'Grouping interval', caption: 'Configure the grouping interval.' },
  { src: '/src/assets/images/user-guide/dashboards/timewindow-grouping-interval-2.png', alt: 'Grouping interval result', caption: 'Grouping interval applied.' },
]} />

## Max values

Defines the maximum number of telemetry points returned.

*Example:*

```
Max values = 50000
```

Purpose:
- Prevent excessive data retrieval
- Protect browser performance
- Limit memory usage

Especially important when **Aggregation = None**.

<Aside type="note">
  If the number of raw points exceeds the limit, only the most recent values within the window are returned.
</Aside>

## Zoom

Drag the sliders to narrow the visible time range; return them to their default position to zoom out.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/timewindow-zoom-1.png', alt: 'Zoom controls', caption: 'Use zoom controls on the time axis.' },
  { src: '/src/assets/images/user-guide/dashboards/timewindow-zoom-2.png', alt: 'Zoomed in', caption: 'Zoomed into a time range.' },
  { src: '/src/assets/images/user-guide/dashboards/timewindow-zoom-3.png', alt: 'Zoomed in', caption: 'To reset the chart zoom, move the sliders to their default positions.' },
]} />

## Time zone

Defaults to the browser time zone; can be changed to any specific time zone.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/timewindow-timezone-1.png', alt: 'Time zone setting', caption: 'Configure the dashboard time zone.' },
]} />

## Time window settings

You can control which time window controls are visible to end users.

In dashboard edit mode, open the Edit time window dialog and click the gear icon.

This allows you to:
- Hide **Time window** section entirely.
- Hide specific interval types (e.g., *Last, Range, Relative*)
- Configure available time intervals and default values
- Control visibility of **aggregation**, **grouping interval**, and **Time zone** selectors

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/dashboards/time-window-settings-1.png', alt: 'Time window settings', caption: 'In dashboard edit mode, open the Edit time window dialog and click the gear icon.' },
  { src: '/src/assets/images/user-guide/dashboards/time-window-settings-2.png', alt: 'Time window options', caption: 'Configure available options.' },
  { src: '/src/assets/images/user-guide/dashboards/hide-time-window-section-from-end-users-1.png', alt: 'Hide time window', caption: 'Hide the time window section.' },
  { src: '/src/assets/images/user-guide/dashboards/hide-only-last-or-relative-interval-from-end-users-1.png', alt: 'Hide intervals', caption: 'Hide specific intervals.' },
  { src: '/src/assets/images/user-guide/dashboards/edit-list-of-intervals-1.png', alt: 'Edit intervals', caption: 'Edit the available intervals.' },
  { src: '/src/assets/images/user-guide/dashboards/hide-aggregation-1.png', alt: 'Hide aggregation', caption: 'Hide aggregation options.' },
  { src: '/src/assets/images/user-guide/dashboards/edit-aggregation-list-1.png', alt: 'Aggregation list', caption: 'Edit available aggregation functions.' },
  { src: '/src/assets/images/user-guide/dashboards/hide-grouping-interval-1.png', alt: 'Hide grouping interval', caption: 'Hide the grouping interval.' },
  { src: '/src/assets/images/user-guide/dashboards/hide-time-zone-1.png', alt: 'Hide time zone', caption: 'Hide the time zone selector.' },
]} />
