import DocLink from '@components/DocLink.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';

The Image gallery is a centralized repository for storing and managing images in ThingsBoard.
Upload logos, icons, backgrounds, or any visual asset and reuse it across <DocLink product={props.product} path="user-guide/dashboards">dashboards</DocLink>, <DocLink product={props.product} path="user-guide/widgets">widgets</DocLink>, device profiles, and mobile applications — ensuring a consistent, branded look across the platform.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/image-gallery/image-gallery.png', alt: 'Image gallery overview' },
]} />

Available since ThingsBoard **3.6.2**.

## Cache configuration

The image API uses **ETags** to optimize caching — images are only downloaded when they have changed, saving bandwidth.
By default, no `Cache-Control` headers are applied. You can customize caching behavior through environment variables:

| Variable | Description | Default |
|----------|-------------|---------|
| `CACHE_SPECS_IMAGE_ETAGS_TTL` | Image ETags cache TTL, in minutes | 44 640 (31 days) |
| `CACHE_SPECS_IMAGE_ETAGS_MAX_SIZE` | Maximum cache size, in bytes. `0` = disabled | 0 |
| `CACHE_SPECS_IMAGE_SYSTEM_BROWSER_TTL` | Browser cache TTL for system images, in minutes | 0 (disabled) |
| `CACHE_SPECS_IMAGE_TENANT_BROWSER_TTL` | Browser cache TTL for tenant images, in minutes | 0 (disabled) |

A longer TTL improves load time for repeated visits and reduces server load; a shorter TTL ensures users see updated images sooner.
See the <DocLink product={props.product} path="reference/configuration/how-to-change-config">Configuration guide</DocLink> for instructions on changing these values.

<Aside type="note">
  System administrators can also restrict the maximum size of a single image and the total image storage per tenant through **Tenant profiles** (Files limits section). Values are specified in bytes.
</Aside>

## Adding images

Add images in native **image file format** (PNG, JPEG, GIF, etc.) or as a **JSON export**.

### Upload an image file

<Steps>
1. Go to the **Image gallery** page in the **Resources** section. Click the **Upload image** button in the top-right corner.

   <MultiProductImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/image-gallery/upload-image-1.png', alt: 'Click Upload image button' },
   ]} />

2. Select an image or drag it into the upload window, then click **Upload**.

   <MultiProductImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/image-gallery/upload-image-2.png', alt: 'Select or drag image file' },
   ]} />

3. The image appears in the gallery.

   <MultiProductImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/image-gallery/upload-image-3.png', alt: 'Image uploaded successfully' },
   ]} />
</Steps>

### Import from JSON

<Steps>
1. Click the **Import image** icon in the top-right corner of the Image gallery page.

   <MultiProductImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/image-gallery/upload-image-4.png', alt: 'Click Import image icon' },
   ]} />

2. Upload a JSON image file and click **Import**.

   <MultiProductImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/image-gallery/upload-image-5.png', alt: 'Upload JSON file' },
   ]} />

3. The image has been imported.

   <MultiProductImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/image-gallery/upload-image-6.png', alt: 'Image imported successfully' },
   ]} />
</Steps>

## System images

The gallery includes built-in **system images** you can use in dashboards and widgets.
By default, only tenant images are shown. Enable the **Include system images** toggle to see both.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/image-gallery/include-system-images-1.png', alt: 'Default view — tenant images only', caption: 'By default, only your images are displayed' },
  { src: '/src/assets/images/user-guide/image-gallery/include-system-images-2.png', alt: 'System images enabled', caption: 'After enabling "Include system images"' },
]} />

<Aside type="note">
  Only system administrators can delete, rename, or update system images.
</Aside>

## View modes

Images can be displayed in **list** or **grid** mode. Switch between them using the toggle in the top-left corner of the gallery.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/image-gallery/image-viewing-mode-1.png', alt: 'List view', caption: 'List view' },
  { src: '/src/assets/images/user-guide/image-gallery/image-viewing-mode-2.png', alt: 'Grid view', caption: 'Grid view' },
]} />

## Image operations

### Download

Download an image in its original file format:

- **List view** — click the **Download image** icon next to the image name.
- **Grid view** — hover over the image and click the **Download image** icon.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/image-gallery/download-image-1.png', alt: 'Download from list view', caption: 'List view' },
  { src: '/src/assets/images/user-guide/image-gallery/download-image-2.png', alt: 'Download from grid view', caption: 'Grid view' },
]} />

### Export to JSON

Export an image as a JSON file (useful for migrating images between ThingsBoard instances):

- **List view** — click the **Export image to JSON** icon next to the image name.
- **Grid view** — hover over the image and click the **Export image to JSON** icon.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/image-gallery/export-image-1.png', alt: 'Export from list view', caption: 'List view' },
  { src: '/src/assets/images/user-guide/image-gallery/export-image-2.png', alt: 'Export from grid view', caption: 'Grid view' },
]} />

### Embed

After adding an image to the gallery, you can embed it in HTML Card widgets, the HTML section of the widget editor, cell content functions, custom actions, and more.

Each image has a unique URL. Use the snippet that matches your context:

**Plain HTML** (no authentication — use in HTML Card widgets, cell content functions, etc.):

```html
<img src="relative link to the image" alt="text description of the image" />
```

**Angular HTML template** (with authentication — use in Markdown/HTML Card widget, widget editor HTML section):

```html
<img [src]="'image URL' | image | async" />
```

The Angular method automatically includes authentication headers, allowing the same URL to return different images for different users.

To get the embed link:

<Steps>
1. Click the **Embed image** icon next to the image name (in grid view, hover first).

   <MultiProductImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/image-gallery/embed-image-1.png', alt: 'Click Embed image icon' },
   ]} />

2. Select the code snippet type (Angular or plain HTML) and copy the unique image URL.

   <MultiProductImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/image-gallery/embed-image-2.png', alt: 'Copy embed code snippet' },
   ]} />
</Steps>

### Edit

Click the **Edit image** icon next to the image name (in grid view, hover first) to open the editing window.
From here you can rename, download, export, embed, or [update](#update) the image.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/image-gallery/edit-image-1.png', alt: 'Click Edit image icon', caption: 'Click the edit icon to open the editing window' },
  { src: '/src/assets/images/user-guide/image-gallery/edit-image-2.png', alt: 'Edit image window', caption: 'Editing window with rename, download, export, embed, and update options' },
]} />

To rename an image, enter the new name and click the **Save** icon.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/image-gallery/edit-image-3.png', alt: 'Enter new image name', caption: 'Enter a new name' },
  { src: '/src/assets/images/user-guide/image-gallery/edit-image-4.png', alt: 'Image name changed', caption: 'Name saved successfully' },
]} />

### Update

Replacing an image is useful when the same picture serves as the background for multiple dashboards — update it once and all references automatically reflect the new version.

<Steps>
1. Open the image editing window and click **Update image**.

   <MultiProductImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/image-gallery/update-image-1.png', alt: 'Click Update image button' },
   ]} />

2. Select a new image or drag it into the window, then click **Update**.

   <MultiProductImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/image-gallery/update-image-2.png', alt: 'Select new image' },
   ]} />

3. The image has been updated. All dashboards and widgets referencing it now show the new version.

   <MultiProductImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/image-gallery/update-image-3.png', alt: 'Image updated successfully' },
   ]} />
</Steps>

### Delete

<Aside type="caution">
  Deleting an image is permanent and cannot be undone. Widgets and dashboards referencing the deleted image will show a broken image.
</Aside>

**Delete from list view:**

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/image-gallery/delete-image-1.png', alt: 'Click delete icon in list view', caption: 'Click the delete icon next to the image name' },
  { src: '/src/assets/images/user-guide/image-gallery/delete-image-2.png', alt: 'Confirm deletion', caption: 'Confirm deletion in the dialog' },
]} />

**Delete from grid view:**

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/image-gallery/delete-image-3.png', alt: 'Hover and click delete in grid view', caption: 'Hover over the image and click the delete icon' },
  { src: '/src/assets/images/user-guide/image-gallery/delete-image-4.png', alt: 'Confirm deletion', caption: 'Confirm deletion in the dialog' },
]} />

**Delete multiple images** (list view only):

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/user-guide/image-gallery/delete-image-5.png', alt: 'Select multiple images', caption: 'Check the images you want to delete, then click the trash icon in the top-right corner' },
  { src: '/src/assets/images/user-guide/image-gallery/delete-image-6.png', alt: 'Confirm batch deletion', caption: 'Confirm batch deletion in the dialog' },
]} />
