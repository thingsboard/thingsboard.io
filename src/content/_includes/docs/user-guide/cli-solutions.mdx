import DocLink from '@components/DocLink.astro';
import Banner from '~/components/Banner.astro';
import { Products } from '~/models/site.models';
import { Steps, Aside } from '@astrojs/starlight/components';
import ImageGallery from '~/components/ImageGallery.astro';

<Banner variant="peFeature" product={props.product}>ThingsBoard CLI works only with <DocLink product={Products.PE} path="user-guide/cli" target="_blank">ThingsBoard Professional</DocLink> and <a href="/installations/?product=thingsboard-cloud" target="_blank" rel="noopener noreferrer" class="tb-banner-ext"><b>ThingsBoard Cloud</b></a> version **4.3.1.2** or later.</Banner>

{props.product !== Products.CE && (
  <Banner variant="cloud">Available from ThingsBoard PE/Cloud <strong>4.3.1.2</strong> and later.</Banner>
)}

You can manage IoT solutions with ThingsBoard CLI two ways: manually, by <DocLink product={props.product} path="user-guide/cli#using-the-cli" target="_blank">running the CLI commands directly</DocLink>, or by letting an AI coding agent drive them for you. This page is about the AI-agent way.

Once your project is set up, you build and manage IoT solutions by describing what you want to your AI coding agent — <a href="https://docs.anthropic.com/en/docs/claude-code/overview" target="_blank" rel="noopener noreferrer nofollow"><strong>Claude Code</strong></a> or <a href="https://antigravity.google/docs/cli/overview" target="_blank" rel="noopener noreferrer nofollow"><strong>Antigravity</strong></a> — which drives ThingsBoard CLI for you. Haven't set up the CLI yet? Start with <DocLink product={props.product} path="user-guide/cli" target="_blank">Getting started with ThingsBoard CLI</DocLink>.

`tb init` sets up the bundled skills and agents in your project — for entity generation, dashboards, alarms, calculated fields, and telemetry emulators — and they update with the CLI. Launch your agent from inside the project directory so the bundled skills load automatically. From there you describe the goal, and the agent designs the entities, validates them against your instance, and shows you the result before anything deploys.

<Aside>
The agent-config directory (`.claude/` for Claude Code, `.agents/` plus `AGENTS.md` for Antigravity) is regenerated by the CLI on launch.
</Aside>

## What you can do

Describe what you want in plain English, and your agent designs, builds, and maintains it for you — no hand-written JSON:

- **Create a single entity** — one targeted artifact, like a calculated field on a specific device or a couple of devices for a profile.

  **Starting point:** a `Pump-42` device is already on your tenant, reporting `flowRate`, `powerConsumption`, `pressure`, `rpm`, `temperature`, and `vibration`.

  ```text
  For Pump-42, I want to know the average pressure over the last 5 minutes.
  ```

  <ImageGallery product={props.product} images={[
    { src: '/src/assets/images/user-guide/cli-solutions/create-single-entity-flow.png', alt: 'From prompt to result: agent output, the generated calculated field, and the metric on the dashboard', caption: 'From prompt to result — the agent creates the calculated field, and its 5-minute average shows up on the pump dashboard.' },
  ]} />

- **Build something new** — a full solution from scratch: devices and their hierarchy, dashboards, alarm rules, calculated fields, customers and users, plus demo telemetry so the dashboards have data from day one.

  **Starting point:** an empty tenant — no entities yet.

  ```text wrap
  I want a dashboard to monitor telemetry for my logistics company. I have several fleets; each fleet contains vans, and every van reports rpm, speed, fuelConsumption, and fuelLevel.

  I'd like an alternative design. On the main page, show my vans in two switchable views: grid and list. When I drill down into a van, give me a button to set a threshold for each telemetry key. The full focus should be the van view: a route map, a SCADA symbol of the van, and value cards right on the widget.

  Aim for a very smart design.
  ```

  <ImageGallery product={props.product} images={[
    { src: '/src/assets/images/user-guide/cli-solutions/build-something-new-flow.png', alt: 'The agent builds the logistics solution: main fleet console and van detail view', caption: 'From one prompt — the agent designs the whole logistics solution: the main fleet console and the van detail view.' },
  ]} />

- **Evolve an existing solution** — add devices, change a threshold, add a dashboard widget, or introduce a new sensor type. The agent edits only what changed and leaves the rest untouched.

  **Starting point:** a waste management solution you set up earlier by hand, already running on your tenant.

  ```text wrap
  In my waste management solution I want to be able to set the alarm level for each bin separately, right from the dashboard. Right now every bin alarms at the same fill level, but some bins fill up faster than others, so I need to set a different threshold for each one. Let me click on a bin and type in its own threshold, and the alarm should use that number for that specific bin.
  ```

  <ImageGallery product={props.product} images={[
    { src: '/src/assets/images/user-guide/cli-solutions/evolve-existing-flow.png', alt: 'Per-bin threshold: alarm rule reads a per-bin attribute and the dashboard gets a save-threshold control', caption: 'The agent adds a per-bin fullLevelThreshold attribute, points the alarm rule at it, and adds a Save-threshold control to the bin dashboard.' },
  ]} />

- **Fix something that's broken** — point the agent at a misbehaving entity, like a calculated field that produces no values or an alarm that never fires. It diagnoses the cause, proposes a fix, and applies it only after you approve.

  **Starting point:** a building asset and its temperature sensor devices are already on your tenant, and a calculated field you added by hand isn't producing any values — and you can't tell why.

  ```text wrap
  My calculated field "Building Average Temperature" isn't producing any values. The building should show the average temperature across all its temperature sensors, but avgTemperature is just empty — nothing ever shows up. Can you look into why and tell me what's going on?
  ```

  <ImageGallery product={props.product} images={[
    { src: '/src/assets/images/user-guide/cli-solutions/fix-something-broken-flow.png', alt: 'The agent diagnoses the broken calculated field, adds the missing Contains relations, and the average temperature populates on the dashboard', caption: 'The agent finds the root cause — the building had no relations to its sensors — adds the missing Contains relations, and the Average Temperature calculated field starts producing values.' },
  ]} />

These are the actions people reach for most — but they're not the limit. You can ask for any custom result you want, and the more detail you give (the entities, their hierarchy, who sees what, and the thresholds that matter), the closer the agent's result matches what you had in mind.

The agent builds and validates everything locally — you deploy it yourself with `tb push`. Push to a dev instance first, check that everything's correct, then deploy to prod:

```bash
tb push <solution> --profile dev --run-tasks   # deploy to your dev instance, with demo data
tb push <solution> --profile prod              # deploy to production
```

`--run-tasks` runs the telemetry emulators and backfills the calculated fields the agent set up, so a fresh instance has data the moment the solution goes live. For all push options and profiles, see <DocLink product={props.product} path="user-guide/cli" target="_blank">Getting started with ThingsBoard CLI</DocLink>.

## Why it's powerful

Working through the CLI gives you more than a faster way to click:

- **Fully custom, not template-bound.** The agent's real strength is flexibility. Beyond the system widgets, it can craft a fully custom visualization or control for you inside an <DocLink product={props.product} path="reference/widgets/html-widgets/html-container" target="_blank">HTML container widget</DocLink> — written to your exact description. That means almost any dashboard layout or interaction you can picture is within reach, so you're free to describe the result you actually want rather than the closest ready-made option.
- **Versioned by default.** The agent commits every change it makes, so your whole IoT setup lives in git. Switch to any earlier version of your project whenever you need to, and restore any previous state — no rebuilds.
- **One source, every environment.** The same project files deploy to dev, staging, and production under different profiles. Promote with a single `tb push`, and pushes are idempotent — re-push as often as you like and nothing duplicates, so environments never drift.

## Hands-on guide: build and update a solution

This walkthrough builds a solution from a single prompt, then updates it — the everyday loop of working with the agent. We'll build a smart retail solution: a chain of supermarket stores with refrigeration, ambient, and people-counter sensors, and a dashboard to monitor them.

<Steps>
1. **Describe what you want.** Front-load the whole picture in your first prompt — the domain, the entity hierarchy, who sees what, the alarms that matter, and how the dashboard should behave. The more detail you give, the closer the first design lands:

   ```text wrap
   create a smart retail solution for my supermarket chain. i have 3 stores across the city, and each store has refrigeration units reporting temperature (this is cold-chain critical), ambient sensors on the sales floor reporting temperature, humidity and CO2, and door sensors reporting open/closed. there are two roles: an admin who manages the whole system across all stores, and a store manager who can see only their own store. i want alarms when a freezer goes over temperature (critical), and warnings for a door left open or comfort out of range.

   i want a dashboard per role — same layout, but the admin sees all stores while the store manager opens straight to their own. the admin has two tabs: stores (4 header cards, a big map of all stores, and a stores table hidden behind a toggle) and alarms (a table). clicking a store opens its details, which has three tabs: sensors (a floorplan with color-coded device markers and a device table filterable by type; clicking a device opens a mat-drawer with its latest readings and analytics), analytics (charts of all the store's readings, split into per-type sub-tabs — refrigeration, ambient, door), and alarms (a table). the store manager sees that same store-details view, just for their own store.

   i want to switch between dark and light, use var(--tb-primary-500) for all primary/accent components, and give every widget border-radius: 12px and some border or box-shadow. make a very good/smart design!
   ```

   <ImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-request.png', alt: 'Describing the smart retail solution to the agent in the terminal', caption: 'Describe the solution in plain English — the agent recognizes it as a full solution build and gets to work.' },
   ]} />

2. **Choose how to review it.** Before building anything, the agent asks how you want to approve the design: pause after the entity model and again after the dashboards so you can review each in turn, or build everything in one pass and see the results as it goes. You can switch modes anytime by telling it to just continue or to stop asking.

   <ImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-approval-mode.png', alt: 'The agent asking how you want to approve the design', caption: 'The agent asks how to proceed — approve at each step, or build it all in one go.' },
   ]} />

3. **Answer the agent's questions.** Before it proposes anything, the agent asks clarifying questions to fill the gaps in your request — here, how many devices of each type to generate for the demo. Answer them, or let it decide with sensible defaults.

   <ImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-questions.png', alt: 'The agent asking clarifying questions to fill gaps in the request', caption: 'The agent asks clarifying questions to fill the gaps — like how many demo devices per type.' },
   ]} />

4. **Review the proposed design.** The agent lays out the full design before creating anything: the entity hierarchy, each device profile with its telemetry keys and attributes, the relations, the alarm rules, and the demo data it will generate. Approve to continue, or revise if something's off.

   <ImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-design.png', alt: 'The proposed design: entity hierarchy, profiles, relations, alarm rules, demo data, and the design-approval gate', caption: 'The agent lays out the entity hierarchy, profiles, relations, alarm rules, and demo data, and pauses at the design-approval gate.' },
   ]} />

5. **Revise anything that's off.** If the design isn't quite right, choose *Revise* and describe the change — the agent reworks the design and brings it back for approval, with no entities created yet. Say you want to separate frozen storage from chilled:

   ```text wrap
   split CommercialFreezer into two profiles — Freezer and Fridge — since a freezer for frozen goods needs a much lower range than a fridge for chilled goods.
   ```

   <ImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-revised.png', alt: 'Revised design splitting CommercialFreezer into Freezer and Fridge profiles, with the design-approval gate', caption: 'The agent splits CommercialFreezer into Freezer and Fridge profiles — each with its own range and over-temperature alarm — reports that everything else is unchanged, and pauses again for approval.' },
   ]} />

6. **Let it build the entities — each milestone is committed.** Once you approve, the agent writes a `README.md` for the `smart-retail-chain` solution and commits the solution directory to your project's git history, then generates everything from the design — profiles, devices, assets, customers, users, roles, and the alarm rules — with several workers running in parallel. It commits the directory again as the files land, so each milestone — the design first, then the generated entities — is its own restorable checkpoint in git.

   <ImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-build-flow.png', alt: 'The entity build: commit the design, generate the entities with parallel workers, then report the finished entities', caption: 'The agent commits the design, generates the entities with several workers in parallel, and reports the finished entities before moving on to dashboards.' },
   ]} />

7. **Review and build the dashboards.** With the entities in place, the agent designs the dashboards and pauses at a dashboard-approval gate. Here it proposes two — a chain-wide **Retail Chain Operations Center** for the admin (Stores and Alarms tabs, drilling into any store's Sensors / Analytics / Alarms) and a **Store Operations Dashboard** for the store manager (opening straight to its own store) — both carrying your UI spec (sidebar, header cards, map, toggle table, floorplan with markers, mat-drawer, per-type analytics sub-tabs, dark/light, `var(--tb-primary-500)`, 12px radius). Approve, and it commits the design, generates both dashboards in parallel, and reports the finished solution — validated and ready to deploy.

   <ImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-dashboards-flow.png', alt: 'The dashboard flow: the per-role design gate, committing and generating both dashboards in parallel, and the finished solution', caption: 'The agent proposes the two role dashboards, then on approval commits the design, generates both in parallel, and reports the finished solution — validated and ready to deploy.' },
   ]} />

8. **Deploy it.** Push the finished solution and generate demo data so the dashboards have telemetry from the first open:

   ```bash
   tb push smart-retail-chain --run-tasks
   ```

   The CLI validates, creates the entities, and prints a link to each dashboard, then runs the emulators to seed telemetry. Open one of those links — the solution is live, with demo data flowing.

   <ImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-push.png', alt: 'tb push output: entities created, links to both dashboards, and emulators seeding telemetry', caption: 'tb push creates the entities, prints a link to each dashboard, and runs the emulators to seed demo telemetry across every device.' },
   ]} />

9. **Review the solution.** Open the live solution and check it all landed as intended — the entities, the alarms, and the two role dashboards, with demo data already flowing. Each role gets its own dashboard, both sharing the same store-detail view.

   **The admin dashboard** gives a chain-wide view — all your stores on a map, with drill-down into any one. Light and dark both ship.

   <ImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-admin-stores.png', alt: 'The admin chain-wide stores overview, in light and dark', caption: 'The admin\'s chain-wide overview of every store, in light and dark.' },
   ]} />

   <ImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-store-filter.png', alt: 'The Sensors tab: devices on the store floorplan, filterable by type', caption: 'The Sensors tab — devices on the store floorplan, filterable by type.' },
   ]} />

   **The store manager dashboard** opens straight to a single store's detail view — Sensors, Analytics, and Alarms tabs. (The admin drills into a similar view from the map or store table.)

   <ImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-drawer-flow.png', alt: 'Clicking a device opens its details in a side drawer', caption: 'Click a device to open its details in a side drawer.' },
   ]} />

   <ImageGallery product={props.product} images={[
     { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-analytics-toggle.png', alt: 'The Analytics tab with per-type charts, in light and dark', caption: 'The Analytics tab — per-type charts, in light and dark.' },
   ]} />

10. **Ask for an update.** Describe the change in plain English; the agent edits only what's affected and leaves the rest untouched. Say you now want to add energy monitoring per store:

    ```text wrap
    add an energy meter to each store, reporting power and cumulative energy, and show it on the store details view alongside the other devices. calculate each store's total energy consumption and show it as its own card. on the admin dashboard, add a new analytics tab with a chart comparing energy consumption across all stores. on the store manager dashboard, add an energy option to the analytics tab, showing the store's energy analytics with a pie-chart comparison.
    ```

    The agent parses the change into a plan — here, a new `EnergyMeter` profile, the calculated fields behind the total-consumption card, and in-place updates to both dashboards (a cross-store energy analytics tab for the admin, an energy sub-tab for the store manager) — and spells out exactly what stays untouched before it applies anything.

    <ImageGallery product={props.product} images={[
      { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-update-flow.png', alt: 'The update plan and its applied result: new energy profile, calculated fields, and per-role energy views, with everything else preserved', caption: 'The agent scopes the update to just the new energy meter, its calculated fields, and the per-role energy views — every existing entity and dashboard is preserved — then on approval applies and commits it, ready to re-push.' },
    ]} />

11. **Re-deploy.** Re-push — pushes are idempotent, so the 3 new energy meters are created while the existing devices and everything else are updated in place, with nothing duplicated. `--run-tasks` seeds telemetry for just the new meters and reprocesses the calculated fields behind the total-consumption card:

    ```bash
    tb push smart-retail-chain --run-tasks
    ```

    <ImageGallery product={props.product} images={[
      { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-repush.png', alt: 'Re-push output: 3 energy meters created, existing entities updated in place, emulators seeding the new meters, calculated fields reprocessed', caption: 'The re-push creates only the 3 new energy meters and updates the rest in place, then seeds telemetry for the new meters and reprocesses the calculated fields to backfill the total-consumption history.' },
    ]} />

12. **Review the update.** Reopen the dashboards and the energy monitoring is live — the new meter, the per-store total, the admin's cross-store comparison, and the manager's energy analytics, all added without disturbing anything else:

    <ImageGallery product={props.product} images={[
      { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-admin-energy-flow.png', alt: 'The admin dashboard gains an Energy tab comparing total consumption across all stores', caption: 'The admin dashboard gains a new Energy tab — a chart comparing total energy consumption across all stores.' },
    ]} />

    <ImageGallery product={props.product} images={[
      { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-store-energy-flow.png', alt: 'The Sensors tab gains an Energy filter and the new meter appears on the floorplan', caption: 'The store\'s Sensors tab gains an Energy filter, and the new energy meter appears on the floorplan and in the device list.' },
    ]} />

    <ImageGallery product={props.product} images={[
      { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-energy-drawer-flow.png', alt: 'The new energy meter appears alongside the other sensors and opens the same mat-drawer', caption: 'The new energy meter is added alongside all the other sensors, and opens the same mat-drawer when clicked — here its power and cumulative energy.' },
    ]} />

    <ImageGallery product={props.product} images={[
      { src: '/src/assets/images/user-guide/cli-solutions/smart-retail-analytics-energy-flow.png', alt: 'The store manager Analytics gains an Energy sub-tab with a daily-consumption pie chart', caption: 'The store manager dashboard gains an Energy sub-tab in Analytics — the per-store total and a daily-consumption pie chart.' },
    ]} />
</Steps>

This walkthrough is only a small showcase — the same describe, review, deploy loop handles far more custom results than this one. Whatever you can describe, you can build the same way.

<Aside type="tip">
- Front-load context in your first prompt — domain, hierarchy, roles, and the alarms that matter — for a better first design.
- After someone edits entities in the UI, run `tb pull` before making further changes so the next `tb push` doesn't overwrite them.
- If the agent gets something wrong, just tell it what's off and it fixes forward — like any other change.
</Aside>

After a push, the CLI may ask how it went — share your experience any time with `tb feedback`.
