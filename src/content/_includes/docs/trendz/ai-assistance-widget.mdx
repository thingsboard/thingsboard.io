import { Steps, Aside } from '@astrojs/starlight/components';
import ImageGallery from '~/components/ImageGallery.astro';

The **Advanced Analytics Chat Assistant** widget embeds the Trendz AI Assistant directly into a
ThingsBoard dashboard. Users can type questions about the selected entity and receive visualizations
instantly, without leaving the dashboard.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/ai/chat-widget/chat-widget-example-1.png',
    alt: 'Chat Assistant widget on a dashboard — asking a question',
    caption: 'The Chat Assistant widget embedded in a ThingsBoard dashboard.',
  },
  {
    src: '/src/assets/images/trendz/ai/chat-widget/chat-widget-example-2.png',
    alt: 'Chat Assistant widget — seeing the result',
    caption: 'Visualization generated in response to the question.',
  },
]} />

## Key Features

| Feature | Description |
|---|---|
| **Chat-based interaction** | Type questions, edit them, and request new data. Responses appear on the right side of the chat window. |
| **History tracking** | Previous questions and answers are stored so you can revisit past queries and see updated responses with the latest data. |
| **Without alias** | The assistant answers based on all data accessible to the user — no entity selection needed. |
| **With alias** | The widget can be bound to a specific entity via a ThingsBoard alias; responses are scoped to the selected entity. |
| **Dynamic entity switching** | Selecting a different entity from a linked table automatically adjusts the assistant's responses. |
| **Dashboard-specific history** | Each widget instance maintains its own chat history. Users and customers only see their own histories. |

## Add the Widget with an Alias

This guide shows how to set up the widget so it responds about the entity selected in an entities
table on the same dashboard.

```plaintext
  ┌─────────────────────────┐    row click     ┌──────────────────────────┐
  │  Entities Table Widget  │ ───────────────▶ │  Dashboard state         │
  │  (Alias 1: asset type)  │  sets "default"  │  entity param: "default" │
  └─────────────────────────┘                  └────────────┬─────────────┘
                                                            │ bound to
                                               ┌────────────▼─────────────┐
                                               │  Chat Assistant Widget   │
                                               │  (Alias 2: state entity) │
                                               └──────────────────────────┘
```

<Steps>

1. **Create a dashboard.**

   Go to **Dashboards → Add Dashboard → Create New Dashboard**, enter a title, and click **Add**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/ai/chat-widget/create-dashboard.png',
       alt: 'Create a new dashboard',
       caption: 'Create a new dashboard in ThingsBoard.',
     },
   ]} />

2. **Set up two aliases.**

   Open **Aliases → Add Alias** and create the first alias:
   - **Alias Name**: any name (e.g., `Assets`)
   - **Filter Type**: `Asset type`
   - Select the asset type, then click **Add**.

   Add a second alias:
   - **Alias Name**: any name (e.g., `Selected Asset`)
   - **Filter Type**: `Entity from dashboard state`
   - **State entity parameter name**: `default`
   - Click **Add**.

   Click **Save** once both aliases are listed.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/ai/chat-widget/set-up-alias-1.png',
       alt: 'Open Aliases and add alias',
       caption: 'Go to Aliases → Add Alias.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/set-up-alias-2.png',
       alt: 'Configure the first alias (asset type)',
       caption: 'First alias — Filter Type: Asset type, select the desired asset type.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/set-up-alias-3.png',
       alt: 'Configure the second alias (entity from dashboard state)',
       caption: 'Second alias — Filter Type: Entity from dashboard state, State entity parameter name: default.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/set-up-alias-4.png',
       alt: 'Two aliases listed — click Save',
       caption: 'Confirm two aliases are listed, then click Save.',
     },
   ]} />

3. **Add an Entities Table widget.**

   Click **Add Widget**, select **Tables → Entities table**. Set the **Entity alias** to your
   first alias (the asset-type alias).

   Add a row-click action:
   - **Action source**: `On row click`
   - **Name**: any name
   - **State entity parameter name**: `default`

   Click **Save**, confirm the action is listed, then click **Add**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/ai/chat-widget/add-table-1.png',
       alt: 'Add Widget — select Tables',
       caption: 'Click Add Widget, select Tables.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/add-table-2.png',
       alt: 'Select Entities table',
       caption: 'Select Entities table.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/add-table-3.png',
       alt: 'Set entity alias and add an action',
       caption: 'Choose the first alias as the entity alias, then click Add action.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/add-table-4.png',
       alt: 'Add new action dialog',
       caption: 'Add a new action.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/add-table-5.png',
       alt: 'Configure the row-click action',
       caption: 'Action source: On row click. State entity parameter name: default.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/add-table-6.png',
       alt: 'Action saved and listed',
       caption: 'Click Save and confirm one action is listed.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/add-table-7.png',
       alt: 'Click Add to finalize',
       caption: 'Click Add to add the Entities Table widget.',
     },
   ]} />

4. **Add the Advanced Analytics Chat Assistant widget.**

   Click **+ Add widget**, select **Advanced analytics Bundle → Advanced analytics Chat Assistant**.

   Configure it:
   - **Entity alias**: select your second alias (the dashboard-state alias)
   - **Data key**: `Name`
   - In **Appearance**, fill in the **Filter name** field with the name of the Trendz Business Entity you want to filter by, then click **Add**.

   Click **Save**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/ai/chat-widget/add-chat-widget-1.png',
       alt: 'Click Add widget',
       caption: 'Click + Add widget.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/add-chat-widget-2.png',
       alt: 'Select Advanced analytics Bundle',
       caption: 'Select the Advanced analytics Bundle.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/add-chat-widget-3.png',
       alt: 'Select Advanced analytics Chat Assistant',
       caption: 'Select Advanced analytics Chat Assistant.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/add-chat-widget-4.png',
       alt: 'Set entity alias and data key',
       caption: 'Entity alias: second alias. Data key: Name.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/add-chat-widget-5.png',
       alt: 'Set Filter name in Appearance',
       caption: 'In Appearance, fill in the Filter name fields and click Add.',
     },
   ]} />

5. **Interact with the AI Assistant.**

   Click on any row in the Entities Table to select an asset, then click **START AI CHAT**.
   In the pop-up, type your question about the selected asset.

   The assistant remembers the conversation context — you can ask follow-up questions without
   restating the original request. To start a fresh conversation, click **New Conversation**.
   To return to a previous conversation, use the dropdown under the **Current Chat** label.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/ai/chat-widget/ai-chat-interaction-1.png',
       alt: 'Select an asset and click START AI CHAT',
       caption: 'Click on the desired asset, then click START AI CHAT.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/ai-chat-interaction-2.png',
       alt: 'Ask a question about the selected asset',
       caption: 'Ask a question about the selected asset in the pop-up window.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/ai-chat-interaction-3.png',
       alt: 'View the generated result',
       caption: 'The assistant responds with a visualization.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/ai-chat-interaction-4.png',
       alt: 'Ask follow-up questions',
       caption: 'Ask follow-up questions or add specifications — the assistant remembers context.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/ai-chat-interaction-5.png',
       alt: 'Start a new conversation',
       caption: 'Click New Conversation to start a fresh chat.',
     },
     {
       src: '/src/assets/images/trendz/ai/chat-widget/ai-chat-interaction-6.png',
       alt: 'Return to a previous conversation',
       caption: 'Use the Current Chat dropdown to return to a previous conversation.',
     },
   ]} />

   Conversations started from the dashboard widget are tagged as **Dashboard Origin** in the
   Trendz **Chats** tab. You can view messages or save generated views from there.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/ai/chat-widget/ai-chat-interaction-7.png',
       alt: 'Dashboard Origin chats in Trendz',
       caption: 'In Trendz → Chats, filter by Dashboard Origin to view or save widget-originated conversations.',
     },
   ]} />

</Steps>

<Aside type="note">
For a full reference of AI Assistant capabilities — visualization types, chat page controls,
topology entities, and limitations — see the [AI Assistant Overview](/docs/trendz/ai-assistance-overview/).
</Aside>
