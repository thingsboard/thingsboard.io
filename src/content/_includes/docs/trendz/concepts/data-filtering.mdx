import ImageGallery from '~/components/ImageGallery.astro';

Drag any field into the **Filter** section of a view to restrict which data points are included. Filters can be applied to fields that are not shown in the visualization — for example, filter out empty apartments by floor number without adding floor number as a chart axis.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/filter-example.png',
    alt: 'View configuration showing floor number and occupancy filters applied without those fields appearing on the chart axes',
    caption: 'Filtering by floor and occupancy status without adding those fields to the visualization.',
  },
]} />

## Filterable Fields

Any field present in ThingsBoard can be used as a filter:

| Field type | Examples |
|------------|---------|
| Asset / Device name | Filter to a specific device or asset by name |
| Asset / Device label | Filter by the label assigned in ThingsBoard |
| Asset / Device owner | Filter by the customer the entity belongs to |
| Attribute | Any server-side, shared, or client-side attribute |
| Telemetry | Any time-series telemetry key |

## Filter Conditions

The available conditions depend on the field's data type:

| Data type | Supported conditions |
|-----------|---------------------|
| **Numeric** | equals, not equals, greater than, less than, between, is empty |
| **Text** | equals, not equals, contains, starts with, is empty |
| **Boolean** | is true, is false |
| **Date** | see [Filter by Date](#filter-by-date) below |

Click the filter field to open its configuration dialog and choose the required condition.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/filter-dialog.png',
    alt: 'Filter field configuration dialog showing condition selector and value input',
    caption: 'Click a filter field to open its dialog — select the condition and enter the filter value.',
  },
]} />

## Filter by Hierarchy

When assets or devices are connected by relations, you can filter by a parent entity to scope the view to its children only.

**Example:** Buildings contain Apartments. Dropping **Building name** into the Filter section and selecting a specific building limits the view to apartments from that building only.

The same logic applies to customer ownership — dropping the **Owner** field filters all entities to those belonging to the selected customer.

## Filter by Date

The date filter restricts which telemetry data points are included based on a repeating calendar dimension, independent of the overall time range. This lets you focus on a specific recurring period — for example, weekdays only or a particular hour of the day.

| Date dimension | Filters by | Values |
|----------------|-----------|--------|
| **Hour** | Hour of the day | 0–23 |
| **Day** | Day of the week | Mon–Sun |
| **Date** | Day of the month | 1–31 |
| **Month** | Month of the year | Jan–Dec |

**Example — weekdays only:**

Add a **Date** field to the Filters section, open its dialog, set the dimension to **Day**, and select Monday through Friday. The view will include only data points that fall on weekdays, regardless of the selected time range.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/date-filtering-1.png',
    alt: 'Date filter dialog with Day dimension selected and Monday to Friday checked',
    caption: 'Configure the date filter — select Day as the dimension and check the days to include.',
  },
  {
    src: '/src/assets/images/trendz/date-filtering-2.png',
    alt: 'Table view showing energy consumption data for weekdays only after applying the date filter',
    caption: 'Result — only weekday rows appear in the table after applying the Mon–Fri date filter.',
  },
]} />
