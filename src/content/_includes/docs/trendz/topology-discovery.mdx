import DocLink from '~/components/DocLink.astro';
import ImageGallery from '~/components/ImageGallery.astro';
import { Steps, Aside } from '@astrojs/starlight/components';

Before building any view, Trendz must scan your ThingsBoard instance to learn which asset types,
device types, fields, and relations exist. This scan is called Topology Discovery, and its result
is the set of <DocLink product={props.product} path="concepts/business-entities">Business Entities</DocLink>
that appear in the field picker when you build analytics views.

## Full Discovery

Full Discovery performs a complete scan of your ThingsBoard instance from scratch. Run it once on
first setup, and again whenever you add new entity profile types or significantly restructure your
entity hierarchy.

### How to run it

<Steps>
1. Navigate to the **Business Entities** page using the icon in the bottom-left corner.
2. Click **Refresh Topology**.
3. Wait until Full Discovery completes.
4. Click **Finish**.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/topology-discovery/step-1-business-entities.png',
    alt: 'Business Entities icon in the Trendz bottom-left navigation',
    caption: 'Click the Business Entities icon in the bottom-left corner.',
  },
  {
    src: '/src/assets/images/trendz/topology-discovery/step-2-refresh-topology.png',
    alt: 'Refresh Topology button in the Business Entities panel',
    caption: 'Click Refresh Topology to start a full scan.',
  },
  {
    src: '/src/assets/images/trendz/topology-discovery/step-3-in-progress.png',
    alt: 'Topology Discovery in progress screen',
    caption: 'Trendz scans all asset types, device types, fields, and relations.',
  },
  {
    src: '/src/assets/images/trendz/topology-discovery/step-4-finish.png',
    alt: 'Topology Discovery Finished screen with Finish button',
    caption: 'Click Finish once discovery completes.',
  },
]} />

### How it works

Full Discovery runs the following sequence:

```plaintext
  ┌─────────────────────────────────────────┐
  │  ThingsBoard                            │
  │                                         │
  │  Asset types + Device types             │
  │              │                          │
  │              ▼                          │
  │  Sample entities per type               │
  │              │                          │
  │              ▼                          │
  │  Discover fields (attrs + telemetry)    │
  │              │                          │
  │              ▼                          │
  │  Discover relations between types       │
  └──────────────┬──────────────────────────┘
                 ▼
     Business Entities in Trendz
```

Sampling limits the number of entities analyzed per type to keep discovery fast on large
installations. Every discovered entity always includes the mandatory fields `ENTITY_ID`,
`ENTITY_NAME`, `ENTITY_LABEL`, and `OWNER`.

### Configuration

| Env variable | Default | Description |
|-------------|---------|-------------|
| `TOPOLOGY_DISCOVERY_DEFAULT_CONFIG_MIN_ITEM_TO_ANALYZE` | `500` | Minimum entities to sample per type |
| `TOPOLOGY_DISCOVERY_DEFAULT_CONFIG_MAX_ITEM_TO_ANALYZE` | `10000` | Maximum entities to sample per type |
| `TOPOLOGY_DISCOVERY_DEFAULT_CONFIG_ANALYZE_PERCENT` | `5.0` | Percentage of total entities to sample |

See <DocLink product={props.product} path="install/configuration-properties#topology-discovery">Configuration Properties — Topology Discovery</DocLink> for the full reference.

## Lite Discovery

<Aside type="caution" title="Cloud only">
  Lite Discovery is currently available on **ThingsBoard Cloud** only. Self-hosted deployments must
  use Full Discovery to keep their topology up to date.
</Aside>

Lite Discovery runs **automatically in the background** and keeps Business Entities current without
any manual intervention. New entity profile types and field changes are picked up continuously as
Trendz receives API requests.

### How it works

Each incoming API call triggers a lightweight check. Trendz decides whether to run discovery and
which mode to use based on cooldowns and what has changed:

```plaintext
  DISCOVER_NEW cooldown elapsed?  ──No──▶  (skip)
       │ Yes
       ▼
  new types in TB (not in topology)?
       │
       ├── Yes ──▶  DISCOVER_NEW
       │            new profile types only
       │
       └── No  ──▶  REFRESH_OLD cooldown elapsed?  ──No──▶  (skip)
                         │ Yes
                         ▼
                    REFRESH_OLD
                    N oldest types first
```

Both modes execute the same three phases:

```plaintext
  Phase 1 — Schema    discover field keys
  Phase 2 — Relations discover links between entity types
  Phase 3 — Types     resolve field types (numeric/bool/string)
```

The cooldowns are 5 minutes for `DISCOVER_NEW` and 10 minutes for `REFRESH_OLD`.
