import ImageGallery from '~/components/ImageGallery.astro';
import DocLink from '~/components/DocLink.astro';
import { Steps, Aside } from '@astrojs/starlight/components';

The AI Card displays cumulative telemetry for a selected time range, extends it with a forecast for the remaining portion of that period, and adds an AI-generated summary. It is well-suited for monitoring energy consumption, sales metrics, equipment performance, or any cumulative measurement where historical context and short-term forecasting matter.

```plaintext
  Time range: This Month (Oct 1 – Oct 31, today = Oct 27)

  ┌─────────────────────────────────────────────────────┐
  │  Current cumulative value  (incl. forecast)         │
  │  Δ vs. previous month  (absolute + %)               │
  │                                                     │
  │  ──────────────────────╌╌╌╌╌╌╌╌   ← main value     │
  │  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   ← prev. period   │
  │        ↑ Oct 27                                     │
  │  <── real data ──><── forecast ──>                  │
  │                                                     │
  │  AI summary text …                                  │
  └─────────────────────────────────────────────────────┘
```

## Base AI Card

<Steps>
1. Open Trendz and create a new view, selecting **AI Card** as the visualization type.

2. Drag the telemetry field you want to analyze into the **Value** column and choose an aggregation type.

3. Select a time range. The card shows real data from the start of the range up to today, and generates a forecast for all remaining dates in the range.

   **Example:** With *This Month* selected on October 27th, the card shows actual data for Oct 1–27 and a forecast for Oct 28–31.

4. Click **Build Report**.
</Steps>

The resulting card shows six elements:

| # | Element | Description |
|---|---------|-------------|
| 1 | Current cumulative value | Total for the range including forecast; color-coded by threshold |
| 2 | Difference from previous period | Absolute change and percentage versus the equivalent preceding period |
| 3 | Historical data (solid line) | Actual cumulative telemetry up to today |
| 4 | Predicted data (dashed line) | Forecast extending to the end of the selected range |
| 5 | Previous period trend (shaded area) | Cumulative trend from the equivalent previous period |
| 6 | AI summary | Auto-generated interpretation based on historical data and the selected prompt |

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/base-ai-card.png',
    alt: 'AI Card widget showing cumulative telemetry, forecast, previous period comparison, and AI summary',
    caption: 'Base AI Card: numbered callouts correspond to the six elements described in the table above.',
  },
]} />

## Configuration

### Prediction method

The AI Card uses **Fourier Transformation** by default. To switch methods:

<Steps>
1. Click the telemetry field in the **Value** column to open its settings.
2. Go to the **Prediction** section.
3. Select the desired method from the dropdown.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/ai-card-prediction.png',
    alt: 'Prediction section in field settings showing available forecast methods',
    caption: 'Prediction settings: choose the forecast algorithm that best fits your data characteristics.',
  },
]} />

### Prediction time range

The card generates predictions for all dates within the selected range that fall after the current timestamp.

| Time range | Behavior |
|------------|----------|
| *This Month* | Real data from the 1st through today; forecast for the rest of the month |
| *This Year* | Real data from Jan 1 through today; forecast for the rest of the year |
| Custom *Oct 1 – Nov 15* (today = Oct 27) | Real data Oct 1–27; forecast Oct 28 – Nov 15 |

Use predefined ranges such as *This Month* or *This Year* for the most meaningful combined view.

### AI summary

The AI summary is generated automatically using the LLM model configured in the Trendz-wide <DocLink product={props.product} path="ai-assistance-overview">AI settings</DocLink>. To configure the summary for this card:

<Steps>
1. Open **Chart settings → AI Assistant**.
2. To hide the summary, uncheck **Show AI summary**.
3. To change the analysis focus, select a different entry from the **Prompt** dropdown.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/ai-card-summary.png',
    alt: 'AI Assistant settings showing Show AI summary checkbox and Prompt dropdown',
    caption: 'AI summary settings: toggle visibility and select the prompt that best interprets your data.',
  },
]} />

Custom prompts can be created for domain-specific use cases. See <DocLink product={props.product} path="ai-assistance-prompts">Trendz AI Prompts</DocLink> for details.

### Value color threshold

The cumulative value at the top of the card is color-coded based on its percentage change relative to the previous period:

| Color | Meaning | Default condition |
|-------|---------|-------------------|
| Blue | Normal performance | Absolute change ≤ 10% |
| Red | Significant deviation | Absolute change > 10% |

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/ai-card-threshold-normal.png',
    alt: 'AI Card with blue value indicating normal performance within the 10% threshold',
    caption: 'Blue: the current cumulative value is within the normal range.',
  },
  {
    src: '/src/assets/images/trendz/ai-card-threshold-deviation.png',
    alt: 'AI Card with red value indicating significant deviation exceeding the threshold',
    caption: 'Red: the current cumulative value has deviated beyond the configured threshold.',
  },
]} />

To customize the threshold, open **Value Color Logic** in the card settings:

| Option | Behavior |
|--------|----------|
| Default | 10% threshold for all color calculations |
| Custom | Set your own threshold percentage (e.g. 5% for sensitive alerts, 20% for coarser alerting) |
| Unset | Color-coding disabled; value always displays without highlighting |

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/ai-card-threshold-config.png',
    alt: 'Value Color Logic setting with Default, Custom, and Unset options',
    caption: 'Value Color Logic: adjust the deviation threshold or disable color-coding entirely.',
  },
]} />
