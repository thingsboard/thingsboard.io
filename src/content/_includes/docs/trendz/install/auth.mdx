import DocLink from '@components/DocLink.astro';
import ImageGallery from '~/components/ImageGallery.astro';

## Authentication

Access Trendz UI at `http://localhost:8888`.

Trendz uses ThingsBoard as its authentication provider — log in with your **Tenant Administrator** credentials from ThingsBoard.

### Topology discovery

After the first login, discover the topology so Trendz can learn about your assets, devices, profiles, and relations:

<ImageGallery images={[
  { src: '/src/assets/images/trendz/install/topology/topology-discovery-1.png', alt: 'Click Discover Topology', caption: 'Click the <b>Discover Topology</b> button to start the topology discovery process.' },
  { src: '/src/assets/images/trendz/install/topology/topology-discovery-2.png', alt: 'Click Finish', caption: 'Click the <b>Finish</b> button to close the topology discovery page and unlock the main Trendz functionality.' },
]} />

See <DocLink product={props.product} path="concepts/business-entities">Business Entities</DocLink> for more on how Trendz uses this topology.
