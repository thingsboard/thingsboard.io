import { Aside } from '@astrojs/starlight/components';
import ImageGallery from '~/components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';

Trendz provides a guided three-step wizard to build an anomaly model — no manual algorithm configuration required.
Pick a detection intent, scope your data, and fine-tune sensitivity. The wizard configures everything else automatically.

## Steps to Create

### Step 1: Open the Wizard

Two entry points are available on the Anomaly Detection page:

- **Create model** — button in the top-right corner, always visible on all tabs.
- **+ Create** — inline button on any profile row in the Profile Coverage table. Opens the wizard with that profile pre-selected.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/create-btn-top.png',
    alt: 'Anomaly Detection Summary tab with the "Create model" button visible in the top-right corner',
    caption: '"Create model" in the top-right corner — always accessible from any tab.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/create-btn-inline.png',
    alt: 'Anomaly Detection Summary tab with the "+ Create" button highlighted on a profile row',
    caption: '"+ Create" on a profile row — opens the wizard with that profile already selected.',
  },
]} />

### Step 2: Choose Detection Intent

The **Select Intent** step asks what you want to detect. Choose the strategy that matches your goal —
each option automatically pre-configures the optimal clustering, segmentation, and scoring settings:

| Intent | Best for |
|--------|----------|
| **Quick Scan** *(recommended)* | Rapid baseline scan for obvious deviations in recent data. Best starting point for most use cases. |
| **Operational Monitoring** | Continuous alerting for deviations from known seasonal or operational patterns. Uses a rolling window for ongoing detection. |
| **Comparative Analysis** | Identifies units that behave differently from the peer group. Useful for fleet-wide benchmarking. |
| **Health Monitoring** | Detects long-term gradual drift in overall system health. Uses progressive tracking across extended time ranges. |

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/create-intent-wizard.png',
    alt: 'Select Intent step showing Quick Scan, Operational Monitoring, Comparative Analysis, and Health Monitoring options',
    caption: 'Choose the intent that matches your detection goal. Quick Scan is recommended for first-time use.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/create-intent-next.png',
    alt: 'Select Intent step with the "Next: Select Intent" button highlighted',
    caption: 'Click "Next: Select Intent" to confirm your strategy and proceed.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/create-intent-advanced.png',
    alt: 'Select Intent step with Advanced configuration link highlighted at the bottom',
    caption: '"Advanced configuration" — bypass the wizard and configure all parameters manually.',
  },
]} />

<Aside type="note">
  Need full control over every parameter? Click **Advanced configuration** at the bottom of the screen
  to switch to [Advanced Mode](#advanced-mode), where you can manually configure clustering, segmentation,
  and scoring thresholds.
</Aside>

### Step 3: Configure Input Scope

The **Configure** step defines the entities, telemetry keys, and time range the model will train on.

**Entity Selection** — select the entity type and profile to analyze:

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/create-scope-entity-type.png',
    alt: 'Configure step with entity type dropdown open showing Device profiles and Asset profiles',
    caption: 'Choose Device profiles or Asset profiles as the entity type.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/create-scope-profile.png',
    alt: 'Configure step with profile selection dropdown open listing available device profiles',
    caption: 'Select the specific profile — the list shows all available profiles for the chosen entity type.',
  },
]} />

<Aside type="tip">
  Used **+ Create** in Step 1? The entity type and profile are already pre-filled here — skip straight to item selection.
</Aside>

**Item Selection** — choose which devices or assets from the profile to include:

| Mode | Behavior |
|------|----------|
| **All entities** | Includes every device or asset in the selected profile. |
| **Manual selection** | Pick specific items from a searchable list. |

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/create-scope-items-mode.png',
    alt: 'Items mode dropdown open showing All entities and Manual selection options',
    caption: 'Choose between All entities or Manual selection.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/create-scope-items-all.png',
    alt: 'Configure step with All entities mode selected showing 14 entities included and Data Readiness Pending',
    caption: '"All entities" — all items in the profile are included automatically.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/create-scope-items-selected.png',
    alt: 'Configure step with several devices selected as chips and Data Readiness showing Pending',
    caption: '"Manual selection" — pick specific items; selected devices appear as removable chips.',
  },
]} />

**Telemetry Selection** — pick the numeric telemetry keys the model will monitor. Type to search all keys available for the selected profile:

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/create-scope-telemetry.png',
    alt: 'Configure step with Telemetry Selection field highlighted and a consumption key added',
    caption: 'Select the telemetry keys to analyze. Only combine keys that physically correlate with each other.',
  },
]} />

**Date Range** — set the historical period for training. Use a quick preset or enter a custom range:

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/create-scope-date-range.png',
    alt: 'Configure step with Date Range section highlighted showing All available, Last 30/90 days, Last year, Last 3 years presets',
    caption: 'Presets: All available, Last 30 days, Last 90 days, Last year, Last 3 years.',
  },
]} />

**Data Readiness** — the panel on the right updates in real time as you fill in each field:

| Field | Description |
|-------|-------------|
| **Status** | Overall assessment: **Pending** → **Poor** → **Fair** → **Good** → **Excellent**. |
| **Entities** | Number of devices or assets selected. |
| **Telemetry keys** | Number of keys selected. |
| **Approximate points** | Estimated total data points available for training. |
| **Step frequency** | Approximate interval between consecutive readings. |
| **Time range** | Duration of the selected training period. |

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/create-scope-readiness.png',
    alt: 'Data Readiness panel showing Excellent status with entity count, telemetry keys, data points, step frequency, and time range',
    caption: 'Excellent status confirms sufficient data quality for reliable anomaly detection.',
  },
]} />

Aim for **Good** or **Excellent** before proceeding. A **Poor** status typically indicates too few data points,
too short a time range, or too sparse a telemetry key.

Once the status is acceptable, click **Next: Configure** to advance to the final step.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/create-scope-next.png',
    alt: 'Configure step with "Next: Configure" button highlighted in the bottom-right corner',
    caption: 'Click "Next: Configure" once the Data Readiness status is Good or Excellent.',
  },
]} />

### Step 4: Review & Build

The **Review** step lets you fine-tune two parameters before the model trains.

**Sensitivity** — controls how aggressively the model flags anomalies:

| Option | Behavior |
|--------|----------|
| **Strict** | Minimizes false positives. Only flags extreme deviations. Best when alert fatigue is a concern. |
| **Balanced** *(recommended)* | Best default for most industrial IoT environments. |
| **Don't miss anything** | Maximum recall — flags every potential deviation for expert review. Expect more false positives. |

**Detection Granularity** — the time window the model evaluates data in. Trendz suggests up to five options
based on your data's step frequency. Shorter windows catch brief spikes; longer windows surface gradual drift.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/create-review-sensitivity.png',
    alt: 'Review & Build step with Sensitivity section highlighted — Strict, Balanced, Don\'t miss anything options',
    caption: 'Sensitivity — choose how aggressively the model flags anomalies.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/create-review-granularity.png',
    alt: 'Review & Build step with Detection Granularity highlighted showing 3h, 6h, 12h, 1d, 2d options',
    caption: 'Detection Granularity — select the analysis window that fits your anomaly patterns.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/create-review-finish.png',
    alt: 'Review & Build step with Finish button highlighted in the bottom-right corner',
    caption: 'Click Finish to launch model training.',
  },
]} />

When satisfied, click **Finish** to submit the model for training.

The model enters the **Queued → Running → Ready** lifecycle. Training typically completes within seconds
to a few minutes depending on data volume.

### Step 5: Explore Results

Once training completes, Trendz redirects you to the **Result** tab of the newly built model.
It shows an immediate overview of what the model found: key metrics, an activity heatmap,
ranked items by anomaly impact, and a list of top anomalies.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/create-result.png',
    alt: 'Result tab after model training showing key metrics, activity heatmap, ranked items, and top anomalies',
    caption: 'Result tab — inspect the model\'s initial findings immediately after training completes.',
  },
]} />

Review the results to verify the model is detecting meaningful anomalies before enabling continuous
monitoring or alarms. See <DocLink product={props.product} path="anomaly/manage">Anomaly Model Results</DocLink>
for a detailed guide on analyzing results.

## Advanced Mode

The wizard covers the most common detection scenarios. For full control over every parameter —
clustering algorithm, distance function, segmentation window, score thresholds, and limits — use
**Advanced Mode** instead.

Access it via **Advanced configuration** on the intent selection step.
Full parameter documentation is available in
<DocLink product={props.product} path="anomaly/build">Anomaly Model Properties</DocLink>.

## Best Practices

| Situation | Recommendation |
|-----------|----------------|
| **Too many anomalies** | Switch to **Strict** sensitivity. The model is flagging normal variation — tighten the threshold to reduce noise. |
| **Too few anomalies** | Switch to **Don't miss anything** or choose a shorter detection granularity. The model may be too conservative. |
| **Selecting telemetry keys** | Only combine keys that physically correlate — e.g. vibration and temperature on the same pump. Mixing unrelated keys degrades model accuracy. |
| **Training data quality** | Use a period of normal operation. Avoid ranges covering outages, commissioning, or maintenance — they skew what the model learns as "normal." |
| **Using Advanced Mode** | Only switch if you understand clustering and segmentation. Misconfigured parameters can produce worse results than the defaults. |
| **Training data volume** | Aim for at least 3 months of regular telemetry. Sparse or short datasets result in Poor readiness and unreliable detection. |
