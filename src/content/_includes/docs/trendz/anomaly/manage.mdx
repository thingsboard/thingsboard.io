import { Aside } from '@astrojs/starlight/components';
import ImageGallery from '~/components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';

Once an anomaly model finishes building, its detail view opens automatically. The interface is
organized into five tabs — **Result**, **Review**, **Cluster Info**, **Properties**, and **Tasks** —
each serving a distinct role in understanding and managing detection quality.

## Result Tab

The **Result** tab is the primary view for analyzing what the model detected. It shows fleet-level
metrics, device-by-device rankings, and the most significant anomalies found.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/result-tab-overview.png',
    alt: 'Result tab showing key metrics, analysis scope, cluster analysis, activity heatmap, ranked items, and top anomalies',
    caption: 'Result tab — full view of model findings.',
  },
]} />

### Key Metrics

Four cards at the top of the Result tab summarize detection quality at a glance:

| Card | Color | Meaning |
|------|-------|---------|
| **Anomalies** | Red | Total count of anomaly events detected by this model |
| **Affected** | Blue | Ratio of items with at least one anomaly to total scanned items (e.g. 12 / 535) |
| **Peak Score** | Orange | The highest <DocLink product={props.product} path="anomaly/overview#key-metrics">anomaly score</DocLink> recorded by this model across all items and time |
| **Health** | Green | Percentage of time during which telemetry showed no anomalous behavior |

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/result-key-metrics.png',
    alt: 'Result tab with Key Metrics cards highlighted — Anomalies, Affected, Peak Score, and Health',
    caption: 'Key Metrics — four cards summarizing detection quality at a glance.',
  },
]} />

### Analysis Scope

The Analysis Scope section provides context about what data the model processed:

| Field | Description |
|-------|-------------|
| **Time Window** | Total duration of the analyzed time range (e.g. 2 months 29 days) |
| **Items Scanned** | Number of scanned items out of total items in the profile (e.g. 5/10) |
| **Fields** | Number of telemetry keys used by this model for detection |
| **Profile** | The device or asset profile whose items were analyzed |
| **Total Impact** | Sum of all <DocLink product={props.product} path="anomaly/overview#key-metrics">anomaly score indexes</DocLink> across all detected anomalies — reflects the total cumulative deviation load |
| **Last Scan** | Time elapsed since the most recent scan run (e.g. "5 minutes ago") |

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/result-analysis-scope.png',
    alt: 'Result tab with Analysis Scope section highlighted showing time window, items scanned, and other fields',
    caption: 'Analysis Scope — context about the data the model processed.',
  },
]} />

### Cluster Analysis

The Cluster Analysis section summarizes how the model partitioned normal behavior into clusters
during training:

| Element | Description |
|---------|-------------|
| **Cluster count** | Total number of clusters the model identified |
| **CS** (dominant cluster) | The cluster containing the highest percentage of training segments — represents the most common normal behavior pattern |
| **Score range** | Min–max anomaly score range observed across all clusters (e.g. 76–335) |
| **Color bar** | Each color segment represents one cluster, sized proportionally to the percentage of training data it covers |

Clusters with less than 10% of segments are unlabeled in the bar but still represented by a color.
Hover over a segment to see the cluster ID and percentage.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/result-cluster-analysis.png',
    alt: 'Result tab with Cluster Analysis section highlighted showing cluster count, dominant cluster, score range, and color bar',
    caption: 'Cluster Analysis — how training data was partitioned into normal behavior clusters.',
  },
]} />

### Continuous Monitoring

The **Continuous Monitoring** card on the right side of the Result tab shows whether automated
detection is running for this model. Three status indicators are displayed:

| Indicator | Active state | Inactive state |
|-----------|-------------|----------------|
| **Periodic Scanning** | Scan frequency and entity count (e.g. "Every 1h · 5 entities") | "Disabled" |
| **Score Telemetry** | "Enabled" | "Disabled" |
| **Alarm Generation** | Severity thresholds (e.g. "Critical ≥ 80 · Major ≥ 60") | "Disabled" |

Click **Configure** to open the monitoring setup dialog. See
<DocLink product={props.product} path="anomaly/monitoring">Monitoring & Alarms</DocLink> for full
configuration details.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/result-continuous-monitoring.png',
    alt: 'Result tab with Continuous Monitoring panel highlighted showing periodic scanning, score telemetry, and alarm generation status',
    caption: 'Continuous Monitoring — current status of scanning, telemetry saving, and alarm generation.',
  },
]} />

### Activity Heatmap

The Activity Heatmap shows anomaly intensity across the last 90 days — one cell per day per item.
Color encodes how severe the anomaly activity was on each day:

| Color | Meaning |
|-------|---------|
| **Gray** | Not scanned — no data available for this day |
| **Green** | Clean — no anomalies detected |
| **Yellow** | Low anomaly activity |
| **Orange** | Moderate anomaly activity |
| **Red** | High anomaly activity |

Hover over any cell to see the date, anomaly count, and maximum score for that day.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/result-activity-heatmap.png',
    alt: 'Result tab with Activity Heatmap highlighted showing 90 days of anomaly intensity per device',
    caption: 'Activity Heatmap — 90 days of anomaly intensity color-coded per device per day.',
  },
]} />

### Ranked Items

The Ranked Items table lists all scanned devices or assets sorted by anomaly activity. Each row shows:

| Column | Description |
|--------|-------------|
| **Item** | Device or asset name with a color indicator dot |
| **Anomalies** | Count of anomaly events detected for this item |
| **Peak Score** | Highest anomaly score recorded for this item |
| **Total Duration** | Combined duration of all anomaly events for this item |
| **Total Impact** | Sum of score indexes — cumulative anomaly footprint |
| **Last Seen** | Timestamp of the most recent anomaly, or green "No anomalies" if clean |
| **Alarms** | Number of ThingsBoard alarms created for this item |

Click any row to open the **Review** tab with that item pre-selected, showing its anomaly chart
and detection history.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/result-ranked-items.png',
    alt: 'Result tab with Ranked Items table highlighted showing devices sorted by anomaly activity',
    caption: 'Ranked Items — all scanned devices sorted by anomaly activity.',
  },
]} />

### Top Anomalies

The Top Anomalies panel on the right side of the Result tab lists the 10 highest-scoring individual
anomaly events across all items. Each entry shows the item name, score badge, start/end time,
and duration. Border color indicates severity:

Click any entry to open the **Review** tab navigated directly to that anomaly.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/result-top-anomalies.png',
    alt: 'Result tab with Top Anomalies panel highlighted showing the 10 highest-scoring anomaly events',
    caption: 'Top Anomalies — the 10 highest-scoring individual anomaly events across all items.',
  },
]} />

---

## Review Tab

The **Review** tab shows the full anomaly detection history for the model. Use it to inspect
individual anomaly events, validate detection quality, and determine whether results are reliable
before enabling monitoring or alarms.

**Chart** — the top section displays the telemetry and anomaly score for the selected row:
- Raw telemetry values on the left Y-axis
- Anomaly score on the right Y-axis
- Red background highlights the active anomaly window

Hover over the chart to see exact values at any point in time.

**Anomaly Score Table** — the bottom section lists every anomaly detected during build, refresh, and
reprocess runs. Each row contains:

| Column | Description |
|--------|-------------|
| **Item** | Device or asset name |
| **Start** | Anomaly start timestamp |
| **End** | Anomaly end timestamp |
| **Duration** | Length of the anomaly event |
| **Cluster ID** | Which cluster this anomaly segment was compared against |
| **Score** | Anomaly score — how far the segment deviated from normal |
| **Score Index** | Anomaly score index — cumulative impact value |

Sort by any column to prioritize analysis. Use the **All items** dropdown in the top-right corner
of the table to filter by a specific device or asset.

Click any row in the table to load that anomaly event into the chart above — the telemetry and
anomaly score for the selected time window are displayed together, with the anomaly window
highlighted in red.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/review-tab-overview.png',
    alt: 'Review tab showing the anomaly chart at the top and the Anomaly Score Table below',
    caption: 'Review tab — anomaly chart and sortable Anomaly Score Table.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/review-chart.png',
    alt: 'Review tab chart with hover tooltip showing telemetry and anomaly score values at a specific point',
    caption: 'Chart — hover to inspect exact telemetry and score values at any point.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/review-table.png',
    alt: 'Anomaly Score Table highlighted showing all anomaly rows with sortable columns',
    caption: 'Anomaly Score Table — sortable list of all detected anomaly events.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/review-item-filter.png',
    alt: 'Anomaly Score Table with the All items dropdown open showing a list of devices to filter by',
    caption: '"All items" dropdown — filter the table to a specific device or asset.',
  },
]} />

---

## Cluster Info Tab

The **Cluster Info** tab shows how the model organized training data into clusters. Use it to
validate that clustering accurately captured the range of normal operating behaviors before
interpreting anomaly results.

**Centroid visualization** — the chart at the top shows the average shape (centroid) for each
cluster. Each line represents the typical pattern of segments belonging to that cluster. A well-trained
model produces clearly distinct centroid shapes.

**Cluster Score Table** — the bottom section summarizes each cluster:

| Column | Description |
|--------|-------------|
| **Cluster ID** | Unique cluster identifier |
| **Segments Count** | Number of training segments assigned to this cluster |
| **Segments Percent** | Percentage of all training segments in this cluster |
| **Sum Duration** | Combined duration of all segments in this cluster |
| **Min Score** | Minimum anomaly score among segments in this cluster |
| **Max Score** | Maximum anomaly score among segments in this cluster |

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/cluster-info-tab-overview.png',
    alt: 'Cluster Info tab with the Cluster Info tab highlighted, showing the centroid chart and Cluster Score Table',
    caption: 'Cluster Info tab — centroid visualization and cluster summary table.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/cluster-info-centroid.png',
    alt: 'Cluster Info tab with the Anomaly Cluster Info centroid chart highlighted showing distinct centroid lines for each cluster',
    caption: 'Centroid visualization — average segment shape for each cluster.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/cluster-info-table.png',
    alt: 'Cluster Info tab with the Cluster Score Table highlighted showing segment counts, percentages, duration, and score ranges per cluster',
    caption: 'Cluster Score Table — per-cluster breakdown of segments, duration, and score range.',
  },
]} />

Click any row to see a **detailed view** of that cluster: its centroid shape alongside 20+ random
member segments, plus a score distribution histogram. This is useful for assessing how consistent
the cluster is and whether it captures a coherent operating pattern.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/cluster-info-cluster-detail.png',
    alt: 'Cluster Info tab with a specific cluster row selected, showing the centroid chart overlaid with member segments for that cluster',
    caption: 'Cluster detail — centroid with member segments for the selected cluster.',
  },
]} />

---

## Properties Tab

The **Properties** tab contains the full model configuration — general settings, clustering algorithm,
segmentation parameters, anomaly detection thresholds, limits, and alarm configuration.

Use the Properties tab to review current settings, rename the model, or modify parameters and rebuild.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/input-tab-overview.png',
    alt: 'Properties tab highlighted showing General Settings with model name, entity selection, telemetry selection, and date range fields',
    caption: 'Properties tab — General Settings with the full model configuration.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/input-general-settings.png',
    alt: 'Properties tab General Settings with the Rebuild button highlighted in the top-right corner',
    caption: 'Rebuild button — appears after modifying any parameter that requires retraining.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/input-advanced-properties.png',
    alt: 'Properties tab with Advanced Properties highlighted showing Clustering Settings, Segmentation Settings, Anomaly Detection Settings, and Limits sections',
    caption: 'Advanced Properties — clustering, segmentation, detection, and limits settings.',
  },
]} />

<Aside type="caution">
  Most parameter changes trigger a full model rebuild when you click **Rebuild**. Rebuilding
  deletes all existing detected anomalies and disables the refresh job. Only name change can be saved without rebuilding.
</Aside>

See <DocLink product={props.product} path="anomaly/build">Anomaly Model Properties</DocLink> for
a complete reference of all available settings.

---

## Tasks Tab

The **Tasks** tab provides visibility into the execution history of all background operations
associated with this model. See <DocLink product={props.product} path="tasks-service">Task Service</DocLink>
for full details on managing background tasks in Trendz.

### Anomaly Tasks

Three task types run against an anomaly model:

| Job Type | Task type | Description |
|----------|-----------|-------------|
| **Build anomaly model** | Run Once | Initial training run that produces clusters and baseline anomaly scores. Triggered on model creation or save. |
| **Anomaly autodiscovery** | Scheduled | Incremental scan of new telemetry to detect ongoing and new anomalies. Runs automatically at the configured refresh interval. |
| **Find anomalies** | Run Once | Applies the trained model to a historical date range. Triggered on demand via Reprocess. |

Each row shows the task name, creation time, last updated time, status, task type, and job type.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/tasks-tab-overview.png',
    alt: 'Tasks tab showing the Anomaly Tasks table with Build, Refresh, and Reprocess task entries',
    caption: 'Tasks tab — execution history of all background operations for this model.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/tasks-table.png',
    alt: 'Anomaly Tasks table highlighted showing three task rows with their status and job type',
    caption: 'Anomaly Tasks table — three task types: Build, Refresh (Autodiscovery), and Reprocess.',
  },
]} />

Click **⋯** on any row to open the row action menu.

**Details** opens the Task Executions dialog showing the full run history for that task — each
execution with its start time, duration, finish time, and status.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/tasks-row-actions-details.png',
    alt: 'Anomaly Tasks table row action menu open with the Details option highlighted',
    caption: 'Row actions — Details opens the Task Executions dialog.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/tasks-execution-details.png',
    alt: 'Task Executions dialog showing a list of executions with start time, duration, finish time, and status columns',
    caption: 'Task Executions dialog — full run history for the selected task.',
  },
]} />

**Go to Task** navigates to the standalone task detail page, showing full task metadata and the
complete execution history outside the anomaly model context.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/tasks-row-actions-go-to-task.png',
    alt: 'Anomaly Tasks table row action menu open with the Go to Task option highlighted',
    caption: 'Row actions — Go to Task navigates to the standalone task detail page.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/tasks-task-detail.png',
    alt: 'Standalone task detail page showing task metadata and execution history for an Anomaly autodiscovery task',
    caption: 'Task detail page — full metadata and execution history for a single task.',
  },
]} />

### System Pulse

The System Pulse panel shows the last 10 scan executions for this model. Each entry displays the
execution time and one of four status indicators:

| Color | Status | Meaning |
|-------|--------|---------|
| Green | OK | Scan completed — no anomalies found |
| Orange | Anomalies | Scan completed — anomalies were detected |
| Red | Failed | Scan terminated due to an unexpected error |
| Gray | Canceled | Scan task was canceled before completion |

Click any pulse entry to navigate directly to the relevant view — the Review tab if anomalies were
found, or the Result tab for a clean run.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/tasks-system-pulse.png',
    alt: 'Tasks tab with System Pulse panel highlighted showing recent scan executions with color-coded status indicators',
    caption: 'System Pulse — last 10 scan executions with color-coded status.',
  },
]} />

---

## Actions

Two page-level actions are available in the top-right corner of any model detail view.

### Scan Recent Data

The **Scan Recent Data** button opens the Reprocess Task dialog, letting you apply the trained
model to any historical time range on demand. This is equivalent to triggering a reprocess without
navigating to the Tasks tab. See <DocLink product={props.product} path="anomaly/monitoring">Monitoring & Alarms</DocLink>
for details on scheduled scanning and reprocess configuration.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/actions-scan-recent-data.png',
    alt: 'Model detail header with the Scan Recent Data button highlighted in the top-right corner',
    caption: 'Scan Recent Data — triggers the Reprocess Task dialog.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/actions-reprocess-dialog.png',
    alt: 'Run Reprocess Task dialog showing a date range picker and scope options — Trained Items, All in profile, or Specific',
    caption: 'Reprocess Task dialog — select the date range and entity scope to scan.',
  },
]} />

### More Actions

The **More actions** menu (⋯) provides two additional options:

**Duplicate** creates a copy of this model's full configuration as a new model. The copy opens in
the Properties tab ready for modification and rebuild — useful for testing parameter variations
without losing the original.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/actions-more-menu-duplicate.png',
    alt: 'More actions dropdown menu open with the Duplicate option highlighted',
    caption: 'More actions — Duplicate creates a copy of the model configuration.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/actions-duplicate-result.png',
    alt: 'Duplicated model opened in the Properties tab showing the model name with (Copy) suffix and a Disabled status badge',
    caption: 'Duplicate result — the copy opens in the Properties tab ready to configure and rebuild.',
  },
]} />

**Monitoring** opens the monitoring configuration dialog directly, without navigating to the
Continuous Monitoring card. Equivalent to clicking Configure on the Result tab. See
<DocLink product={props.product} path="anomaly/monitoring">Monitoring & Alarms</DocLink> for full
configuration details.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/anomaly/actions-more-menu-monitoring.png',
    alt: 'More actions dropdown menu open with the Monitoring option highlighted',
    caption: 'More actions — Monitoring opens the monitoring configuration dialog.',
  },
  {
    src: '/src/assets/images/trendz/anomaly/actions-monitoring-config.png',
    alt: 'Monitoring configuration dialog showing Periodic Scanning, Anomaly Score Saving, and Generate Alarms settings with severity thresholds',
    caption: 'Monitoring configuration — configure scanning frequency, score telemetry, and alarm thresholds.',
  },
]} />
