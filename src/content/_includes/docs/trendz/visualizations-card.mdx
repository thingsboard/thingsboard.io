import ImageGallery from '~/components/ImageGallery.astro';
import YouTubeVideo from '~/components/YouTubeVideo.astro';

The Card widget displays a single key metric and lets you monitor its trend by comparing it against a reference value or a previous time period.

## Video tutorial

<YouTubeVideo videoId="ZXORc5nipgg" title="Trendz Card widget tutorial" />

## Simple card

Drop the field you want to monitor into the **Main Value** slot and choose an aggregation type (sum, average, last, etc.). The card shows the aggregated result for the current time range.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/card-simple.png',
    alt: 'Simple card widget configuration with one field in the Main Value slot',
    caption: 'Basic card: one field in Main Value with an aggregation type selected.',
  },
]} />

## Compare with another value

To show how a metric compares against a target or another measurement, add a second field to the **Compared value** slot. The card renders the difference between the two figures as a percentage badge — **green** when the main value is above the compared value, **red** when it is below.

**Example:** Compare the number of products actually produced against the required production target stored as device telemetry.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/card-compare.png',
    alt: 'Card configuration with a compared value field showing percentage difference',
    caption: 'Comparison card: the percentage badge shows how far the main value is from the compared value.',
  },
]} />

## Compare with a previous interval

Use **Local Date** to compare the current period with the immediately preceding one — without building a separate view.

**Example:** Show how much coffee was consumed this month versus last month.

```plaintext
  Time range selector:  This Month
                        ┌────────────────┐
  Main Value            │  87 kg         │
                        │  ▲ +12%        │  ← vs. Prev Month
                        └────────────────┘

  Compared value  ──▶  Local Date ON  +  Prev Month
```

To configure this:

1. Add the same field to the **Compared value** slot.
2. Enable **Local Date** for that field.
3. Set the **Compared value** time range to **Prev Month**.
4. Set the main view time range to **This Month**.

The card then shows the percentage change between this month and last month.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/card-local-config.png',
    alt: 'Card configuration using Local Date and Prev Month for period-over-period comparison',
    caption: 'Local Date + Prev Month: the compared value uses its own time range independent of the dashboard time range.',
  },
]} />

## Card title

Open **Settings** (bottom-right corner of the editor) and enter a name in the **Title** field to label the card for quick identification on a dashboard.
