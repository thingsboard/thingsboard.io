import { Steps, Aside } from '@astrojs/starlight/components';
import ImageGallery from '~/components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';

When a calculated field is **enabled**, Trendz automatically writes its results to ThingsBoard as telemetry under the key `_ECD_{KEY}`. This makes calculated metrics available anywhere ThingsBoard supports telemetry — dashboards, alarm rules, rule chains, predictions, and further calculations.

Disabling a calculation field stops new writes but **never deletes** existing telemetry already stored in ThingsBoard.

```plaintext
  Trendz calculated field
          │
          │ writes telemetry (_ECD_yourKey)
          ▼
  ThingsBoard entity
          │
  ┌───────┴────────────────────────────────┐
  │                                        │
  ▼                                        ▼
Alarm Rules                        Dashboards / Rule Engine /
(threshold alerts)                 Predictions / Anomaly models
```

Two jobs control when data is written:

| Job | When it runs | Purpose |
|-----|-------------|---------|
| **Reprocess** | Once, on demand | Backfill historical data for the selected time range |
| **Refresh** | Scheduled (recurring) | Keep telemetry up to date at a defined frequency |

## Saving a calculation field

Before enabling a calculation, verify the key parameters that determine how data will appear in ThingsBoard:

| Parameter | Description |
|-----------|-------------|
| **Grouping Interval** | Time bucket size — e.g. `HOUR` stores one point per hour in ThingsBoard. |
| **Aggregation** | How values within a bucket are combined — e.g. `SUM` totals all readings in the hour. |
| **Time** | Time range used by the refresh job. Choose a range that covers what each refresh cycle needs to process. |

**Recommended time settings by job frequency:**

| Frequency | Recommended time range |
|-----------|------------------------|
| Minute / Hour | Today |
| Day | Last 7 days |
| Week | Last 14 days |
| Month | Last 3 months |

<Steps>

1. Open the calculation field and verify **Grouping Interval**, **Aggregation**, and **Time**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/calculation-parameters-1.png',
       alt: 'Calculation field Function tab showing Grouping Interval and Aggregation settings',
       caption: 'Verify Grouping Interval, Aggregation, and Time before enabling.',
     },
   ]} />

2. Click **Save Field** to apply changes.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/calculation-parameters-2.png',
       alt: 'Save Field button in the calculation field editor',
       caption: 'Click Save Field to save your changes.',
     },
   ]} />

3. In the confirmation dialog, click **Save**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/calculation-parameters-3.png',
       alt: 'Save confirmation dialog',
       caption: 'Confirm saving the calculation field.',
     },
   ]} />

4. You will be prompted to enable the calculation field and run a refresh task. You can do it now or later.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/calculation-parameters-4.png',
       alt: 'Prompt to enable the calculation field and run a refresh task',
       caption: 'Enable the field now or configure the refresh task manually later.',
     },
   ]} />

</Steps>

Once saved, the calculation field appears in the **Calculation** folder in the business entities panel.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/calculations/save-to-thingsboard/calculation-create-4.png',
    alt: 'Business entities panel showing the saved calculation field in the Calculation folder',
    caption: 'The saved calculation appears in the Calculation folder.',
  },
  {
    src: '/src/assets/images/trendz/calculations/save-to-thingsboard/calculation-create-5.png',
    alt: 'Calculation folder expanded in the business entities panel',
    caption: 'Expand the Calculation folder to see all saved fields.',
  },
]} />

## Reprocess Task

The Reprocess Task runs **once** to compute and store historical data in ThingsBoard for the selected time range and entities.

<Steps>

1. Open the **Tasks** tab and click **Run Reprocess Task**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/reprocess-task-1.png',
       alt: 'Tasks tab with the Run Reprocess Task button',
       caption: 'Open the Tasks tab and click Run Reprocess Task.',
     },
   ]} />

2. Select the **time range** to reprocess.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/reprocess-task-2.png',
       alt: 'Time range selector for the reprocess task',
       caption: 'Choose the time range for historical backfill.',
     },
   ]} />

3. Choose the **target entities** — you can apply the calculation to all entities with the same entity profile.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/reprocess-task-3.png',
       alt: 'Entity profile selector for the reprocess task',
       caption: 'Select individual entities or all entities with the same profile.',
     },
   ]} />

4. Click **Run**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/reprocess-task-4.png',
       alt: 'Run button for the reprocess task',
       caption: 'Click Run to start the historical backfill job.',
     },
   ]} />

</Steps>

After the job completes, the telemetry appears in ThingsBoard under the key `_ECD_{KEY}`.

<Aside type="note">
Reprocessing overwrites any previously stored values in the selected time range. Run it after modifying a formula to apply the updated logic to all historical data.
</Aside>

## Refresh Task

The Refresh Task runs **on a recurring schedule** to keep ThingsBoard telemetry up to date.

<Steps>

1. Click the **Jobs** button in the calculation field editor.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/refresh-task-1.png',
       alt: 'Jobs button in the calculation field editor',
       caption: 'Click Jobs to open the refresh task configuration.',
     },
   ]} />

2. Enable **calculation result saving**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/refresh-task-2.png',
       alt: 'Toggle to enable calculation result saving',
       caption: 'Enable saving to activate telemetry writes to ThingsBoard.',
     },
   ]} />

3. Set the **start date** — the earliest point from which data will be saved.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/refresh-task-3.png',
       alt: 'Start date picker for the refresh task',
       caption: 'Set the start date for telemetry writes.',
     },
   ]} />

4. Define the **frequency** — how often the job runs. The minimum interval is **1 minute** (not recommended due to load); on Trendz Cloud the minimum is **1 hour**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/refresh-task-4.png',
       alt: 'Frequency selector for the refresh task',
       caption: 'Choose how often Trendz refreshes the telemetry in ThingsBoard.',
     },
   ]} />

5. Choose **target entities** — select individual entities or all entities with the same profile for automatic updates.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/refresh-task-5.png',
       alt: 'Entity profile selector for the refresh task',
       caption: 'Select the entities to update on each refresh cycle.',
     },
   ]} />

6. Click **Save**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/refresh-task-6.png',
       alt: 'Save button for the refresh task configuration',
       caption: 'Save the refresh task configuration.',
     },
   ]} />

</Steps>

Once saved, the calculation shows an **Enabled** status. Telemetry will update automatically at the specified frequency for the selected entities.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/calculations/save-to-thingsboard/refresh-task-7.png',
    alt: 'Calculation field showing the Enabled status after the refresh task is configured',
    caption: 'The Enabled status confirms the refresh task is active.',
  },
]} />

## Using saved telemetry in ThingsBoard

Once the refresh and reprocess jobs are running, the calculated telemetry is continuously updated in ThingsBoard and available everywhere telemetry is supported.

### Alarm rules

You can create alarm rules based on the calculated telemetry key (`_ECD_{KEY}`) directly in the entity profile.

**Example:** A calculation produces temperature deviation values for each sensor relative to the building average. When the deviation exceeds 10°, an alarm is triggered. When it falls back below the threshold, the alarm is cleared.

<Steps>

1. Open the **entity profile** where the calculation is created.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/use-alarms-1.png',
       alt: 'ThingsBoard entity profile page',
       caption: 'Open the entity profile that receives the calculated telemetry.',
     },
   ]} />

2. Navigate to **Alarm Rules** and toggle **Edit mode**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/use-alarms-2.png',
       alt: 'Alarm Rules tab with Edit mode toggled on',
       caption: 'Switch to Edit mode to add or modify alarm rules.',
     },
   ]} />

3. Click **Add alarm rule** and configure the create and clear conditions using the `_ECD_` telemetry key.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/use-alarms-3.png',
       alt: 'Add alarm rule dialog with create and clear conditions',
       caption: 'Set the create and clear conditions for the alarm rule.',
     },
   ]} />

4. Click **Apply changes**.

   <ImageGallery images={[
     {
       src: '/src/assets/images/trendz/calculations/save-to-thingsboard/use-alarms-4.png',
       alt: 'Apply changes button after configuring alarm rules',
       caption: 'Apply the alarm rule configuration.',
     },
   ]} />

</Steps>

Once saved, the alarm rules are active.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/calculations/save-to-thingsboard/use-alarms-5.png',
    alt: 'Alarm Rules tab showing the configured alarm rules',
    caption: 'The configured alarm rules are now active.',
  },
  {
    src: '/src/assets/images/trendz/calculations/save-to-thingsboard/use-alarms-6.png',
    alt: 'ThingsBoard alarms list showing an alarm triggered by the calculated telemetry',
    caption: 'An alarm triggered when the calculated temperature deviation exceeded the threshold.',
  },
]} />

### Visualizations

Visualize the metric in any Trendz widget — line chart, heatmap, state view, and others. See <DocLink product={props.product} path="visualizations-overview">Trendz Visualizations</DocLink> for the full list of chart types.

To embed a Trendz view into a ThingsBoard dashboard, see <DocLink product={props.product} path="embed-visuals">Embed Trendz Views</DocLink>.

### Predictions

Use Trendz <DocLink product={props.product} path="prediction">Prediction Models</DocLink> to forecast future values of the saved metric and visualize them alongside the historical data.

### Anomaly detection

Detect anomalies or outages in the saved metric using <DocLink product={props.product} path="anomaly/overview">Trendz Anomaly Models</DocLink>. When an anomaly is detected, you can automatically trigger an alarm in ThingsBoard.
