import ImageGallery from '~/components/ImageGallery.astro';
import YouTubeVideo from '~/components/YouTubeVideo.astro';

The Scatter Plot reveals correlations and dependencies between two or more numeric properties. Each data point is a single measurement plotted by its field values on the X and Y axes, making clusters, outliers, and trends immediately visible.

## Video tutorial

<YouTubeVideo videoId="wX4ro6FfyaE" title="Trendz Scatter Plot tutorial" />

## Compare two dimensions

Drop one numeric field onto **X-axis** and another onto **Y-axis**. Each measurement becomes a dot at the intersection of its two values.

```plaintext
  Y-axis: heat_consumption
  X-axis: energy_consumption

  heat │                    ●
       │          ●    ●
       │     ●  ●
       │   ●
       │ ●
       └────────────────────── energy
         low              high

  Trend: higher energy → higher heat (positive correlation)
```

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/simple-scatter.png',
    alt: 'Scatter plot comparing two telemetry dimensions',
    caption: 'Two-dimensional scatter plot: each dot is one measurement.',
  },
]} />

## Compare many dimensions

When you need to compare more than two dimensions at once, add grouping fields to the **Series** section. Each unique combination of group values gets a distinct color, making it possible to spot patterns across multiple segments in a single chart.

**Example:** Compare heat and energy consumption across buildings and floors — each floor of each building is a separate color group.

```plaintext
  Y-axis:  heat_consumption
  X-axis:  energy_consumption
  Series:  building name + floor

  heat │  ● Retroville F1    ○ Retroville F3
       │     ● Astarta F1
       │  ○        ○
       │     ●  ●     ○
       │  ●
       └────────────────────────── energy

  ● = Retroville F1/F3 (clustered low-right = efficient)
  ● = Astarta F1 (clustered high = less efficient)
```

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/complex-scatter.png',
    alt: 'Scatter plot with multiple series — each floor of each building is a separate color',
    caption: 'Multi-dimension scatter: building + floor as series gives a distinct color per group, revealing which floors are most energy-efficient.',
  },
]} />
