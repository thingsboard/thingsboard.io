import { Steps, Aside } from '@astrojs/starlight/components';
import ImageGallery from '~/components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';

Metrics created in the Trendz Metric Explorer can be published to ThingsBoard as telemetry and used
anywhere ThingsBoard supports telemetry — dashboards, Rule Engine, and Alarm Rules.

```plaintext
  Generated Metric
        │
        │ Save Calculation
        ▼
  Native Calculation Field
        │
        ├── Reprocess Task ──▶ Historical data saved to ThingsBoard
        │
        └── Refresh Task ────▶ Ongoing automatic updates
                                       │
                                       ▼
                          ThingsBoard Telemetry (key: _ECD_*)
                                       │
                       ┌───────────────┼───────────────┐
                       ▼               ▼               ▼
                  Dashboards      Alarm Rules      Rule Engine
```

## Save to ThingsBoard

### Create Calculation Field

From the Generated Metric editor, click **Save Calculation** and enter:

| Parameter | Description |
|---|---|
| **Calculation Name** | Label shown in Trendz for this field. |
| **Calculation Key** | Telemetry key used in ThingsBoard, automatically prefixed with `_ECD_`. |

<Aside type="note">
The `_ECD_` prefix is added automatically by Trendz and cannot be changed.
</Aside>

**Example:** saving key `energy_consumption_rate` creates ThingsBoard telemetry at
`_ECD_energy_consumption_rate`.

Click **Save** — the field appears in the **Calculation** folder in the Fields Section.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/calculation-create-1.png',
    alt: 'Click Save Calculation',
    caption: 'Click Save Calculation.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/calculation-create-2.png',
    alt: 'Enter Calculation Name and Key',
    caption: 'Enter the Calculation Name and Calculation Key.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/calculation-create-3.png',
    alt: 'Click Save',
    caption: 'Click Save.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/calculation-create-4.png',
    alt: 'Calculation field in the Calculation folder',
    caption: 'The Calculation Field appears in the Calculation folder.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/calculation-create-5.png',
    alt: 'Calculation field visible in fields list',
    caption: 'The Calculation Field is now available in the Fields Section.',
  },
]} />

Before running any jobs, verify the following parameters on the Calculation Field:

| Parameter | Description |
|---|---|
| **Grouping Interval** | How data is bucketed — e.g., `HOUR` saves one point per hour to ThingsBoard. |
| **Aggregation** | How multiple values in the same interval are combined — e.g., `SUM`. |
| **Time** | The lookback window used by the refresh job. |

**Recommended Time settings by job frequency:**

| Time setting | Use when |
|---|---|
| Today | Minute or hourly jobs |
| Last 7 Days | Daily jobs |
| Last 14 Days | Weekly jobs |
| Last 3 Months | Monthly jobs |

Click **Save Field**, then confirm with **Save** to apply the parameters.

<Aside type="note">
You will be prompted to enable the refresh task immediately after saving. You can do this now or
configure it later as described in the [Run Refresh Task](#run-refresh-task) section below.
</Aside>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/calculation-parameters-1.png',
    alt: 'Verify Grouping Interval, Aggregation and Time',
    caption: 'Verify the Grouping Interval, Aggregation, and Time parameters before saving.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/calculation-parameters-2.png',
    alt: 'Click Save Field',
    caption: 'Click Save Field to apply the parameters.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/calculation-parameters-3.png',
    alt: 'Confirm save',
    caption: 'Confirm by clicking Save.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/calculation-parameters-4.png',
    alt: 'Prompt to enable the refresh task',
    caption: 'You are prompted to enable the calculation field (run refresh task) — do it now or later.',
  },
]} />

### Run Reprocess Task

A **Reprocess Task** calculates the metric over a historical time range and saves the results to
ThingsBoard. Run this once after creating the Calculation Field to populate historical data.

<Steps>

1. Open the **Tasks** tab.

2. Click **Run Reprocess Task**.

3. Select the **time range** to reprocess.

4. Choose the **items** — you can apply the job to the current item, all items, or a subset of items
   with the same entity profile.

5. Click **Run**.

</Steps>

After the task completes, ThingsBoard will contain telemetry points for the selected period.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/reprocess-task-1.png',
    alt: 'Open Tasks tab and click Run Reprocess Task',
    caption: 'Open the Tasks tab and click Run Reprocess Task.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/reprocess-task-2.png',
    alt: 'Select time range',
    caption: 'Select the time range for reprocessing.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/reprocess-task-3.png',
    alt: 'Choose items',
    caption: 'Choose which items to include — current item, all items, or a subset.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/reprocess-task-4.png',
    alt: 'Click Run',
    caption: 'Click Run to start the task.',
  },
]} />

### Run Refresh Task

A **Refresh Task** keeps telemetry up to date by automatically recalculating the metric on a schedule.

<Steps>

1. Click the **Jobs** button.

2. Enable **calculation result saving**.

3. Set the **start date** — the date from which new data will be saved.

4. Define the **frequency** — how often the job runs.

5. Choose the **target items** — entities with the same entity profile to update automatically.

6. Click **Save**.

</Steps>

Once saved, the **Enabled** status appears next to the calculation name. Telemetry will now update
automatically at the configured frequency for the selected items.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/refresh-task-1.png',
    alt: 'Click the Jobs button',
    caption: 'Click the Jobs button.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/refresh-task-2.png',
    alt: 'Enable calculation result saving',
    caption: 'Enable calculation result saving.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/refresh-task-3.png',
    alt: 'Set the start date',
    caption: 'Set the start date from which data will be saved.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/refresh-task-4.png',
    alt: 'Define the frequency',
    caption: 'Define how often the job runs.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/refresh-task-5.png',
    alt: 'Choose target items',
    caption: 'Choose the target items for automatic updates.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/refresh-task-6.png',
    alt: 'Click Save',
    caption: 'Click Save.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/refresh-task-7.png',
    alt: 'Enabled status shown',
    caption: 'The Enabled status appears next to the calculation name.',
  },
]} />

## Use in ThingsBoard

Once the Calculation Field is configured with a refresh job and historical data is loaded via a
reprocess job, the metric telemetry is continuously updated in ThingsBoard. You can use it anywhere
ThingsBoard supports telemetry — including dashboards, Alarm Rules, and the Rule Engine. The metric
telemetry key (e.g., `_ECD_energy_consumption_rate`) is used in Rule Engine chains exactly like any
other device telemetry key.

### Alarm Rules

Create ThingsBoard alarms triggered by the metric telemetry:

<Steps>

1. Open the **entity profile** where the calculation is applied.

2. Navigate to **Alarm Rules** and enable **Edit mode**.

3. Click **Add alarm rule** and configure the create and clear conditions.

4. Click **Apply changes**.

</Steps>

**Example:** A metric calculates the absolute temperature deviation from the building average. An alarm
rule triggers a **Warning** alarm of type *Abnormal Temperature* when the deviation exceeds 10, and
clears it when it drops back below. This can detect device overheating, an open window, or a broken
refrigerator — without writing any Rule Engine logic.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/use-alarms-1.png',
    alt: 'Open the entity profile',
    caption: 'Open the entity profile where the calculation is applied.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/use-alarms-2.png',
    alt: 'Navigate to Alarm Rules and enable Edit mode',
    caption: 'Navigate to Alarm Rules and toggle Edit mode.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/use-alarms-3.png',
    alt: 'Add alarm rule',
    caption: 'Click Add alarm rule and configure create and clear conditions.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/use-alarms-4.png',
    alt: 'Click Apply changes',
    caption: 'Click Apply changes.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/use-alarms-5.png',
    alt: 'Alarm rules configured',
    caption: 'Alarm rules are now active.',
  },
  {
    src: '/src/assets/images/trendz/metric/use-in-thingsboard/use-alarms-6.png',
    alt: 'Alarm triggered by the rule',
    caption: 'An alarm is triggered when the metric value meets the alarm condition.',
  },
]} />

### Trendz Visualizations

Visualize the metric using any Trendz widget — line chart, heatmap, state chart (from the Range
Analysis tab), and more. Completed views can be embedded into ThingsBoard dashboards.

See <DocLink product={props.product} path="visualizations-overview">Trendz Visualizations</DocLink> and
<DocLink product={props.product} path="embed-visuals">Embed Visuals in ThingsBoard</DocLink> for details.

### Predict Future Values

Use Trendz **Prediction Models** to forecast future values of the metric and embed the prediction
view into a ThingsBoard dashboard.

See <DocLink product={props.product} path="prediction">Prediction Models</DocLink> for details.

### Detect Anomalies

Use Trendz **Anomaly Models** to detect anomalies or outages in the metric. When an anomaly is
detected, automatic alarm creation can be configured.

See <DocLink product={props.product} path="anomaly/overview">Anomaly Detection</DocLink> for details.
