import { Steps, Aside } from '@astrojs/starlight/components';
import ImageGallery from '~/components/ImageGallery.astro';
import YouTubeVideo from '~/components/YouTubeVideo.astro';

The Line Chart view in Trendz plots numeric telemetry over time. It shares its axis and series model with the Bar Chart — the same three sections control what appears on each axis and how data is grouped.

## Video tutorial

<YouTubeVideo videoId="v2pZKQhiw8s" title="Trendz Line Chart tutorial" />

## Chart sections

Line and Bar charts use three configurable drop zones:

| Section | Purpose |
|---------|---------|
| **X-axis** | Horizontal axis — usually a Date field, but can be any field or combination of fields |
| **Y-axis** | Vertical axis — drop one or more numeric telemetry fields here with the required aggregation |
| **Series** | Split data into groups — each group becomes a separate line |

## Simple line chart

Drop a Date field onto **X-axis** and a numeric telemetry field onto **Y-axis**. Leave **Series** empty. Trendz plots one line for each entity that has data in the selected time range.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/simple-line.png',
    alt: 'Simple line chart with date on X-axis and telemetry on Y-axis',
    caption: 'Simple line chart: date on X-axis, one telemetry field on Y-axis.',
  },
]} />

## Line chart with series

### Multiple telemetry fields as separate lines

To plot several telemetry fields for the same device or asset on a single chart, add each field to **Y-axis** and leave **Series** empty. Use **Filter** to focus on a specific device.

```plaintext
  Y-axis: [ temperature ] [ humidity ] [ pressure ]
  Series: (empty)

  ──────────────────────────────────────────────▶ time
    temperature  ────────────────────
    humidity     ·················
    pressure     ─ ─ ─ ─ ─ ─ ─ ─ ─
```

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/line-multi-telemetry.png',
    alt: 'Line chart with multiple telemetry fields each plotted as a separate line',
    caption: 'Multiple telemetry fields on Y-axis — each becomes a separate line.',
  },
]} />

### Groups as series

To compare the same telemetry across different devices, assets, or categories, add a grouping field (such as a device name or asset attribute) to the **Series** section. Trendz draws one line per group.

```plaintext
  Y-axis:  [ energy_consumption ]
  Series:  [ building name ]

  ──────────────────────────────────────────────▶ time
    Building A   ────────────────────
    Building B   ·················
    Building C   ─ ─ ─ ─ ─ ─ ─ ─ ─
```

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/basic-line.png',
    alt: 'Line chart with building names as series — one line per building',
    caption: 'Building name in Series: one line per building.',
  },
  {
    src: '/src/assets/images/trendz/multiple-bar.png',
    alt: 'Combined field example — energy consumption by quarter split by day of week',
    caption: 'Multiple fields combined: energy consumption by quarter, split by day of week.',
  },
]} />

## Color zones

Color zones mark threshold regions on the chart background to make it immediately visible when a value crosses a limit. Thresholds are static values set at design time.

<Steps>

1. Open the **Settings** card for the view.

2. Go to the **Annotation** section.

3. Choose a value and a color, then click **OK**.

4. Click **Build Report**.

</Steps>

You can add multiple color zones to a single chart.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/line-background-example.png',
    alt: 'Line chart with color zone background annotations marking thresholds',
    caption: 'Color zones highlight threshold regions on the chart background.',
  },
  {
    src: '/src/assets/images/trendz/line-background-config.png',
    alt: 'Color zone configuration panel in the Annotation settings',
    caption: 'Configure color zones in Settings → Annotation.',
  },
]} />

## Mixed charts

A mixed chart overlays columns, lines, and areas on a shared X-axis, with a separate Y-axis scale for each component. This is useful when combining fields with different units or magnitudes.

<Aside type="note">
Mixed chart types (Area, Line, Bar per field) are only available when the **Series** section is empty.
</Aside>

<Steps>

1. Open the settings card for the telemetry field you want to change.

2. Choose the chart type: **Area**, **Line**, or **Bar**.

</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/line-mixed.png',
    alt: 'Mixed chart combining bar columns and a line overlay',
    caption: 'Mixed chart: bar and line combined on a shared X-axis.',
  },
  {
    src: '/src/assets/images/trendz/line-mixed-config.png',
    alt: 'Per-field chart type selector showing Area, Line, and Bar options',
    caption: 'Choose the chart type per field in its settings card.',
  },
]} />

## Y-axis control

By default each field on the **Y-axis** gets its own axis — three fields means three Y-axes on the chart, with the additional axes placed on the right side. When all fields share the same unit, you can merge them onto a single axis.

<Steps>

1. Open **View Settings**.

2. Enable **Single axis**.

</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/line-single-axis.png',
    alt: 'Line chart with all Y-axis fields merged onto a single axis',
    caption: 'Single axis mode: all fields share one Y-axis.',
  },
]} />
