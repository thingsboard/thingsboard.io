import ImageGallery from '@components/ImageGallery.astro';
import { Steps, Aside } from '@astrojs/starlight/components';

Energy consumption is one of the largest operating expenses for commercial buildings, yet most facilities lack accurate,
real-time visibility into where energy is used and what it costs.
This guide shows how to build an analytic dashboard that breaks down energy usage by source and by area,
enables year-over-year comparisons, and tracks carbon emissions — all with interactive filtering.

**Task definition:** Create an analytic dashboard to analyze energy consumption sources in a building and identify areas for improvement.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/building_energy/energy_consumption_analytic_dashboard.png',
    alt: 'Dashboard for energy usage analysis of the building',
    caption: 'Dashboard for energy usage analysis of the building.',
  },
]} />

### Implementation plan

- Table: energy consumption broken down by source (HVAC, lighting, plug loads, elevators, etc.)
- Table: energy consumption broken down by area (offices, meeting rooms, kitchen, storage, etc.)
- Bar chart: compare energy usage across sources
- Bar chart: compare current and previous year with monthly breakdown
- Card: total CO₂ emissions with comparison to the previous period and a 6-month sparkline
- ThingsBoard dashboard combining all views with building-level filtering

## Prerequisites

Energy meters are installed in the building and connected to ThingsBoard via MQTT.
Each meter has two attributes:
- `sourceType` — energy consumption source (HVAC, lighting, plug loads, elevators, etc.)
- `area` — installation area (office, meeting room, hall, kitchen, etc.)

Meters have relations to a Building asset, and buildings have relations to a Customer.
This hierarchy enables aggregation at the device, area, building, or customer level.

## Implementation

### Step 1: Table — energy consumption by source

<Steps>
1. Create a **Table** view in Trendz.
2. Add `energyMeter.sourceType` to the **Columns** section — this splits readings by source type.
3. Add `energyMeter.usageKWH` to the **Columns** section with aggregation `SUM`.
4. Add a calculated field with label **Price**, unit `$`, decimals `2`:

   ```javascript
   var price = uniq(building.energyPrice);
   var totalUsage = sum(energyMeter.usageKWH);

   return totalUsage * price;
   ```

5. Add a calculated field with label **CO2 emissions**, unit `kg CO2e`, decimals `1`:

   ```javascript
   var emissionConversionFactor = 0.21233;
   var totalUsage = sum(energyMeter.usageKWH);

   return totalUsage * emissionConversionFactor;
   ```

6. Add `energyMeter.sourceType`, `energyMeter.area`, and `building` to the **Filters** section.
7. Open view settings → **General** → **Sort order**:
   - Enable descending sorting
   - Sort column: `usageKWH`
8. Set the default time range to **This month**.
9. Save the view as **Energy consumption by source**.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/building_energy/energy_sources_final_table.png',
    alt: 'Energy consumption by source table',
    caption: 'Energy consumption by source table in the building.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/energy_sources_configuration.png',
    alt: 'Add required fields to the table configuration',
    caption: 'Add required fields to the table configuration.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/energy_sources_calculate_cost.png',
    alt: 'Compute total electricity cost for each energy source',
    caption: 'Compute total electricity cost for each energy source.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/energy_sources_filtering.png',
    alt: 'Add filtering options to enable drill-down analytics',
    caption: 'Add filtering options to enable drill-down analytics.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/energy_sources_default_sort.png',
    alt: 'Sort by total energy cost in descending order',
    caption: 'Sort by total energy cost in descending order.',
  },
]} />

### Step 2: Table — energy consumption by area

Repeat Step 1, but replace `energyMeter.sourceType` with `energyMeter.area` in the Columns section.
Save the view as **Energy consumption by area**.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/building_energy/energy_areas_final_table.png',
    alt: 'Table with total electricity consumption by area',
    caption: 'Table with total electricity consumption in different areas of the building.',
  },
]} />

### Step 3: Bar chart — compare usage by source type

Tables give exact numbers; a horizontal bar chart makes it faster to spot the top consumers at a glance.

<Steps>
1. Create a **Bar chart** in Trendz.
2. Add `energyMeter.sourceType` to the **X-axis** section.
3. Add `energyMeter.usageKWH` to the **Y-axis** section.
4. Add `building` to the **Filters** section.
5. Open view settings → **General** → enable **Horizontal Bar**.
6. Configure default sorting: descending by `usageKWH`.
7. Set the default time range to **This month**.
8. Save the view as **Bar: energy use compare by sources**.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/building_energy/energy_compare_sources_bar_configuration.png',
    alt: 'Comparison bar chart configuration',
    caption: 'Comparison bar chart configuration.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/energy_compare_sources_bar_chart.png',
    alt: 'Compare energy consumers on bar chart',
    caption: 'Compare energy consumers on bar chart.',
  },
]} />

### Step 4: Bar chart — year-over-year comparison with monthly breakdown

<Steps>
1. Create a **Bar chart** in Trendz.
2. Add a **Date** field with type `MONTH` to the **X-axis** section.
3. Add `energyMeter.usageKWH` to the **Y-axis** section.
4. Add a **Date** field with type `YEAR` to the **Series** section — this splits bars by year.
5. Add `energyMeter.sourceType`, `energyMeter.area`, and `building` to the **Filters** section.
6. Set the default time range to **Last 3 years**.
7. Save the view as **Bar: compare year-to-year energy usage**.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/building_energy/energy_consumption_compare_time_ranges_configuration.png',
    alt: 'Add Month and Year date fields to compare energy consumption across time ranges',
    caption: 'Add Month and Year date fields to compare energy consumption across time ranges.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/energy_consumption_compare_time_ranges_filters.png',
    alt: 'Add filtering options to focus on specific areas',
    caption: 'Add filtering options to focus on specific areas.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/energy_consumption_compare_time_ranges_bar.png',
    alt: 'Compare electricity consumption across time ranges on bar chart',
    caption: 'Compare electricity consumption across time ranges on bar chart.',
  },
]} />

### Step 5: Card — CO₂ emissions with trend

<Steps>
1. Create a **Card with line chart** view in Trendz.
2. Add a calculated field with label **CO2 emissions**, unit `kg CO2e`, decimals `1`:

   ```javascript
   var emissionConversionFactor = 0.21233;
   var totalUsage = sum(energyMeter.usageKWH);

   return totalUsage * emissionConversionFactor;
   ```

3. Set the default time range to **This month**.
4. Open **General** settings → enable **Enable comparison** — adds a percentage delta vs. the previous period.
5. Enable **Reverse comparison colors** — green when emissions are lower, red when higher.
6. Set the card title to **CO2 emissions**.
7. Save the view as **Card: CO2 emissions insights**.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/building_energy/co2_emission_transform_formula_for_energy.png',
    alt: 'Add calculated field to compute carbon emissions',
    caption: 'Add calculated field to compute carbon emissions based on energy consumption.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/co2_emission_compare_with_prev_interval.png',
    alt: 'Enable comparison with previous interval to see trend',
    caption: 'Enable comparison with previous interval to see trend.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/co2_emission_card_with_trend_and_dynamic.png',
    alt: 'CO2 emissions card with trend and dynamic over last 7 days',
    caption: 'CO2 emissions card with trend and dynamic over last 7 days.',
  },
]} />

### Step 6: Create the energy consumption dashboard in ThingsBoard

Combine all Trendz views into one interactive dashboard that any customer can use to explore their buildings.

<Steps>
1. In ThingsBoard, create a dashboard named **Energy consumption report**.
2. In Trendz, for each view created in Steps 1–5:
   - Click **Share to ThingsBoard** → copy **Add on Dashboard**.
   - Select the **Energy consumption report** dashboard.
   - Enable **Create alias** and choose **Building** as the filter entity.
3. Back in ThingsBoard, open the dashboard and tune the layout.
4. Create the alias **All buildings**:
   - Filter type: **Asset type**
   - Asset type: **Building**
5. Create the alias **Selected entity**:
   - Filter type: **Entity from dashboard state**
   - State entity parameter name: `selectedEntity`
6. Add a **Cards → Entities table** widget and set its datasource alias to **All buildings**.
   Add the **Name** key as a column.
7. Add an **On row click** action to the entities table:
   - Action type: **Update current dashboard state**
   - State entity parameter name: `selectedEntity`
8. For all Trendz widgets, set the datasource alias to **selectedEntity**.
9. Save the dashboard.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/building_energy/building_electricity_dashboard_add_new.png',
    alt: 'Create new dashboard in ThingsBoard',
    caption: 'Create new dashboard in ThingsBoard.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/building_electricity_dashboard_config.png',
    alt: 'Set dashboard name to Energy consumption report',
    caption: 'Set dashboard name to Energy consumption report.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/building_electricity_dashboard_add_aliases.png',
    alt: 'Configure required dashboard aliases',
    caption: 'Configure required dashboard aliases.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/building_electricity_dashboard_all_buildings_alias.png',
    alt: 'Use All Asset by type alias to display all buildings',
    caption: 'Use All Asset by type alias to display all buildings.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/building_electricity_dashboard_add_table.png',
    alt: 'Add table showing all buildings in the system',
    caption: 'Add table showing all buildings in the system.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/building_electricity_dashboard_configure_table.png',
    alt: 'Connect table with buildings alias',
    caption: 'Connect table with buildings alias.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/building_electricity_dashboard_row_click_for_building.png',
    alt: 'Add row click event in buildings table',
    caption: 'Add row click event in buildings table.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/building_electricity_dashboard_rowclick_configuration.png',
    alt: 'Row click event updates filters in all widgets',
    caption: 'Row click event updates filters in all widgets on the dashboard.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/building_electricity_dashboard_add_trendz_views.png',
    alt: 'Add all Trendz views to the new dashboard',
    caption: 'Add all Trendz views to the new dashboard.',
  },
  {
    src: '/src/assets/images/trendz/guide/building_energy/building_electricity_dashboard_add_trendz_confg.png',
    alt: 'Configure Trendz views to enable filtering by building',
    caption: 'Configure Trendz views to enable filtering by building.',
  },
]} />

## Summary

The dashboard created in this guide gives facility managers a complete view of energy usage:
which sources consume the most, which areas are the biggest contributors,
how this year compares to last year by month, and what the building's carbon footprint looks like.
Interactive building filters make it easy to drill down from a portfolio view into a single site.
