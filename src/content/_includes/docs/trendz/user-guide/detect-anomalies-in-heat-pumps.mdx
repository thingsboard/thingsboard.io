import ImageGallery from '@components/ImageGallery.astro';
import { Steps, Aside } from '@astrojs/starlight/components';

Detecting anomalies in heat pumps is essential to maintain their performance and avoid expensive repairs.
Identifying unusual patterns in power usage, compressor speed, airflow, coolant pressure, and temperature lets you catch issues before they escalate into costly failures.
Common problems include low coolant levels, blocked air filters, faulty compressors, and overheating.

**Task definition:** Detect anomalies in heat pumps in real time and notify the maintenance team about potential problems.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_anomaly_detection_dashboard.png',
    alt: 'Dashboard for investigating discovered anomalies in heat pump operation',
    caption: 'Dashboard for investigating discovered anomalies in heat pump operation.',
  },
]} />

### Implementation plan

- Create an anomaly detection model for heat pumps using historical telemetry data.
- Configure automated anomaly discovery on real-time telemetry data.
- Create a table widget to review all detected anomalies.
- Save the anomaly score as device telemetry so ThingsBoard alarms can use it.
- Notify the maintenance team when an anomaly is detected.

## Prerequisites

Heat pumps report telemetry to ThingsBoard via MQTT and are registered as device entities.
Devices have a relation to an Apartment asset, and apartments are assigned to customer accounts.
For this guide, each heat pump reports the following telemetry keys: `compressorSpeed`, `airflow`, `coolantPressure`, `coolantTemperature`, `powerUsageWh`.

## Implementation

### Step 1: Create an anomaly detection model

Trendz uses unsupervised machine learning to detect anomalies in time series data.
Train the model on historical data to establish what normal heat pump behavior looks like.

<Steps>
1. Go to the **Anomalies** page and click **Create model**.
2. Set the model name to `HeatPumpAnomalyModel`.
3. Configure the model properties:
   - Cluster algorithm: **K-Means**
   - Segment time range: **1 hour** — detect abnormal behavior within a 1-hour window
   - Comparison type: **Behavior based** — detect anomalies based on overall behavioral patterns
4. Configure the datasource:
   - Time range: **Last 90 days**
   - Fields: `heatPump.compressorSpeed`, `heatPump.airflow`, `heatPump.coolantPressure`, `heatPump.coolantTemperature`, `heatPump.powerUsageWh`
   - Filters: leave blank to include all heat pumps
5. Click **Build model**.
</Steps>

Once the model is trained, Trendz shows historical anomalies it discovered.
Each anomaly has a `score` and a `score index` — higher values mean the behavior is more abnormal.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_create_anomaly_model.png',
    alt: 'Click Create Anomaly Detection Model button',
    caption: 'Click Create Anomaly Detection Model button.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_configure_anomaly_model.png',
    alt: 'Set anomaly detection model parameters',
    caption: 'Set anomaly detection model parameters.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_anomalies_summary.png',
    alt: 'Review results of anomaly detection model training',
    caption: 'Review results of anomaly detection model training.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_review_anomaly.png',
    alt: 'Review discovered anomalies for heat pump',
    caption: 'Review discovered anomalies for heat pump.',
  },
]} />

### Step 2: Schedule anomaly autodiscovery

Configure a background job so Trendz continuously analyzes fresh telemetry and detects new anomalies.

<Steps>
1. On the model summary page, click **Auto discovery**.
2. Enable the **Auto discovery** checkbox.
3. Set **Interval** to `1 hour`.
4. Click **Apply**.
</Steps>

Trendz will now periodically fetch new data from heat pumps, identify anomalies, compute anomaly scores, and save them to the database.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpumps_autodiscover_enable.png',
    alt: 'Click Anomaly Automated Discovery button',
    caption: 'Click Anomaly Automated Discovery button.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpumps_autodiscover_job_configuration.png',
    alt: 'Configure intervals for automated anomaly discovery job',
    caption: 'Configure intervals for automated anomaly discovery job.',
  },
]} />

### Step 3: Review all discovered anomalies

Create a view that shows both historical anomalies (discovered during model training) and new ones detected by the autodiscovery job.

<Steps>
1. Create an **Anomaly** view.
2. Select the model created in Step 1 — `HeatPumpAnomalyModel`.
3. Add the `heatPump` field to the **Filter** section to focus on a specific device.
4. Click **Build report** and review the discovered anomalies.
5. Set the view name to **heat pump anomalies table**.
6. Click **Save**.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_create_anomaly_widget.png',
    alt: 'Create anomaly review widget for heat pumps',
    caption: 'Create anomaly review widget for heat pumps.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_anomaly_select_model.png',
    alt: 'Select anomaly detection model',
    caption: 'Select the anomaly detection model created in Step 1.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_anomaly_review_widget.png',
    alt: 'Review discovered anomalies for heat pumps on a widget',
    caption: 'Review discovered anomalies for heat pumps on a widget.',
  },
]} />

### Step 4: Notify the maintenance team

To trigger ThingsBoard alarms from anomaly data, save the anomaly score as device telemetry, then create an alarm rule that fires when the score exceeds a threshold.

**Save the anomaly score as telemetry**

<Steps>
1. Create a **Table** view in Trendz.
2. Add the `heatPump` field to the **Columns** section.
3. Add the **Date FULL_HOUR** field to the **Columns** section.
4. Add an **Anomaly** field to the **Columns** section:
   - Model: `HeatPumpAnomalyModel`
   - Anomaly field: `score index`
   - Aggregation: `MAX`
   - Label: `heatpumpAnomalyScore` — Trendz saves this as a telemetry key
5. Add the `heatPump` field to the **Filters** section.
6. Open view settings → **Tb calculated telemetry save** section:
   - Enabled: **true**
   - Interval: `1`
   - Unit: `hours`
7. In **View mode fields**, set `heatPump` as the **Row click entity** — this determines under which device entity the telemetry is saved.
8. Set the default time range to **Last 7 days**.
9. Save the view as **Heatpump anomaly score save job**.
</Steps>

Trendz now runs a background job that periodically computes the anomaly score and saves it as telemetry on each heat pump device.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_anomaly_score_table_create.png',
    alt: 'Create anomaly score table for heat pumps',
    caption: 'Create anomaly score table for heat pumps.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_anomaly_score_table_row_click.png',
    alt: 'Select heat pump as row click entity',
    caption: 'Select heat pump as the row click entity.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_anomaly_score_save.png',
    alt: 'Schedule anomaly score saving job for heat pumps',
    caption: 'Schedule anomaly score saving job for heat pumps.',
  },
]} />

**Create an alarm rule for anomaly score > 50**

<Steps>
1. In ThingsBoard, open the heat pump's device profile and add a new **Alarm Rule**.
2. Alarm type: **Abnormal behavior**
3. Create alarm condition:
   - Severity: `Warning`
   - Condition: `heatpumpAnomalyScore` is greater than `50`
4. Clear alarm condition:
   - Condition: `heatpumpAnomalyScore` is lower or equals `50`
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_anomaly_score_alert_create.png',
    alt: 'Create anomaly score alarm in ThingsBoard device profile',
    caption: 'Create anomaly score alarm in ThingsBoard device profile.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_anomaly_score_alert_configuration.png',
    alt: 'Configure alarm create and clear rules',
    caption: 'Configure alarm create and clear rules.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_anomaly_score_alert_creation_condition.png',
    alt: 'Configure anomaly score threshold for alarm creation',
    caption: 'Configure anomaly score threshold for alarm creation.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_anomaly_score_alert_clear_condition.png',
    alt: 'Configure anomaly score threshold for alarm clearing',
    caption: 'Configure anomaly score threshold for alarm clearing.',
  },
]} />

**Send an email notification when the alarm is created**

<Steps>
1. Open the **Root rule chain** in ThingsBoard.
2. Add a `toEmail` rule node after the **Device profile** node, connected via the **Alarm Created** relation.
3. Configure the `toEmail` node:
   - From template: `info@testmail.org`
   - To template: `maintenance@testmail.com`
   - Subject template: `Abnormal behavior in $\{entityName\}`
   - Body template: `Maintenance required for heat pump $\{entityName\}. Anomaly score is $\{heatpumpAnomalyScore\}. Please check the heat pump status on the dashboard.`
4. Add a `send email` rule node after the `toEmail` node, connected via the **Successful** relation.
5. Save the rule chain.
</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_open_root_rulechain.png',
    alt: 'Open root rule chain in ThingsBoard',
    caption: 'Open root rule chain in ThingsBoard.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_send_email_node_configuration.png',
    alt: 'Add toEmail rule node to root rule chain',
    caption: 'Add toEmail rule node to root rule chain.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_email_notification_config.png',
    alt: 'Configure email properties for notification',
    caption: 'Configure email properties for notification.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_add_send_email_node.png',
    alt: 'Add send email node to root rule chain',
    caption: 'Add send email node to root rule chain.',
  },
  {
    src: '/src/assets/images/trendz/guide/heatpump_anomalies/heatpump_anomaly_send_notification_rulechain.png',
    alt: 'Configured rule chain for sending anomaly notifications',
    caption: 'Configured rule chain for sending anomaly notifications about detected anomalies in heat pumps.',
  },
]} />

## Summary

The implementation covers the full lifecycle: train an anomaly detection model on 90 days of historical data,
schedule an hourly autodiscovery job, surface results in a Trendz view, write the anomaly score back to ThingsBoard telemetry,
and raise an alarm that emails the maintenance team when the score exceeds 50.
This proactive approach catches problems such as low coolant levels, blocked filters, or failing compressors early,
extending equipment lifespan and avoiding unplanned repairs.
