import { Steps, Aside } from '@astrojs/starlight/components';
import ImageGallery from '~/components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';
import { Products } from '~/models/site.models';

Trendz can connect to external SQL databases and combine their data with ThingsBoard device and asset data in a single visualization. This reduces load on ThingsBoard, increases report build speed, and enables integration with data that lives outside ThingsBoard — such as ERP exports, maintenance logs, or billing records.

<Aside type="caution">
External datasources are available only on **self-hosted** Trendz installations. ThingsBoard Cloud (PAAS) is not supported.
</Aside>

Trendz treats external SQL tables as **read-only**. It never writes, updates, or deletes rows in the external database.

## Supported databases

| Database    | Minimum version |
|-------------|-----------------|
| PostgreSQL  | 9.4+            |
| MySQL       | 5.0+            |
| MSSQL       | 2016+           |
| Oracle      | 9i+             |

Trendz bundles JDBC drivers for the databases above. Custom or third-party drivers are not supported.

## External table requirements

Trendz can read a table from an external SQL database if it meets these structural requirements:

1. One column contains a UUID that matches a ThingsBoard Device or Asset ID.
2. One column contains a `BIGINT` timestamp in milliseconds.
3. All other columns can be of any type.

```plaintext
  External SQL table
  ┌──────────────┬─────────────────────┬──────────────────────┐
  │ building_id  │ ts                  │ heat_consumption     │
  │  (UUID)      │  (BIGINT, ms)       │  (BIGINT)            │
  ├──────────────┼─────────────────────┼──────────────────────┤
  │ <TB asset ID>│ 1700000000000       │ 4200                 │
  └──────────────┴─────────────────────┴──────────────────────┘
         │
         └──── must match a ThingsBoard Device/Asset ID
```

<Aside type="note">
External data can only be used in a view together with at least one ThingsBoard entity. Trendz uses the entity link to filter external rows to those the current user is permitted to access.
</Aside>

## Manage external SQL datasources

Before referencing external tables in views, register the database connection in Trendz settings. You can register any number of datasources — there is no limit.

### Add a datasource

<Steps>

1. Go to **Settings**, then the **System** tab.

2. Under the **External Data Source** section, click **Add new data source**.

3. Fill in the connection form:
   - **URL** — JDBC connection string
   - **Database Type** — PostgreSQL, MySQL, MSSQL, or Oracle
   - **Login** and **Password**

4. Click **Save**. Trendz tests the connection and shows a success or error message.

</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-settings-add-1.png',
    alt: 'Navigate to Settings > System > External Data Source and click Add new data source',
    caption: 'Go to <b>Settings</b> → <b>System</b> → <b>External Data Source</b>, then click <b>Add new data source</b>.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-settings-add-2.png',
    alt: 'Fill in the database connection form and save',
    caption: 'Fill in the connection details and click <b>Save</b>.',
  },
]} />

### View all datasources

- Go to **Settings** → **System** → **External Data Source**, then click **See all data sources**.

A table lists all registered datasources with their **Data Source URL**, **Database type**, and **Actions**.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-settings-see-1.png',
    alt: 'Click See all data sources',
    caption: 'Click <b>See all data sources</b> to open the list.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-settings-see-2.png',
    alt: 'Table showing all registered external datasources',
    caption: 'All registered datasources with URL, database type, and action menu.',
  },
]} />

### Delete a datasource

- In the datasource list, click the three-dot menu under **Actions** and select **Delete**.
- Confirm the deletion — Trendz shows which business entities will be affected.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-settings-delete-1.png',
    alt: 'Open the datasource list',
    caption: 'Open the datasource list.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-settings-delete-2.png',
    alt: 'Select Delete from the action menu',
    caption: 'Select <b>Delete</b> from the three-dot action menu.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-settings-delete-3.png',
    alt: 'Datasource deleted successfully',
    caption: 'The datasource is removed.',
  },
]} />

### Edit a datasource

- In the datasource list, click the three-dot menu under **Actions** and select **Edit**.
- Update the URL, database type, login, or password.
- Click **Save** and confirm — Trendz shows which entities will be affected and tests the connection.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-settings-edit-1.png',
    alt: 'Select Edit from the action menu',
    caption: 'Select <b>Edit</b> from the three-dot action menu.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-settings-edit-2.png',
    alt: 'Edit connection details and save',
    caption: 'Edit the connection details and click <b>Save</b>. Confirm which entities are affected.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-settings-edit-3.png',
    alt: 'Connection test result after editing',
    caption: 'Trendz tests the updated connection and reports success or an error.',
  },
]} />

## Define an external business entity

To use external SQL tables in views, create a <DocLink product={props.product} path="concepts/business-entities">Business Entity</DocLink> that describes the table structure and links it to ThingsBoard entities. There is no limit on the number of external business entities you can create.

The example below maps the following PostgreSQL table:

```sql
CREATE TABLE IF NOT EXISTS building_consumption (
    id UUID PRIMARY KEY,
    building_id UUID NOT NULL,
    heat_consumption BIGINT NOT NULL,
    energy_consumption BIGINT NOT NULL,
    ts BIGINT NOT NULL
);
```

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-db-data.png',
    alt: 'Sample data from the building_consumption table',
    caption: 'Demo data from the <code>building_consumption</code> table.',
  },
]} />

### Step 1 — Add an external business entity

<Steps>

1. Go to the **Business Entities** section and click **Add Business Entity**.

2. Fill in the form:
   - **Name** — any display name (e.g. *Building Consumption Data*)
   - **Entity Type** — set to *External*
   - **Table Name** — the exact table name (e.g. `building_consumption`; case-sensitive)
   - **Datasource** — select the datasource registered in the previous section

3. Click **Save**.

</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-1-1.png',
    alt: 'Go to Business Entities and click Add Business Entity',
    caption: 'Go to <b>Business Entities</b> and click <b>Add Business Entity</b>.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-1-2.png',
    alt: 'Fill in name, table name, entity type External, and select the datasource',
    caption: 'Enter the name, table name, set entity type to <b>External</b>, and choose the datasource.',
  },
]} />

### Step 2 — Add the key entity field

This field tells Trendz which column holds the ThingsBoard Device/Asset ID.

<Steps>

1. Go to the **Fields** tab and click **Add field**.

2. Fill in the form:
   - **Name** — display name (e.g. *Building ID*)
   - **Field Type** — *String*
   - **Query Type** — *Attribute*
   - **Key** — the exact column name containing the ThingsBoard entity ID (e.g. `building_id`; case-sensitive)

3. Enable **Use Field as Business Entity Key**.

</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-2-1.png',
    alt: 'Go to Fields tab and click Add field',
    caption: 'Go to the <b>Fields</b> tab and click <b>Add field</b>.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-2-2.png',
    alt: 'Configure the key entity field and enable Use Field as Business Entity Key',
    caption: 'Set the field type, query type, key column name, and enable <b>Use Field as Business Entity Key</b>.',
  },
]} />

### Step 3 — Add the timestamp field

This field tells Trendz which column contains the row timestamp.

<Steps>

1. Click **Add field**.

2. Fill in the form:
   - **Name** — display name (e.g. *Timestamp*)
   - **Field Type** — *Numeric*
   - **Query Type** — *Telemetry*
   - **Key** — the exact column name containing the BIGINT millisecond timestamp (e.g. `ts`; case-sensitive)

3. Enable **Use Field as Telemetry Timestamp**.

</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-3-1.png',
    alt: 'Click Add field to add the timestamp field',
    caption: 'Click <b>Add field</b> to add the timestamp field.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-3-2.png',
    alt: 'Configure the timestamp field and enable Use Field as Telemetry Timestamp',
    caption: 'Set the field type to <b>Numeric</b>, query type to <b>Telemetry</b>, and enable <b>Use Field as Telemetry Timestamp</b>.',
  },
]} />

### Step 4 — Add telemetry fields

Repeat this step for each data column you want to expose in views.

<Steps>

1. Click **Add field**.

2. Fill in the form:
   - **Name** — display name (e.g. *Heat Consumption* or *Energy Consumption*)
   - **Field Type** — the appropriate type for the column values
   - **Query Type** — *Telemetry*
   - **Key** — the exact column name (e.g. `heat_consumption` or `energy_consumption`; case-sensitive)

3. To remove a field, click the delete button in its top-right corner.

</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-4-1.png',
    alt: 'Click Add field to add a telemetry field',
    caption: 'Click <b>Add field</b>.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-4-2.png',
    alt: 'Configure the heat_consumption telemetry field',
    caption: 'Configure the <b>heat_consumption</b> field.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-4-3.png',
    alt: 'Configure the energy_consumption telemetry field',
    caption: 'Configure the <b>energy_consumption</b> field.',
  },
]} />

### Step 5 — Link to a ThingsBoard business entity

Create a relation that connects the external business entity to a ThingsBoard asset or device profile. Trendz uses this link to filter external rows by ThingsBoard entity access permissions.

<Steps>

1. Go to the **Relations** tab and click **Add relation**.

2. Fill in the form:
   - **Name** — any label (e.g. *External*)
   - **Direction** — *TO*
   - **Selected Business Entity** — the ThingsBoard business entity whose IDs appear in the key column from Step 2 (e.g. *EM building*)
   - **Enabled** — check this box

3. Click **Save**.

</Steps>

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-5-1.png',
    alt: 'Go to Relations tab and click Add relation',
    caption: 'Go to the <b>Relations</b> tab and click <b>Add relation</b>.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-5-2.png',
    alt: 'Configure the relation name, direction, target business entity, and enable it',
    caption: 'Set the relation direction to <b>TO</b>, choose the linked ThingsBoard business entity, and enable it.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-5-3.png',
    alt: 'Save the relation',
    caption: 'Click <b>Save</b>.',
  },
]} />

### Step 6 — Verify with a simple view

Build a minimal table view to confirm the external business entity is wired up correctly.

<Steps>

1. Go to the home page, click **Create View**, and choose the **Table** view type.

2. Drag the ThingsBoard business entity name field (e.g. *EM building*) into the column section.

3. Drag any field from the external business entity (e.g. *energy_consumption*) into the column section.

4. Select a date range that contains data.

5. Click **Build Report**.

</Steps>

If the view builds successfully, the external business entity is set up correctly. If errors appear, review Steps 1–5 for misconfigured column names or missing key/timestamp fields.

<ImageGallery images={[
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-6-1.png',
    alt: 'Create a new Table view',
    caption: 'Click <b>Create View</b> and choose <b>Table</b>.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-6-2.png',
    alt: 'Drag the ThingsBoard entity name field into the column section',
    caption: 'Drag the ThingsBoard business entity name field into the column section.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-6-3.png',
    alt: 'Drag an external field into the column section',
    caption: 'Drag an external business entity field into the column section.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-6-4.png',
    alt: 'Select a date range with data',
    caption: 'Select a date range that contains data.',
  },
  {
    src: '/src/assets/images/trendz/datasource/external-datasources-step-6-5.png',
    alt: 'Click Build Report to verify the setup',
    caption: 'Click <b>Build Report</b>. A successful result confirms the external entity is configured correctly.',
  },
]} />

## Security and data visibility

Trendz enforces ThingsBoard access permissions when querying external SQL tables. External rows are filtered by the Device/Asset IDs linked through the relation defined in Step 5 — a user sees only rows whose ID corresponds to a ThingsBoard entity they have access to.

For example, if an external table stores maintenance records for factory machines, users see only the records for machines assigned to them in ThingsBoard.
