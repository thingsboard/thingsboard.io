import DocLink from '@components/DocLink.astro';
import { Steps, Aside } from '@astrojs/starlight/components';
import ImageGallery from '@components/ImageGallery.astro';
import { Products } from '~/models/site.models';

The mobile app supports OAuth 2.0 authentication. When configured, additional sign-in buttons appear on the login screen so users can authenticate with the configured OAuth 2.0 provider.
A System Administrator configures OAuth 2.0. For general OAuth configuration, see <a href={props.product === Products.MOBILE_PE ? '/docs/pe/user-guide/security/oauth-2-support/' : '/docs/user-guide/security/oauth-2-support/'}>OAuth 2.0 Support</a>.

## Register the mobile app in OAuth settings

<Steps>

1. As a System Administrator, go to **System Settings** → **OAuth2**.
2. Expand the domains panel.
3. Open the **Mobile applications** tab and click **Add application**.
4. Enter your application package name — for Android: the Application ID; for iOS: the Product Bundle Identifier.
5. Copy the auto-generated **Application secret**, or enter your own.
6. Click **Save** in the bottom-right corner to apply the changes.

</Steps>

{props.product === Products.MOBILE_PE ? (
  <ImageGallery images={[
    { src: '/src/assets/images/mobile/oauth2/pe/oauth2-1.png', alt: 'Navigate to System Settings OAuth2', caption: 'Go to System Settings → OAuth2 and expand the domains panel.' },
    { src: '/src/assets/images/mobile/oauth2/pe/oauth2-2.png', alt: 'Mobile applications tab', caption: 'Open the Mobile applications tab and click Add application.' },
    { src: '/src/assets/images/mobile/oauth2/pe/oauth2-3.png', alt: 'Application package and secret fields', caption: 'Enter the application package name and note the Application secret.' },
    { src: '/src/assets/images/mobile/oauth2/pe/oauth2-4.png', alt: 'Save OAuth settings', caption: 'Click Save to apply the changes.' },
  ]} />
) : (
  <ImageGallery images={[
    { src: '/src/assets/images/mobile/oauth2/oauth2-1.png', alt: 'Navigate to System Settings OAuth2', caption: 'Go to System Settings → OAuth2 and expand the domains panel.' },
    { src: '/src/assets/images/mobile/oauth2/oauth2-2.png', alt: 'Mobile applications tab', caption: 'Open the Mobile applications tab and click Add application.' },
    { src: '/src/assets/images/mobile/oauth2/oauth2-3.png', alt: 'Application package and secret fields', caption: 'Enter the application package name and note the Application secret.' },
    { src: '/src/assets/images/mobile/oauth2/oauth2-4.png', alt: 'Save OAuth settings', caption: 'Click Save to apply the changes.' },
  ]} />
)}

## Configure the mobile app

Open the {props.product === Products.MOBILE_PE ? '`flutter_thingsboard_pe_app`' : '`flutter_thingsboard_app`'} project in your editor. Edit `lib/constants/app_constants.dart`:

- Set `thingsboardOAuth2AppSecret` to the **Application secret** value from the previous step.
- Set `thingsboardOAuth2CallbackUrlScheme` to a unique scheme — for example, your application package name with an `auth` suffix (like `org.mycompany.myapp.auth`).

```dart
abstract class ThingsboardAppConstants {
  // ...

  static final thingsboardOAuth2CallbackUrlScheme = 'Your callback url scheme here';

  static final thingsboardOAuth2AppSecret = 'Your app secret here';
}
```

<Aside type="note">
  The domain in `thingsBoardApiEndpoint` must match one of the domains configured in the OAuth settings form.
</Aside>

Next, edit `android/app/src/main/AndroidManifest.xml`. Find `TbWebCallbackActivity` and set its scheme to the same value as `thingsboardOAuth2CallbackUrlScheme`:

```xml
<activity android:name=".TbWebCallbackActivity">
    <intent-filter android:label="tb_web_auth">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="Your callback url scheme here" />
    </intent-filter>
</activity>
```

## Verify OAuth 2.0 setup

Run the mobile app. The login screen should show the OAuth 2.0 sign-in button(s) at the top. Use one to log in as an existing user to confirm the setup works.

<div style="max-width: 25%">
{props.product === Products.MOBILE_PE ? (
  <ImageGallery images={[
    { src: '/src/assets/images/mobile/oauth2/pe/oauth2-5.png', alt: 'OAuth 2.0 sign-in buttons on login screen', caption: 'OAuth 2.0 sign-in buttons appear at the top of the login form.' },
  ]} />
) : (
  <ImageGallery images={[
    { src: '/src/assets/images/mobile/oauth2/oauth2-5.png', alt: 'OAuth 2.0 sign-in buttons on login screen', caption: 'OAuth 2.0 sign-in buttons appear at the top of the login form.' },
  ]} />
)}
</div>
