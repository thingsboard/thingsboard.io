import DocLink from '@components/DocLink.astro';
import { Products } from '~/models/site.models';
import { Aside } from '@astrojs/starlight/components';

export const PEOnly = () => (
  <span style="display:inline-block;background:#3b82f6;color:white;padding:2px 10px;border-radius:999px;font-size:0.75em;font-weight:600;white-space:nowrap">PE / Cloud only</span>
);

<Aside type="note">
  <PEOnly /> All analytics nodes require **ThingsBoard PE** or **ThingsBoard Cloud**.
</Aside>

Compute statistical metrics and aggregations on data streams or groups of entities.

| Node | Description |
|------|-------------|
| <DocLink product={props.product} path="reference/rule-engine/nodes/analytics/aggregate-latest" bold={false}>Aggregate latest</DocLink> | Periodically aggregates attributes or latest telemetry from child entities and writes results to parent entities |
| <DocLink product={props.product} path="reference/rule-engine/nodes/analytics/aggregate-stream" bold={false}>Aggregate stream</DocLink> | Computes MIN, MAX, SUM, AVG, COUNT, or UNIQUE from an incoming data stream over a tumbling time window |
| <DocLink product={props.product} path="reference/rule-engine/nodes/analytics/alarms-count" bold={false}>Alarms count</DocLink> | On each alarm-related message, counts alarms matching configurable status, severity, and type filters; writes the result as telemetry |
