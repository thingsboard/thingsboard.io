import DocLink from '@components/DocLink.astro';
import Tabs from '@components/Tabs.astro';
import TabItem from '@components/TabItem.astro';
import HostCode from '@components/HostCode.astro';
import ShowFor from '@components/ShowFor.astro';
import { Products } from '@models/site.models';
import { Aside } from '@astrojs/starlight/components';

## Publish Client-Side Attributes

Upload client-side attributes for one or more devices.

**Topic:** `v1/gateway/attributes`

```json
{
  "Device A": {"fw_version": "1.0", "battery": 87},
  "Device B": {"fw_version": "2.1", "battery": 92}
}
```

<ShowFor product={props.product} show={[Products.CE, Products.PE]}>

Replace `GATEWAY_DEVICE_ACCESS_TOKEN` in the commands below with the gateway device's access token, and `$THINGSBOARD_HOST` with your ThingsBoard instance's host.

</ShowFor>

<ShowFor product={props.product} show={[Products.PAAS, Products.PAAS_EU]}>

Replace `GATEWAY_DEVICE_ACCESS_TOKEN` in the commands below with the gateway device's access token.

</ShowFor>

<HostCode product={props.product} lang="bash" code={`mosquitto_pub -h "{MQTT_HOST}" -t "v1/gateway/attributes" -u "GATEWAY_DEVICE_ACCESS_TOKEN" -m '{"Device A":{"fw_version":"1.0","battery":87}}'`} />

## Request Attribute Values

Subscribe to the response topic first, then publish the request.

**Subscribe:** `v1/gateway/attributes/response`

**Publish:** `v1/gateway/attributes/request`

**Request:**

| Field | Required | Description |
|-------|----------|-------------|
| `id` | Yes | Integer request identifier — echoed in the response |
| `device` | Yes | Name of a device connected through this gateway |
| `clientKeys` | No | Comma-separated client attribute keys to return; use `""` (empty string) to return all client attributes; omit the field entirely to exclude the client scope |
| `sharedKeys` | No | Comma-separated shared attribute keys to return; use `""` (empty string) to return all shared attributes; omit the field entirely to exclude the shared scope |

Omitting **both** `clientKeys` and `sharedKeys` (sending only `id` and `device`) returns **all** client and **all** shared attributes.

```json
{"id": 1, "device": "Device A", "clientKeys": "fw_version,battery", "sharedKeys": "targetFwVersion"}
```

**Response:**

```json
{"id": 1, "device": "Device A", "client": {"fw_version": "1.0", "battery": 87}, "shared": {"targetFwVersion": "2.0"}}
```

A scope you did not request is omitted from the response.

### Example

The gateway keeps a single MQTT connection open per device, so use <a href="https://mosquitto.org/man/mosquitto_rr-1.html" target="_blank" rel="noopener noreferrer"><code>mosquitto_rr</code></a>: it subscribes, publishes the request, and waits for the response over that one connection, matching the request/response pattern this API expects.

<ShowFor product={props.product} show={[Products.CE, Products.PE]}>

Replace `GATEWAY_DEVICE_ACCESS_TOKEN` in the commands below with the gateway device's access token, and `$THINGSBOARD_HOST` with your ThingsBoard instance's host.

</ShowFor>

<ShowFor product={props.product} show={[Products.PAAS, Products.PAAS_EU]}>

Replace `GATEWAY_DEVICE_ACCESS_TOKEN` in the commands below with the gateway device's access token.

</ShowFor>

<Tabs>
<TabItem label="Specific keys">

<HostCode product={props.product} lang="bash" code={`mosquitto_rr -d -q 1 -h "{MQTT_HOST}" -p 1883 -t "v1/gateway/attributes/request" -e "v1/gateway/attributes/response" -u "GATEWAY_DEVICE_ACCESS_TOKEN" -m '{"id":1,"device":"Device A","clientKeys":"fw_version,battery","sharedKeys":"targetFwVersion"}'`} />

</TabItem>
<TabItem label="All shared attributes">

<HostCode product={props.product} lang="bash" code={`mosquitto_rr -d -q 1 -h "{MQTT_HOST}" -p 1883 -t "v1/gateway/attributes/request" -e "v1/gateway/attributes/response" -u "GATEWAY_DEVICE_ACCESS_TOKEN" -m '{"id":1,"device":"Device A","sharedKeys":""}'`} />

</TabItem>
<TabItem label="All attributes">

<HostCode product={props.product} lang="bash" code={`mosquitto_rr -d -q 1 -h "{MQTT_HOST}" -p 1883 -t "v1/gateway/attributes/request" -e "v1/gateway/attributes/response" -u "GATEWAY_DEVICE_ACCESS_TOKEN" -m '{"id":1,"device":"Device A"}'`} />

</TabItem>
</Tabs>

<Aside type="note">
`Device A` must already exist and have the requested attributes — otherwise the response will not contain any values.
</Aside>

### Deprecated request format

<Aside type="caution">
The `client` (boolean) + `key`/`keys` fields are deprecated and kept only for backward compatibility. Use `clientKeys`/`sharedKeys` above instead.
</Aside>

| Field | Required | Description |
|-------|----------|-------------|
| `id` | Yes | Integer request identifier — echoed in the response |
| `device` | Yes | Name of a device connected through this gateway |
| `client` | Yes | Boolean flag: set to `true` to request client-side attributes, or `false` to request shared attributes |
| `key` | Yes\* | A single attribute key to return |
| `keys` | Yes\* | Array of attribute keys to return |

\* Send either `key` or `keys`, not both — `key` takes precedence when both are present.

```json
{"id": 1, "device": "Device A", "keys": ["fw_version", "battery"], "client": true}
```

<HostCode product={props.product} lang="bash" code={`mosquitto_rr -d -q 1 -h "{MQTT_HOST}" -p 1883 -t "v1/gateway/attributes/request" -e "v1/gateway/attributes/response" -u "GATEWAY_DEVICE_ACCESS_TOKEN" -m '{"id":1,"device":"Device A","keys":["fw_version","battery"],"client":true}'`} />

This format returns a scope-merged response rather than the scope-separated one — `value` for a single key, `values` for several:

```json
{"id": 1, "device": "Device A", "values": {"fw_version": "1.0", "battery": 87}}
```

Its known limitations, all of which the `clientKeys`/`sharedKeys` format resolves:

- `client` is ignored whenever no keys are given — both scopes are returned regardless of whether it is `true` or `false`. The flag only takes effect together with an explicit `key`/`keys`.
- Client-side and shared results are merged into one object, so a key present in both scopes collides and only the shared value survives.
- `client` and `key`/`keys` are both required; a request that omits them is not supported.


## Subscribe to Shared Attribute Updates

Subscribe to receive push notifications when shared attributes change for any downstream device.

**Topic:** `v1/gateway/attributes` (subscribe)

<HostCode product={props.product} lang="bash" code={`mosquitto_sub -h "{MQTT_HOST}" -t "v1/gateway/attributes" -u "$ACCESS_TOKEN"`} />

ThingsBoard delivers updates in this format:

```json
{
  "device": "Device A",
  "data": {"targetFwVersion": "2.0", "enabled": true}
}
```
