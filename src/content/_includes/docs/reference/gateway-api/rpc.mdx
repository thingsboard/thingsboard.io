import DocLink from '@components/DocLink.astro';
import HostCode from '~/components/HostCode.astro';
import { Aside } from '@astrojs/starlight/components';

## Subscribe to Server-Side RPC Commands

Subscribe to the RPC topic to receive commands targeted at downstream devices.

**Topic:** `v1/gateway/rpc` (subscribe + publish)

Replace `$ACCESS_TOKEN` in the commands below with the gateway device's access token.

<HostCode product={props.product} lang="bash" code={`mosquitto_sub -h "{MQTT_HOST}" -t "v1/gateway/rpc" -u "$ACCESS_TOKEN"`} />

Incoming command format:

```json
{
  "device": "Device A",
  "data": {"id": 1, "method": "toggle_gpio", "params": {"pin": 1}}
}
```

| Field | Description |
|-------|-------------|
| `device` | The target device name |
| `data.id` | Request identifier — must be echoed in the reply |
| `data.method` | RPC method name |
| `data.params` | Method parameters (arbitrary JSON) |

## Send RPC Reply

Send the reply to the **same topic**:

```json
{"device": "Device A", "id": 1, "data": {"success": true}}
```

<HostCode product={props.product} lang="bash" code={`mosquitto_pub -h "{MQTT_HOST}" -t "v1/gateway/rpc" -u "$ACCESS_TOKEN" -m '{"device":"Device A","id":1,"data":{"success":true}}'`} />

<Aside type="note">
  The device must be connected via `v1/gateway/connect` before it can receive RPC commands.
</Aside>
