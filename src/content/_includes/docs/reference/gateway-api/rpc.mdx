import DocLink from '@components/DocLink.astro';
import HostCode from '@components/HostCode.astro';
import ShowFor from '@components/ShowFor.astro';
import { Products } from '@models/site.models';
import { Aside } from '@astrojs/starlight/components';

## Subscribe to Server-Side RPC Commands

Subscribe to the RPC topic to receive commands targeted at downstream devices.

**Topic:** `v1/gateway/rpc` (subscribe + publish)

<ShowFor product={props.product} show={[Products.CE, Products.PE]}>

Replace `GATEWAY_DEVICE_ACCESS_TOKEN` in the commands below with the gateway device's access token, and `$THINGSBOARD_HOST` with your ThingsBoard instance's host.

</ShowFor>

<ShowFor product={props.product} show={[Products.PAAS, Products.PAAS_EU]}>

Replace `GATEWAY_DEVICE_ACCESS_TOKEN` in the commands below with the gateway device's access token.

</ShowFor>

<HostCode product={props.product} lang="bash" code={`mosquitto_sub -h "{MQTT_HOST}" -t "v1/gateway/rpc" -u "GATEWAY_DEVICE_ACCESS_TOKEN"`} />

Incoming command format:

```json
{
  "device": "Device A",
  "data": {"id": 1, "method": "toggle_gpio", "params": {"pin": 1}}
}
```

| Field | Description |
|-------|-------------|
| `device` | The target device name |
| `data.id` | Request identifier — must be echoed in the reply |
| `data.method` | RPC method name |
| `data.params` | Method parameters (arbitrary JSON) |

## Send RPC Reply

Send the reply to the **same topic**:

```json
{"device": "Device A", "id": 1, "data": {"success": true}}
```

<HostCode product={props.product} lang="bash" code={`mosquitto_pub -h "{MQTT_HOST}" -t "v1/gateway/rpc" -u "GATEWAY_DEVICE_ACCESS_TOKEN" -m '{"device":"Device A","id":1,"data":{"success":true}}'`} />

<Aside type="note">
  The device must be connected via `v1/gateway/connect` before it can receive RPC commands.
</Aside>
