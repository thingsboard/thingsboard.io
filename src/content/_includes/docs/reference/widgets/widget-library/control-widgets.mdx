import WidgetGrid from '~/components/WidgetGrid.astro';

Control widgets are various interactive ThingsBoard widgets designed to control the behavior and state of IoT devices.
They enable sending RPC commands, updating attributes, toggling switches, adjusting sliders, and monitoring GPIO states directly from dashboards.

<WidgetGrid bundle="control-widgets" product={props.product} />
