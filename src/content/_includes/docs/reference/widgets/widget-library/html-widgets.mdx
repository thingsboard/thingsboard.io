import WidgetGrid from '~/components/WidgetGrid.astro';

HTML widgets are ThingsBoard widgets that allow displaying custom HTML content in dashboards.
They support static HTML markup, dynamic value injection from data sources, and full markdown/HTML rendering with telemetry and attribute values.

<WidgetGrid bundle="html-widgets" product={props.product} />
