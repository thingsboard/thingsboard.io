import WidgetGrid from '~/components/WidgetGrid.astro';

Buttons widgets facilitate user interaction within ThingsBoard dashboards by enabling navigation between dashboard states, sending RPC commands to devices, and updating device attributes or time series data.

<WidgetGrid bundle="buttons" product={props.product} />
