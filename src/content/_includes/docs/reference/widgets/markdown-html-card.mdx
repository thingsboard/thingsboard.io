import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import ImageGallery from '~/components/ImageGallery.astro';
import DocImage from '~/components/DocImage.astro';
import DocLink from '@components/DocLink.astro';
import { Products } from '~/models/site.models';
import { Steps, Aside } from '@astrojs/starlight/components';

The **Markdown/HTML Card** widget renders Markdown or HTML content on a dashboard. Use it for static information displays, templated data cards, or fully custom HTML layouts styled with CSS.

## Key capabilities

- **Static Markdown** — write Markdown or HTML with `${key}` placeholders that resolve to live entity data.
- **Dynamic content** — use a JavaScript function to build HTML at runtime from any data key.
- **CSS styling** — add custom styles in the widget's CSS tab.
- **Actions** — attach click or row-click actions for navigation or state changes.

## Add the widget

<Steps>
1. Enter dashboard edit mode and click **+ Add widget**.
2. Select the **HTML widgets** bundle.
3. Choose **Markdown/HTML Card**.
4. Configure the data source (entity alias and keys).
5. Click **Add**.
</Steps>

## Data sources (optional)

You can attach <DocLink product={props.product} path="user-guide/aliases">entity aliases</DocLink> as data sources. The widget resolves `${key}` placeholders or passes data to the value function.

## Configuration

### Static Markdown/HTML template

Enter Markdown or HTML directly in the **Markdown/HTML pattern** field. Use `${key}` to insert live values and `${key:N}` to control decimal places.

<DocImage src="/src/assets/images/reference/widgets/markdown-html-card/markdown-html-pattern.png" alt="Markdown/HTML pattern field" width="80%" />

```markdown
### Temperature value card
- **Current entity**: ${entityName}
- **Current temperature**: ${temperature:1} °C
```

Download a sample widget: <a href="/resources/markdown_html_card_temperature_value_card.json" download>markdown_html_card_temperature_value_card.json</a>

### Dynamic Markdown/HTML content

Enable the **Use value function** toggle to build content programmatically. The function receives a `data` array with the latest values for each entity and must return an HTML string.

<DocImage src="/src/assets/images/reference/widgets/markdown-html-card/markdown-html-value-function.png" alt="Value function editor" width="80%" />

```js
const entity = data[0];
const color = entity.temperature > 25 ? "red"
            : entity.temperature > 20 ? "green"
            : "blue";
return `### Temperature value card
- Current entity: <span>${entity.entityName}</span>
- Current temperature: <span style="color:${color};">${entity.temperature.toFixed(1)} °C</span>`;
```

Download a sample widget: <a href="/resources/markdown_html_card_temperature_value_card_2.json" download>markdown_html_card_temperature_value_card_2.json</a>

### Content styling (CSS)

Add custom CSS in the widget's **CSS** tab. Class names used in the Markdown/HTML template are available for styling.

<DocImage src="/src/assets/images/reference/widgets/markdown-html-card/content-styling-css.png" alt="CSS styling tab" width="80%" />

```html
<div class="office-card">
  <h3>${entityName}</h3>
  <p class="blue-box">
    <strong>Current temperature:</strong>
    <span class="temp-value">${temperature:1}</span> °C
  </p>
  <p class="green-box">
    <strong>Current humidity:</strong>
    <span class="hum-value">${humidity:0}</span> %
  </p>
</div>
```

```css
.office-card {
  box-sizing: border-box;
  padding: 16px;
  margin: 6px 0;
  text-align: center;
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, "Noto Sans";
}
.office-card h3 {
  margin: 0 0 12px;
  font-size: 24px;
}
.blue-box {
  background: #2196F31A;
  border: 2px solid #2196F3;
  border-radius: 10px;
  padding: 10px;
  margin: 8px 0;
  font-weight: 700;
}
.green-box {
  background: #4CAF501A;
  border: 2px solid #4CAF50;
  border-radius: 10px;
  padding: 10px;
  margin: 6px 0;
  font-weight: 700;
}
```

Download a sample widget: <a href="/resources/markdown_html_card_with_css.json" download>markdown_html_card_with_css.json</a>

## Card styling

Configure the widget card appearance (title, background, shadow, border) in the **Card** tab of the widget settings. See <DocLink product={props.product} path="user-guide/widgets#widget-card">Widget card settings</DocLink> for details.

## Actions and navigation

Attach <DocLink product={props.product} path="user-guide/actions">widget actions</DocLink> to navigate between dashboard states, open URLs, or execute custom functions on click.

## Example: Office information card

This example creates an information card for an office asset, displaying address, manager, phone, and email attributes.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/markdown-html-card/office-information-card.png', alt: 'Office information card', caption: 'The finished office information card.' },
]} />

### Step 1: Prepare entity

Import the office asset using this CSV: <a href="/resources/office_a_asset.csv" download>office_a_asset.csv</a>

The asset should have server attributes: `address`, `officeManager`, `phone`, and `email`.

### Step 2: Add and configure the widget

<Steps>
1. Add a **Markdown/HTML Card** widget to your dashboard.
2. Configure a data source pointing to the office asset, adding the four attributes as data keys.
3. Enable **Use value function** and paste the JavaScript function below.
4. Add CSS styles in the CSS tab.
5. Save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/markdown-html-card/example-markdown-html-card-1.png', alt: 'Add widget', caption: 'Add the Markdown/HTML Card widget.' },
  { src: '/src/assets/images/reference/widgets/markdown-html-card/example-markdown-html-card-2.png', alt: 'Configure datasource', caption: 'Configure the data source with office attributes.' },
  { src: '/src/assets/images/reference/widgets/markdown-html-card/example-markdown-html-card-3.png', alt: 'Value function', caption: 'Enable the value function and paste the code.' },
  { src: '/src/assets/images/reference/widgets/markdown-html-card/example-markdown-html-card-4.png', alt: 'Final result', caption: 'The office information card on the dashboard.' },
]} />

**Value function:**

```js maxLines=20 collapsible
if (data.length) {
    const buildingAttributes = ['address', 'officeManager', 'phone', 'email'];
    let information = {
        entityName: data[0].entityName ? data[0].entityName : 'Not found',
        buildingAttributesField: {}
    };
    for (let key of buildingAttributes) {
        information.buildingAttributesField[key] = data[0][key] ? data[0][key] : 'Not found';
    }
    const attributesNotFound = Object.values(information.buildingAttributesField)
        .every(value => value === 'Not found');
    if (attributesNotFound) {
        return '<div class="no-data-card">' +
            '<h1 class="card-title">Information on ' + information.entityName + '</h1>' +
            '<div class="no-data-block"><div class="no-data-content">' +
            '<p class="no-data-title">There is no information about the building</p>' +
            '</div></div></div>';
    } else {
        return '<div class="card">' +
            '<h1 class="card-title">Information on ' + information.entityName + '</h1>' +
            '<div class="attributes">' +
            '<div class="attribute_container"><div class="icon_wrapper"><tb-icon class="icon">place</tb-icon></div>' +
            '<div><div class="attribute_label">Address</div><div class="attribute">' + information.buildingAttributesField.address + '</div></div></div>' +
            '<div class="attribute_container"><div class="icon_wrapper"><tb-icon class="icon">person</tb-icon></div>' +
            '<div><div class="attribute_label">Manager</div><div class="attribute">' + information.buildingAttributesField.officeManager + '</div></div></div>' +
            '<div class="attribute_container"><div class="icon_wrapper"><tb-icon class="icon">call</tb-icon></div>' +
            '<div><div class="attribute_label">Phone</div><div class="attribute">' + information.buildingAttributesField.phone + '</div></div></div>' +
            '<div class="attribute_container"><div class="icon_wrapper"><tb-icon class="icon">mail_outline</tb-icon></div>' +
            '<div><div class="attribute_label">Email</div><div class="attribute">' + information.buildingAttributesField.email + '</div></div></div>' +
            '</div></div>';
    }
}
```

**CSS styles:**

```css maxLines=20 collapsible
.card, .no-data-card { padding: 0px; height: 100%; }
.card-title { position: sticky; top: 0; z-index: 2; font-size: 16px; letter-spacing: 0.25px; padding: 16px; }
.attribute_container { display: flex; align-items: center; gap: 20px; padding: 16px; border-bottom: 1px solid #0000000d; }
.icon { z-index: 1; }
.icon_wrapper { display: flex; position: relative; align-items: center; padding: 8px; border: 1px solid var(--tb-primary-100, #305680); border-radius: 4px; }
.icon_wrapper:before { content: ''; z-index: 0; position: absolute; opacity: 0.1; background-color: var(--tb-primary-200, #305680); width: 100%; height: 100%; left: 0; top: 0; }
.attribute, .attribute_label { font-size: 14px; line-height: 20px; }
.attribute_label { color: #00000061; }
.no-data-card { display: flex; flex-direction: column; }
.no-data-block { display: flex; flex-direction: column; justify-content: center; height: 100%; max-width: 345px; padding: 0 15px; margin: 0 auto; text-align: center; }
.no-data-content { transform: translateY(-30%); }
.no-data-block p { font-weight: 500; }
.no-data-title { color: rgba(0, 0, 0, 0.54); }
```

Download the complete widget: <a href="/resources/markdown_html_card_office_information_card.json" download>markdown_html_card_office_information_card.json</a>
