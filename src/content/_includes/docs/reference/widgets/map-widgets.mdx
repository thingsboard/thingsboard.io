import MultiProductImageGallery from '~/components/MultiProductImageGallery.astro';
import DocLink from '@components/DocLink.astro';
import { Products } from '~/models/site.models';
import { Steps, Aside } from '@astrojs/starlight/components';

Map widgets in ThingsBoard enable you to visualize entities and data on a map. They allow placing markers, zones, routes, and other geo-objects in space, which is especially useful for IoT solutions involving physical locations.

In ThingsBoard version 4.0, we've completely reworked our Map widgets to make them more flexible, easier to configure, and more powerful for working with geospatial data.

**Use cases**

- Real-time entity location tracking (vehicles, drones, people)
- Route visualization and movement analysis (logistics, public transport, historical paths)
- Visualization of object layouts (factory layouts, parking, floor plans)
- Interactive mapping (user-placed markers, geofence creation)

**Types of map widgets**

- **Map** widget: Displays real-world locations using base maps like OpenStreetMap, Google Maps, HERE or Tencent.
- **Image Map** widget: Ideal for indoor layouts or relative positioning — perfect for floor plans, smart parking, factory zones, and more. Works with custom image backgrounds.
- **Trip Map** and **Route Map** widgets: Visualize movement on the map. The Trip Map includes animated playback and timeline control for enhanced tracking visualization.

**Key improvements:**

- **Easily place elements on the map**. Add items like markers, polygons, or circles to the map with just a few clicks. Under the hood, this triggers a custom function that captures the clicked location's coordinates and stores them as attributes on the target entity.
- **New unified Trip Map widget**. Combines the features of the older Trip Animation and Router Map widgets into one powerful tool:
  - Supports animated routes, moving markers, and stop points.
  - Includes a set of dedicated marker icons.
  - Offers multiple route styling options, including path decorators and visible points.
  - Comes with customizable timeline controls — change date formats, playback speed, and more.
- **Dynamic map layer switching**. Switch between map types — standard, satellite, hybrid, or even your own custom layers — directly inside the widget.
- **Reference layer support**. Add a transparent overlay layer to provide extra map context or background visuals.
- **Improved configuration for map items (Marker / Polygon / Circle)**
  - Each overlay type now has separate settings, so they don't conflict with one another.
  - Better coordinate validation reduces the chance of mistakes — fewer missing keys or misconfigured sources.
  - Built-in marker shapes available out of the box, with full support for custom icons too.
  - New "Range" color mode added — dynamically change marker color based on key values (e.g., temperature or status).
  - Set separate "Action on click" and "Tag action" for each overlay for more precise interactivity.
  - Easily group overlays — toggle visibility for multiple map items in one click.
  - Improved editing experience — click on any map item to see contextual edit options right away.
  - You can now choose the attribute scope (e.g., shared, server-side, telemetry) when setting coordinate values.
- **Use extra data sources**. Pull in additional values for labels, tooltips, or any extra info you want to show on the map.
- **Flexible control panel placement**. Move map controls (zoom, layer switchers, custom buttons) to any corner of the widget — perfect for adapting to different UI layouts.
- **Map scale display**. Add an optional scale bar to help users visually estimate distances on the map.
- **Custom action buttons**. Add your own buttons right on the map — each can trigger a specific custom action (like creating an entity, switching views, or opening URLs).

All of this — and more — is now available in the new Map widgets in ThingsBoard 4.0.

## Adding a Map widget to the dashboard

To add a Map widget to the dashboard, follow these steps:

<Steps>
1. In dashboard edit mode, click the "Add widget" button at the top of the screen, or click the large "Add new widget" icon in the center of the screen (if this is your first widget on the dashboard).
2. Find the "Maps" widget bundle and click on it. This category includes all available map widget types.
3. Select the map widget that best fits your use case.
4. Configure the widget to match your data and visualization requirements. You can adjust data sources, appearance, map provider settings, and more.
5. Click the "Add" in the bottom-right corner of the widget configuration window to place it on your dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/adding-map-widget-1.png', alt: 'Add widget button in edit mode', caption: 'In dashboard edit mode, click the "Add widget" button at the top of the screen, or click the large "Add new widget" icon in the center of the screen (if this is your first widget on the dashboard).' },
  { src: '/src/assets/images/reference/widgets/map-widgets/adding-map-widget-2.png', alt: 'Maps widget bundle', caption: 'Find the "Maps" widget bundle and click on it. This category includes all available map widget types.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/adding-map-widget-3.png', alt: 'Select map widget type', caption: 'Select the map widget that best fits your use case.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/adding-map-widget-4.png', alt: 'Configure and add map widget', caption: 'Configure the widget to match your data and visualization requirements. You can adjust data sources, appearance, map provider settings, and more. Click the "Add" in the bottom-right corner of the widget configuration window to place it on your dashboard.' },
]} />

## Map type

The first step in configuring the map widget is selecting the map type.
ThingsBoard offers several map widgets, including Image Map, which allows you to use a custom background image as the map.

You can also configure map layers, enabling flexible switching between different map styles — such as satellite, hybrid, or custom layers — directly within the widget, with just a few clicks.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/map-type-1.png', alt: 'Map type selection', caption: 'The first step in configuring the map widget is selecting the map type. ThingsBoard offers several map widgets, including Image Map, which allows you to use a custom background image as the map. You can also configure map layers, enabling flexible switching between different map styles — such as satellite, hybrid, or custom layers — directly within the widget, with just a few clicks.' },
]} />

**Adding a new map layer**

You can add additional layer to the map widget by following these steps:

<Steps>
1. Click "Add layer".
2. Enter a label for the layer — this name will be shown in the widget's layer switcher.
3. Choose the map provider by selecting from the available options (OpenStreetMap, Google, HERE, Tencent) or by specifying a custom tile server.
4. Apply changes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/map-type-adding-layer-1.png', alt: 'Click Add layer', caption: 'Click "Add layer".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/map-type-adding-layer-2.png', alt: 'Configure layer settings', caption: 'Enter a label for the layer. Select a provider from the available options, or add a custom provider and specify its layer.' },
]} />

Now switch to the new layer on the widget.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/changing-map-type-1.png', alt: 'Click Layer icon button', caption: 'Click the "Layer" icon button on the widget.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/changing-map-type-2.png', alt: 'Available map layers', caption: 'You will see the available map layers. Switch between them.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/changing-map-type-3.png', alt: 'Layer switcher result', caption: 'Enter a label for the layer — this name will be shown in the widget\'s layer switcher. Choose the map provider by selecting from the available options (OpenStreetMap, Google, HERE, Tencent) or by specifying a custom tile server. Apply changes.' },
]} />

In the "**Layer settings**", you can also enable the "**Reference layer**" option — a special transparent layer that contains labeled map elements:

<Steps>
1. Go to the "Layer settings" by clicking the "gear" icon.
2. From the dropdown menu, select the layer type, or leave it as "No layer" if you don't want to use one.
3. Apply changes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/map-type-reference-layer-1.png', alt: 'Open Layer settings', caption: 'Go to the "Layer settings" by clicking the "gear" icon.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/map-type-reference-layer-2.png', alt: 'Select reference layer type', caption: 'From the dropdown menu, select the layer type, or leave it as "No layer" if you don\'t want to use one. Apply changes.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/map-type-reference-layer-3.png', alt: 'Reference layer result' },
]} />

## Overlays

Map items are visual elements displayed on a map widget to represent the spatial position, area, or influence zone of entities and data.
These elements help visualize and interact with your devices, assets, or other tracked objects based on their spatial data.

ThingsBoard supports the following map item types:

- A **marker** is a point on the map based on the coordinates specified in the entity. It is used to display the location of a device, asset, vehicles or any tracked object.
- A **polygon** is a flat, closed shape made up of multiple connected points. It is used to represent areas or boundaries on the map. You can use the polygon option to mark your assets or any other objects. The polygon is based on coordinates specified in the entity.
- A **circle** is a flat shape defined by a center point and a radius, with all boundary points equidistant from the central point. It is ideal for visualizing zones of influence or coverage areas. The circle is based on coordinates specified in the entity.

Each map item type is bound to an entity and visualized based on its coordinates, provided as either attributes or telemetry.
You can customize their appearance, behavior, and interaction rules directly in the widget settings.

### Marker

Markers are commonly used to indicate the location of buildings, fixed sensors, vehicle tracking, or any other static or moving entities.

To be placed on the map, the entity must have coordinates as attributes or telemetry in the following format:

```
Latitude,Longitude
```

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-1.png', alt: 'Marker on the map' },
]} />

#### Place marker on map

To place a marker on the map, follow these steps:

<Steps>
1. Enter widget edit mode and go to the "Overlays" section.
2. Make sure you're on the "Marker" tab and click "Add marker".
3. Select the data source - it can be a device, an entity alias, or a function.
4. Define the coordinate keys. By default, ThingsBoard uses *latitude* and *longitude* attributes as the coordinate keys for the marker. If your entity uses different key names, update them here.
5. Apply the changes.
</Steps>

The marker will appear on the map based on the specified coordinates.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/add-marker-1.png', alt: 'Open Overlays section and add marker', caption: 'Enter widget edit mode and go to the "Overlays" section. Make sure you\'re on the "Marker" tab and click "Add marker".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/add-marker-2.png', alt: 'Configure marker data source and keys', caption: 'Select the data source - it can be a device, an entity alias, or a function. Define the coordinate keys. By default, ThingsBoard uses *latitude* and *longitude* attributes as the coordinate keys for the marker. If your entity uses different key names, update them here. Apply the changes.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/add-marker-3.png', alt: 'Marker placed on the map', caption: 'The marker will appear on the map based on the specified coordinates.' },
]} />

#### Marker configuration

To configure a specific marker, switch to the map widget's edit mode. In the "Overlays" section, click the "gear" icon button next to the desired marker to open its configuration panel.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-1.png', alt: 'Open marker configuration panel', caption: 'To configure a specific marker, switch to the map widget\'s edit mode and, in the Overlays section, click the gear icon next to the desired marker to open its configuration panel.' },
]} />

**Datasource**

Specify the data source for the marker. It can be:
- A <DocLink product={props.product} path="user-guide/ui/devices">device</DocLink>
- An <DocLink product={props.product} path="user-guide/ui/aliases">entity alias</DocLink>
- A function

If needed, use the data filter to refine the selection (e.g., filter by name or type).

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-datasource-1.png', alt: 'Marker datasource configuration', caption: 'Specify the marker data source — it can be a device, entity alias, or function. If necessary, use a filter.' },
]} />

**Keys**

Define the coordinate keys.
By default, ThingsBoard uses *latitude* and *longitude* attributes as the default coordinate keys.
If your entity uses custom key names, update them accordingly.

You can also define additional keys for dynamic display elements, such as marker color, tooltip content, Label text, etc.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-keys-1.png', alt: 'Marker coordinate keys configuration', caption: 'Define the coordinate keys. ThingsBoard automatically sets the coordinate keys as latitude and longitude. If your entity uses custom coordinate key names, update them here. Additional data keys can be used for labels, tooltips, and displaying extra information directly on the map.' },
]} />

**Marker**

This section defines how marker appear on the map.
Customize the marker by selecting a different one from a variety of standard shapes and icons, adjusting its size and color, or use your own images for full visual flexibility.

*Marker styling example*: This example demonstrates how to configure marker styling so that its color changes automatically based on the value of the "state" key:
- Green marker — when state = "opened"
- Red marker — when state has any other value
- Gray marker — if the state key does not exist

This approach visually reflects the status of the marker, greatly enhancing monitoring convenience.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-icon-color-1.png', alt: 'Customize marker shape and icon', caption: 'Customize the marker by selecting a different one from a variety of standard shapes and icons, and adjusting its size.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-icon-color-2.png', alt: 'Set color function for marker', caption: 'Use a function that dynamically changes the marker color based on the value of the "state" key.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-icon-color-3.png', alt: 'Apply marker color changes', caption: 'Apply all changes.' },
]} />

Color-changing function based on the "state" key value:

```js
var state = data.state;
if (state  !== '') {
  if (state == 'opened'){
    return 'green';
  } else {
    return 'red';
  }
} else {
  return 'grey';
}
```

Currently, the "state" key has the value "opened", so the marker is displayed in green.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-icon-color-4.png', alt: 'Marker displayed in green', caption: 'Currently, the "state" key has the value "opened", so the marker is displayed in green.' },
]} />

As soon as the value of the "state" key changes to anything else, the marker will turn red.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-icon-color-5.png', alt: 'Marker displayed in red', caption: 'As soon as the value of the "state" key changes to anything else, the marker will turn red.' },
]} />

**Label**

The marker label is placed above the icon and, by default, displays the entity name.
You can edit the label text or use a label function to show more dynamic information.

Label styling example:

```html
<div style='position: relative; white-space: nowrap; text-align: center; font-size: 14px; top: 2px;'>
    <span style='margin-left: -500%;'></span>
    <div style='border: 2px solid #EC9704; border-radius: 10px; color: #000; background-color: #fff;  padding-left: 8px; padding-right: 8px; padding-top: 3px; padding-bottom: 3px;'>${entityName}</div>
</div>
```

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-label-1.png', alt: 'Marker label configuration', caption: 'Here, you can customize the label displayed above the marker. By default, the label displays the entity name. You can edit the label text or use a label function to display more dynamic information.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-label-2.png', alt: 'Customized marker label', caption: 'Customized marker label.' },
]} />

**Tooltip**

Here you can configure a tooltip that appears when you click on or hover over the marker.
Use the tooltip pattern or tooltip function to define more dynamic content. Alternatively, you can hide the tooltip altogether.

For example, you want the tooltip displays the status of a fueling station (open/closed) by retrieving the value from the "state" key, which is specified as one of the "Additional data keys" in the "Keys" section.
Use the function below and paste it into the Tooltip function window:

```js maxLines=6 collapsible
const stateObj = {
    "opened": "#4CAF50",
    "closed": "#D12730"
};

const currentState = data.state;

return `<div style="display: flex; justify-content: space-between; align-items: center; gap: 7px">
        <span style="font-weight: 600; font-size: 13px; line-height: 20px; color: #000000de">${data.entityName} is ${currentState}</span>
        <span style="width: 10px; height: 10px; border-radius: 100px; background-color: ${stateObj[currentState]}"></span>
    </div>`;
```

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-tooltip-1.png', alt: 'Configure tooltip function', caption: 'Use the tooltip pattern or tooltip function.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-tooltip-2.png', alt: 'Tooltip displayed on marker click', caption: 'Click on the marker to display a tooltip.' },
]} />

**Tag actions**

You can add an action to a tooltip as a tag. This can be to switch to a new dashboard state or another dashboard, to open an external URL, or even to initiate a custom action.
Learn more about the available action types and how to configure them in the <DocLink product={props.product} path="user-guide/ui/widget-actions">widget actions documentation</DocLink>.

First, you need to add a tag action and then reference its name in the tooltip function:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-tooltip-action-1.png', alt: 'Click Add tag action button', caption: 'Click the "Add tag action" icon button.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-tooltip-action-2.png', alt: 'Configure tag action', caption: 'Configure the action and click "Add".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-tooltip-action-3.png', alt: 'Expand tooltip function window', caption: 'Now, you need to add the tag action link to the tooltip function. Expand the tooltip function window to fullscreen for easier editing.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-tooltip-action-4.png', alt: 'Edit tooltip function with tag action link', caption: 'Edit the function by adding a link to the tag action. Be sure to specify the tag action name correctly.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-tooltip-action-5.png', alt: 'Apply tag action changes', caption: 'Apply all changes.' },
]} />

The function below already includes a reference to the tag action named "my-action".
If you gave your tag action a different name, please replace "my-action" with your actual tag name.
Use this function to replace the previous tooltip function:

```js maxLines=6 collapsible
const stateObj = {
  "opened": "#4CAF50",
  "closed": "#D12730"
};

const currentState = data.state;

return `<div style="text-align: center">
            <div style="display: flex; justify-content: space-between; align-items: center; gap: 7px">
                <span style="font-weight: 600; font-size: 13px; line-height: 20px; color: #000000de">${data.entityName} is ${currentState}</span>
                <span style="width: 10px; height: 10px; border-radius: 100px; background-color: ${stateObj[currentState]}"></span>
            </div>
            <link-act name='my-action'>Details</link-act>
    </div>`;
```

Click on the marker. In the tooltip that appears, you'll see a tag — click on it. The action you defined in the tag's settings will be executed.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-tooltip-action-6.png', alt: 'Click tag in tooltip', caption: 'Click on the marker. In the tooltip that appears, you\'ll see a tag — click on it.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-tooltip-action-7.png', alt: 'Tag action executed', caption: 'The action you defined in the tag\'s settings will be executed.' },
]} />

**Behavior**

Set the behavior of the marker when you click it.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-behavior-1.png', alt: 'Open Behavior section', caption: 'Go to the "Behavior" section and click the "On click" action field to define the action instead of the default "Do nothing".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-behavior-2.png', alt: 'Set up desired action', caption: 'Set up the desired action and apply the changes.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-behavior-3.png', alt: 'Click on the marker', caption: 'Click on the marker.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-behavior-4.png', alt: 'Action triggered', caption: 'The action you specified in the action settings will be triggered.' },
]} />

**Groups**

Markers can be grouped, allowing you to easily show or hide grouped elements later using the "Groups" tab.

<Steps>
1. In the "Groups" section, enter a name for the group to which you want to add this marker.
2. A "Groups" icon button will appear on the widget. Click on it.
3. To hide a group, uncheck the box next to its name.
4. The markers that belong to this group have been hidden.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-groups-1.png', alt: 'Enter group name for marker', caption: 'In the "Groups" section, enter a name for the group to which you want to add this marker.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-groups-2.png', alt: 'Groups icon button on widget', caption: 'A "Groups" icon button will appear on the widget. Click on it.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-groups-3.png', alt: 'Uncheck group to hide', caption: 'To hide a group, uncheck the box next to its name.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-groups-4.png', alt: 'Markers hidden by group', caption: 'The markers that belong to this group have been hidden.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-configuration-appearance-groups-5.png', alt: 'Group visibility result' },
]} />

**Marker editing tools**

You can use the built-in tools to perform operations on markers such as **Add** / **Move** / **Delete**. By default, these tools are disabled.

Check the boxes next to the tools you want to enable.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/marker-editing-tools-1.png', alt: 'Enable marker editing tools', caption: 'You can use the built-in tools to perform operations on markers such as Add / Move / Delete. By default, these tools are disabled. Check the boxes next to the tools you want to enable.' },
]} />

**Adding marker**

If your entity doesn't have coordinates set, you can **manually place marker using the map widget tools** — its coordinates will be automatically saved to the entity as attributes.

First, add the marker placement tool to the map:

<Steps>
1. In the "**Edit marker**" section, check the "**Add**" tool.
2. Next, select the **attribute scope** where the coordinates will be stored: "**Server**" or "**Shared**".
3. Optionally, enable snapping to other vertices for precision drawing.
4. Then, save the changes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/manually-add-marker-1.png', alt: 'Enable Add marker tool', caption: 'In the "Edit marker" section, check the "Add" tool. Next, select the attribute scope where the coordinates will be stored: "Server" or "Shared". Optionally, enable snapping to other vertices for precision drawing. Then, save the changes.' },
]} />

You can now place the marker on the map:

<Steps>
1. On the map widget, locate and click the "Place marker" icon button. If you're using an alias with multiple entities as the data source, you'll need to select the appropriate entity from the dropdown menu.
2. Find the location on the map where you want to place the marker and click on it.
3. The marker will be added, and its coordinates will be automatically saved to the entity as attributes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/place-marker-1.png', alt: 'Click Place marker button', caption: 'On the map widget, locate and click the "Place marker" icon button. If you\'re using an alias with multiple entities as the data source, you\'ll need to select the appropriate entity from the dropdown menu.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/place-marker-2.png', alt: 'Click location to place marker', caption: 'Find the location on the map where you want to place the marker and click on it.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/place-marker-3.png', alt: 'Marker placed with coordinates saved', caption: 'The marker will be added, and its coordinates will be automatically saved to the entity as attributes.' },
]} />

Also, by selecting the checkboxes next to the desired tools, you'll be able to **move** or **remove** a marker.

**Moving marker**

<Aside type="note">
Don't forget to enable the "**Move**" tool in the marker settings under the "**Edit marker**" section.
</Aside>

To move the marker, click and hold the marker with your mouse, drag it to the new location, and then release the mouse button.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/move-marker-1.png', alt: 'Move marker by dragging', caption: 'To move the marker, click and hold the marker with your mouse, drag it to the new location, and then release the mouse button.' },
]} />

**Removing marker**

<Aside type="note">
Don't forget to enable the "**Remove**" tool in the marker settings under the "**Edit marker**" section.
</Aside>

To delete a marker, simply click on it, then click the trash bin icon in the menu at the bottom of the widget.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/remove-marker-1.png', alt: 'Remove marker using trash bin icon', caption: 'To delete a marker, simply click on it, then click the trash bin icon in the menu at the bottom of the widget.' },
]} />

**Use map markers clustering**

Map clustering is a feature that automatically groups multiple markers located within the same visible area at a certain zoom level into a single cluster — a circular icon displaying the number of grouped items inside, instead of showing each marker individually.

This greatly improves map readability, especially when visualizing a large number of markers.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/use-map-markers-clustering-1.png', alt: 'Enable marker clustering', caption: 'To enable clustering for map markers, check the corresponding option and configure the necessary settings.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/use-map-markers-clustering-2.png', alt: 'Change zoom level', caption: 'Change the zoom level.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/use-map-markers-clustering-3.png', alt: 'Markers grouped into cluster', caption: 'Your markers with clustering enabled will group into a circular icon that shows the number of clustered items inside.' },
]} />

### Polygon

The polygon is a flat shape defined by a finite number of points.
It can be used to mark areas such as buildings, fields, service areas, visualize coverage areas or restricted regions, etc.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-1.png', alt: 'Polygon on the map' },
]} />

To display an entity as a polygon, it must have coordinates provided as attributes or telemetry in the following format:

```
[[1Latitude,1Longitude],[2Latitude,2Longitude],...,[nLatitude,nLongitude]]
```

where **n** - number of coordinates which polygon is described by.

#### Adding polygon

To place the polygon on the map, follow these steps:

<Steps>
1. Enter widget edit mode and go to the "Overlays" section.
2. Switch to the "Polygons" tab and click "Add polygon".
3. Select the entity that will be represented as a polygon. This can be a device, entity alias, or function.
4. Define the key with the coordinates of the polygon. ThingsBoard will use the "perimeter" key by default to read the polygon coordinates from the entity's attributes. If you use a different key, update it accordingly.
5. Click "Apply" — the polygon will appear on the map based on the entity's data.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/add-polygon-1.png', alt: 'Open Overlays and add polygon', caption: 'Enter widget edit mode and go to the "Overlays" section. Switch to the "Polygons" tab and click "Add polygon".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/add-polygon-2.png', alt: 'Configure polygon entity and key', caption: 'Select the entity that will be represented as a polygon. This can be a device, entity alias, or function. Define the key with the coordinates of the polygon. ThingsBoard will use the "perimeter" key by default to read the polygon coordinates from the entity\'s attributes. If you use a different key, update it accordingly. Click "Apply".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/add-polygon-3.png', alt: 'Polygon displayed on map', caption: 'The polygon will appear on the map based on the entity\'s data.' },
]} />

#### Polygon configuration

To configure a specific polygon, switch to the map widget's edit mode.
Then, in the "Polygon" tab under the "Overlays" section, click the "gear" icon button next to the desired polygon to open its configuration panel.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-configuration-1.png', alt: 'Open polygon configuration panel', caption: 'To configure a specific polygon, switch to the map widget\'s edit mode. Then, in the "Polygon" tab under the "Overlays" section, click the "gear" icon button next to the desired polygon to open its configuration panel.' },
]} />

**Datasource**

Specify the polygon's data source — it can be a device, an entity alias, or a function.

If needed, apply a filter to narrow down the selection.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-configuration-datasource-1.png', alt: 'Polygon datasource configuration', caption: 'Specify the polygon data source — it can be a device, entity alias, or function. If necessary, use a filter.' },
]} />

**Keys**

Define the key that contains the polygon coordinates.
By default, ThingsBoard uses "perimeter" as the polygon key. If your entity uses a different key name, make sure to update it here.

Additional data keys can be used for labels, tooltips, or to display extra information directly on the map.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-configuration-keys-1.png', alt: 'Polygon coordinate keys configuration', caption: 'Define the key that contains the polygon coordinates. By default, ThingsBoard uses "perimeter" as the polygon key. If your entity uses a different key name, make sure to update it here. Additional data keys can be used for labels, tooltips, or to display extra information directly on the map.' },
]} />

**Filling the polygon**

**Fill** — defines how the polygon is filled on the map:
- Color -- Sets a solid fill color.
- Stripe -- Applies diagonal hatching instead of a solid color, useful for indicating special zones.
- Image -- Allows you to use an image as the background fill.

**Stroke** -- defines the polygon outline:
- Border color
- Line thickness

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-color-1.png', alt: 'Configure polygon fill and stroke', caption: 'Change the fill color and stroke color of the polygon. For the fill, you can also use stripes or an image as a background.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-color-2.png', alt: 'Polygon color result' },
]} />

**Label**

Configure the label displayed above the polygon.
By default, the label shows the name of the entity linked to that polygon. You can manually edit the label text or use a label function to display dynamic information.

For example, the label can show the current status of the polygon, providing users with a quick and clear understanding of its state.
If desired, you can also hide the label entirely.

```html
<div style='position: relative; white-space: nowrap; text-align: center; font-size: 10px; top: -7px;'>
  <span style='margin-left: -500%;'></span>
  <div style='border: 2px solid #3388FF; border-radius: 5px; color: #000; background-color: #fff;  padding-left: 4px; padding-right: 4px; padding-top: 2px; padding-bottom: 2px;'>${entityLabel}</div>
</div>
```

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-label-1.png', alt: 'Polygon label configuration', caption: 'Configure the label displayed above the polygon. By default, the label shows the name of the entity linked to that polygon. You can manually edit the label text or use a label function to display dynamic information.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-label-2.png', alt: 'Custom polygon label', caption: 'Custom polygon label.' },
]} />

**Tooltip**

Here you can configure a tooltip that appears when you click on or hover over the polygon.
Use the tooltip pattern or tooltip function to define more dynamic content.
Alternatively, you can hide the tooltip altogether.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-tooltip-1.png', alt: 'Polygon tooltip configuration', caption: 'Here you can configure a tooltip that appears when you click on or hover over the polygon. Use the tooltip pattern or tooltip function to define more dynamic content. Alternatively, you can hide the tooltip altogether.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-tooltip-2.png', alt: 'Polygon tooltip displayed', caption: 'Click on the polygon to display the tooltip.' },
]} />

**Behavior**

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-behavior-1.png', alt: 'Open Behavior section', caption: 'Go to the "Behavior" section and click the "On click" action field to define the action instead of the default "Do nothing".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-behavior-2.png', alt: 'Set up desired action', caption: 'Set up the desired action and apply the changes.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-behavior-3.png', alt: 'Click on the polygon', caption: 'Click on the polygon.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-behavior-4.png', alt: 'Polygon action triggered', caption: 'The action you specified in the action settings will be triggered.' },
]} />

**Groups**

Polygons can be grouped, allowing you to easily show or hide grouped elements later using the "Groups" tab.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-groups-1.png', alt: 'Enter group name for polygon', caption: 'In the "Groups" section, enter a name for the group to which you want to add this polygon.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-groups-2.png', alt: 'Groups icon button on widget', caption: 'A "Groups" icon button will appear on the widget. Click on it.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-groups-3.png', alt: 'Uncheck group to hide polygon', caption: 'To hide a group, uncheck the box next to its name.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-groups-4.png', alt: 'Polygon hidden by group', caption: 'The polygon that belong to this group have been hidden.' },
]} />

**Polygon editing tools**

You can use the built-in tools to perform operations on polygons such as **Add** / **Edit** / **Move** / **Delete**. By default, these tools are disabled.

Check the boxes next to the tools you want to enable.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/polygon-editing-tools-1.png', alt: 'Enable polygon editing tools', caption: 'You can use the built-in tools to perform operations on markers such as Add / Move / Delete. By default, these tools are disabled. Check the boxes next to the tools you want to enable.' },
]} />

**Adding polygon**

If an entity doesn't have polygon coordinates set, you can **manually place polygon using the map widget tools** — its coordinates will be automatically saved to the entity as attribute.

First, add the polygon placement tool to the map:

<Steps>
1. In the "**Edit marker**" section, check the "**Add**" tool.
2. Next, select the **attribute scope** where the coordinates will be stored: "**Server**" or "**Shared**".
3. Optionally, enable snapping to other vertices for precision drawing.
4. Then, save the changes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/manually-add-polygon-1.png', alt: 'Enable Add polygon tool', caption: 'In the "Edit polygon" section, check the "Add" tool. Next, select the attribute scope where the coordinate will be stored: "Server" or "Shared". Optionally, enable snapping to other vertices for precision drawing. Then, save the changes.' },
]} />

You can now place the polygon on the map:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/place-polygon-1.png', alt: 'Choose polygon drawing tool', caption: 'The map widget now offers two tools for adding a polygon: "Drew rectangle" and "Drew polygon". Choose the option that suits your needs.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/place-polygon-2.png', alt: 'Select entity from dropdown', caption: 'If you\'re using an alias with multiple entities as the data source, select the desired entity from the dropdown list.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/place-polygon-3.png', alt: 'Place first polygon point', caption: 'Find the desired location on the map and click to place the first point.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/place-polygon-4.png', alt: 'Adjust polygon size', caption: 'Adjust the polygon to the desired size, then click again to finish. If you\'re using "Drew polygon", make sure to close the shape by clicking the first point once you\'ve placed all others.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/place-polygon-5.png', alt: 'Polygon added to map', caption: 'The polygon will be added, and its coordinates will be automatically saved to the entity as attribute.' },
]} />

**Editing polygon**

<Aside type="note">
Don't forget to enable the "**Edit**" tool in the polygon settings under the "**Edit polygon**" section.
</Aside>

To resize the polygon, click on it — its vertices will appear. Drag any of the points to adjust the polygon's size.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/edit-polygon-1.png', alt: 'Click polygon to show vertices', caption: 'To resize the polygon, click on it — its vertices will appear. Drag any of the points to adjust the polygon\'s size.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/edit-polygon-2.png', alt: 'Click outside to apply dimensions', caption: 'Once you\'re satisfied with the new size, click anywhere outside the polygon to apply the current dimensions.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/edit-polygon-3.png', alt: 'Polygon resized' },
]} />

**Moving polygon**

<Aside type="note">
Don't forget to enable the "**Move**" tool in the polygon settings under the "**Edit polygon**" section.
</Aside>

To move the polygon, click on the polygon, hold down the mouse button, and drag it to the new location.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/move-polygon-1.png', alt: 'Enable Move tool for polygon', caption: 'To enable moving a polygon on the map, activate the "Move" tool in the polygon settings under the "Edit polygon" section.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/move-polygon-2.png', alt: 'Drag polygon to new location', caption: 'Now, click on the polygon, hold down the mouse button, and drag it to the new location.' },
]} />

**Rotating polygon**

To rotate a polygon:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/rotate-polygon-1.png', alt: 'Select polygon and click Rotate button', caption: 'Select the polygon by clicking on it, then click the "Rotate polygon" icon button from the tools at the bottom.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/rotate-polygon-2.png', alt: 'Drag point to rotate polygon', caption: 'Grab any point of the polygon and drag it in the desired direction to rotate it.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/rotate-polygon-3.png', alt: 'Finish rotation', caption: 'Once you\'re satisfied with the rotation, click the "Rotate polygon" icon button again to finish.' },
]} />

**Cut polygon area**

To cut out an area from a polygon:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/cut-polygon-area-1.png', alt: 'Select polygon and choose Cut tool', caption: 'Select the polygon by clicking on it, then choose "Cut polygon area" from the tools at the bottom.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/cut-polygon-area-2.png', alt: 'Draw area to remove', caption: 'Draw the area you want to remove by outlining it on the polygon.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/cut-polygon-area-3.png', alt: 'Close the cut shape', caption: 'Close the shape by connecting the first and last points.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/cut-polygon-area-4.png', alt: 'Click outside to save', caption: 'Click anywhere outside the polygon to save the changes.' },
]} />

**Removing polygon**

<Aside type="note">
Don't forget to enable the "**Remove**" tool in the polygon settings under the "**Edit polygon**" section.
</Aside>

To delete a polygon, simply click on it, then click the "trash bin" icon in the menu at the bottom of the widget.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/remove-polygon-1.png', alt: 'Remove polygon using trash bin icon', caption: 'To delete a polygon, simply click on it, then click the "trash bin" icon in the menu at the bottom of the widget.' },
]} />

### Circle

Circle is a plane figure, boundary points of which are always the same distance away from a fixed central point.
A circle can be used to define areas of influence, such as a Wi-Fi zone or the range of a sensor, and more.
You may mark your assets and any other entities with a circle option.

Circle coordinates must follow this format:

```
{"latitude":CoordinateLatitude, "longitude":CoordinateLongitude, "radius":radius}
```

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-1.png', alt: 'Circle on the map' },
]} />

#### Add circle

You can mark your assets and any other entities with a circle using predefined coordinates received as attributes or telemetry. In this example, I'll use a device, which refers to assets of the type "Parking spots" as the data source.

To place the circle on the map, follow these steps:

<Steps>
1. Enter widget edit mode and go to the "Overlays" section.
2. Switch to the "Circles" tab and click "Add circle".
3. Select the entity that will be represented as a circle. This can be a device, entity alias, or function.
4. Define the key with the coordinates of the circle. ThingsBoard will use the "perimeter" key by default to read the circle coordinates from the entity's attribute. If your entity uses a different key name, update it here.
5. Click "Apply" — the circle will appear on the map based on the entity's data.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/add-circle-1.png', alt: 'Open Overlays and add circle', caption: 'Enter widget edit mode and go to the "Overlays" section. Switch to the "Circles" tab and click "Add circle".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/add-circle-2.png', alt: 'Configure circle entity and key', caption: 'Select the entity that will be represented as a circle. This can be a device, entity alias, or function. Define the key with the coordinates of the circle. ThingsBoard will use the "perimeter" key by default to read the circle coordinates from the entity\'s attribute. If your entity uses a different key name, update it here. Click "Apply" — the circle will appear on the map based on the entity\'s data.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/add-circle-3.png', alt: 'Circle added to map', caption: 'The circle has been successfully added to the map.' },
]} />

#### Circle configuration

To configure a specific circle, switch to the map widget's edit mode.
Then, in the "Circle" tab under the "Overlays" section, click the "gear" icon button next to the desired circle to open its configuration panel.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-configuration-1.png', alt: 'Open circle configuration panel', caption: 'To configure a specific circle, switch to the map widget\'s edit mode. Then, in the "Circle" tab under the "Overlays" section, click the "gear" icon button next to the desired circle to open its configuration panel.' },
]} />

**Datasource**

Specify the circle's data source — it can be a device, an entity alias, or a function.
If needed, apply a filter to narrow down the selection.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-configuration-datasource-1.png', alt: 'Circle datasource configuration', caption: 'Specify the circle\'s data source — it can be a device, an entity alias, or a function. If needed, apply a filter to narrow down the selection.' },
]} />

**Keys**

Define the key that contains the circle coordinates. By default, ThingsBoard uses "perimeter" as the circle key. If your entity uses a different key name, make sure to update it here.

Additional data keys can be used for labels, tooltips, or to display extra information directly on the map.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-configuration-keys-1.png', alt: 'Circle coordinate keys configuration', caption: 'Define the key that contains the circle coordinates. By default, ThingsBoard uses "perimeter" as the circle key. If your entity uses a different key name, make sure to update it here. Additional data keys can be used for labels, tooltips, or to display extra information directly on the map.' },
]} />

**Filling the circle**

**Fill** — defines how the circle is filled on the map:
- Color -- Sets a solid fill color.
- Stripe -- Applies diagonal hatching instead of a solid color, useful for indicating special zones.
- Image -- Allows you to use an image as the background fill.

**Stroke** -- defines the circle outline:
- Border color
- Line thickness

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-color-1.png', alt: 'Configure circle fill and stroke', caption: 'Change the fill color and stroke color of the circle. For the fill, you can also use stripes or an image as a background.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-color-2.png', alt: 'Circle color result' },
]} />

**Label**

Configure the label displayed above the circle. By default, the label shows the name of the entity linked to that circle. You can manually edit the label text or use a label function to display dynamic information.

For example, the label can show the current status of the circle, providing users with a quick and clear understanding of its state. If desired, you can also hide the label entirely.

```html
<div style='position: relative; white-space: nowrap; text-align: center; font-size: 10px; top: -7px;'>
    <span style='margin-left: -500%;'></span>
    <div style='border: 2px solid #3B7FE1; border-radius: 5px; color: #000; background-color: #fff;  padding-left: 4px; padding-right: 4px; padding-top: 2px; padding-bottom: 2px;'>${entityLabel}</div>
</div>
```

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-label-1.png', alt: 'Circle label configuration', caption: 'Configure the label displayed above the circle. By default, the label shows the name of the entity linked to that circle. You can manually edit the label text or use a label function to display dynamic information.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-label-2.png', alt: 'Custom circle label', caption: 'Custom circle label.' },
]} />

**Tooltip**

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-tooltip-1.png', alt: 'Circle tooltip configuration', caption: 'Use tooltip pattern or tooltip function.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-tooltip-2.png', alt: 'Circle tooltip displayed', caption: 'Click on the circle to display the tooltip.' },
]} />

**Behavior**

Set the behavior of the circle when you click it.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-behavior-1.png', alt: 'Open Behavior section', caption: 'Go to the "Behavior" section and click the "On click" action field to define the action instead of the default "Do nothing".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-behavior-2.png', alt: 'Set up desired action', caption: 'Set up the desired action and apply the changes.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-behavior-3.png', alt: 'Click on the circle', caption: 'Click on the circle.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-behavior-4.png', alt: 'Circle action triggered', caption: 'The action you specified in the action settings will be triggered.' },
]} />

**Groups**

Circles can be grouped, allowing you to easily show or hide grouped elements later using the "Groups" tab.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-groups-1.png', alt: 'Enter group name for circle', caption: 'In the "Groups" section, enter a name for the group to which you want to add this circle.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-groups-2.png', alt: 'Click Groups icon button', caption: 'Click on the "Groups" icon button.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-groups-3.png', alt: 'Uncheck group to hide circle', caption: 'To hide a group, uncheck the box next to its name.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-groups-4.png', alt: 'Circle hidden by group', caption: 'The circle that belong to this group have been hidden.' },
]} />

**Circle editing tools**

You can use the built-in tools to perform operations on circles such as **Add** / **Move** / **Delete**. By default, these tools are disabled.

Check the boxes next to the tools you want to activate.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/circle-editing-tools-1.png', alt: 'Enable circle editing tools', caption: 'You can use the built-in tools to perform operations on circles such as Add / Move / Delete. By default, these tools are disabled. Check the boxes next to the tools you want to activate.' },
]} />

**Adding circle**

If an entity doesn't have circle coordinates set, you can **manually place circle using the map widget tools** — its coordinates will be automatically saved to the entity as attribute.

First, add the circle placement tool to the map:

<Steps>
1. In the "**Edit marker**" section, check the "**Add**" tool.
2. Next, select the **attribute scope** where the coordinates will be stored: "**Server**" or "**Shared**".
3. Optionally, enable snapping to other vertices for precision drawing.
4. Then, save the changes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/manually-add-circle-1.png', alt: 'Enable Add circle tool', caption: 'In the "Edit marker" section, check the "Add" tool. Next, select the attribute scope where the coordinates will be stored: "Server" or "Shared". Optionally, enable snapping to other vertices for precision drawing. Then, save the changes.' },
]} />

You can now place the circle on the map:

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/place-circle-1.png', alt: 'Click Draw circle button', caption: 'The map widget now offers a tool for adding a circle: "Draw circle". Click on this icon button to begin.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/place-circle-2.png', alt: 'Click to place center point', caption: 'Find the desired location on the map and click to place the first point.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/place-circle-3.png', alt: 'Adjust circle size', caption: 'Adjust the circle to the desired size, then click again to finish.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/place-circle-4.png', alt: 'Circle added to map', caption: 'The circle has been added to the map.' },
]} />

**Editing circle**

<Aside type="note">
Don't forget to enable the "**Edit**" tool in the circle settings under the "**Edit polygon**" section.
</Aside>

To resize the circle, click on it — the radius handle will appear. Drag this point to adjust the size of the circle.
Once you're satisfied with the new size, click anywhere outside the circle to apply the current dimensions.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/edit-circle-1.png', alt: 'Click circle to show radius handle', caption: 'To resize the circle, click on it, and a radius edit point will appear. Drag it to adjust the size of the circle.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/edit-circle-2.png', alt: 'Click outside to apply dimensions', caption: 'Once you\'re satisfied with the new size, click anywhere outside the circle to apply the current dimensions.' },
]} />

**Moving circle**

<Aside type="note">
Don't forget to enable the "**Move**" tool in the circle settings under the "**Edit circle**" section.
</Aside>

To move a circle, click on it, hold down the mouse button, and drag circle to the new location.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/move-circle-1.png', alt: 'Drag circle to new location', caption: 'To move a circle, click on it, hold down the mouse button, and drag circle to the new location.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/move-circle-2.png', alt: 'Circle moved to new position', caption: 'To move a circle, click on it, hold down the mouse button, and drag circle to the new location.' },
]} />

**Removing circle**

<Aside type="note">
Don't forget to enable the "**Remove**" tool in the circle settings under the "**Edit polygon**" section.
</Aside>

To delete a circle, simply click on it, then click the "trash bin" icon in the menu at the bottom of the widget.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/remove-circle-1.png', alt: 'Remove circle using trash bin icon', caption: 'To delete a circle, simply click on it, then click the "trash bin" icon in the menu at the bottom of the widget.' },
]} />

## Additional datasources

**Additional datasources** are auxiliary data sources that can be used for processing, filtering, or overlaying data on the main entities.
They are often used in map overlay features or tooltips when additional context or information needs to be provided.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/additional-datasources-1.png', alt: 'Additional datasources configuration', caption: 'Additional datasources are auxiliary data sources that can be used for processing, filtering, or overlaying data on the main entities. They are often used in map overlay features or tooltips when additional context or information needs to be provided.' },
]} />

## Map controls

**Map controls** are settings for configuring map control buttons within the widget. They allow you to:
- Specify the **position of control elements** (e.g., top-left, bottom-right, etc.);
- Configure **zoom** interactions (via buttons, mouse wheel, or double-click);
- Choose the zoom **scale units**: metric or imperial.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/map-controls-1.png', alt: 'Map controls settings', caption: 'Enable the "Remove" tool in the circle settings under the "Edit circle" section.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/map-controls-2.png', alt: 'Map controls result', caption: 'To delete a circle, simply click on it, then click the "trash bin" icon in the menu at the bottom of the widget.' },
]} />

## Map action buttons

**Map action buttons** are interactive buttons displayed on top of the map that trigger predefined actions.
Learn more about the available action types and how to configure them in the <DocLink product={props.product} path="user-guide/ui/widget-actions">widget actions</DocLink> documentation.

Let's consider an example of adding an action button that will allow users to place new objects on the map.

<Steps>
1. In the "**Map action buttons**" section, click "**Add button**".
2. Specify a name for the new button — for our example, "Add building" — and set its icon and color.
3. Now click the "**Action**" field to define the action, and instead of the default "Do nothing", select "**Place map item**".
4. Select the map item type to be placed — in this case, it's "Marker".
5. The custom action function field contains a default function that displays a dialog for creating a device or an asset — exactly what we need.
6. Save the changes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/map-action-buttons-1.png', alt: 'Click Add button in Map action buttons', caption: 'In the "Map action buttons" section, click "Add button".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/map-action-buttons-2.png', alt: 'Configure button name, icon and action', caption: 'Specify a name for the new button — for our example, "Add building" — and set its icon and color. Click the "Action" field to define the action, and instead of the default "Do nothing"' },
  { src: '/src/assets/images/reference/widgets/map-widgets/map-action-buttons-3.png', alt: 'Select Place map item action', caption: 'Choose the "Place map item" action.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/map-action-buttons-4.png', alt: 'Select marker type and review function', caption: 'Select the map item type to be placed — in this case, it\'s "Marker". The custom action function field contains a default function that displays a dialog for creating a device or an asset — exactly what we need. Then, click "Add".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/map-action-buttons-5.png', alt: 'Save action button changes', caption: 'Save the changes.' },
]} />

To ensure that newly created entities are immediately displayed on the map, you need to properly configure the data source.

<Steps>
1. In the "**Overlays**" section, under the "**Marker**" tab, create a new entity alias as the data source. Name it "**buildings**".
2. Since I'll be creating assets with the asset type "**buildings**", set the filter type to "**Asset type**" and specify the type as "**buildings**".
3. Apply all changes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/map-action-buttons-6.png', alt: 'Create buildings entity alias', caption: 'In the "Overlays" section, under the "Marker" tab, create a new entity alias as the data source. Name it "buildings".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/map-action-buttons-7.png', alt: 'Set Asset type filter for buildings', caption: 'Since I\'ll be creating assets with the asset type "buildings", set the filter type to "Asset type" and specify the type as "buildings". Apply all changes.' },
]} />

Now, the action button labeled "Add building" has appeared at the top of the map.

<Steps>
1. Find the location on the map where you want to place the entity and click on it.
2. In the "Add entity" dialog window that appears, make sure to **enter a name for the new entity** and **select its type — Asset**. Other fields are optional.
3. Then, click "**Create**".
</Steps>

The new marker is now added to the map.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/map-action-buttons-8.png', alt: 'Click Add building button and place entity', caption: 'Now, the action button labeled "Add building" has appeared at the top of the map. Find the location on the map where you want to place the entity and click on it.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/map-action-buttons-9.png', alt: 'Add entity dialog', caption: 'In the "Add entity" dialog window that appears, make sure to enter a name for the new entity and select its type — Asset. Other fields are optional. Then, click "Create".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/map-action-buttons-10.png', alt: 'New marker added to map', caption: 'The new marker is now added to the map.' },
]} />

Go to the "**Assets**" page — here you'll find your newly created asset, "Building A".

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/map-action-buttons-11.png', alt: 'Building A asset on Assets page', caption: 'Go to the "Assets" page — here you\'ll find your newly created asset, "Building A".' },
]} />

## Common map settings

**Common map settings** are the basic global settings for the Map widget in ThingsBoard. They define how the map behaves on load and what is displayed initially.

- **Fit map bounds to cover all markers**: Automatically adjusts the map zoom and position so that all markers are visible when the widget loads.
- **Default map center position**: Sets the center of the map (lat/lng coordinates) if auto-zoom is disabled. Used as the initial map position when the widget is opened.
- **Default map zoom level**: Sets the default zoom level (e.g., 7 for country-level view, 15 for street-level view).
- **Limit of entities to load**: Restricts the number of entities loaded onto the map. This helps protect performance when working with large datasets (for example, a limit of 100 will prevent more than 100 markers from loading at once).

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/common-map-settings-1.png', alt: 'Common map settings panel', caption: 'Common map settings are the basic global settings for the Map widget in ThingsBoard. They define how the map behaves on load and what is displayed initially.' },
]} />

## Appearance

Customize the widget's appearance.

- **Title** -- Sets the name of the widget that appears at the top of the map. This helps identify the map on the dashboard (e.g., "Vehicle Tracking", "Sensor Locations").
- **Card icon** -- Defines the icon displayed in the widget header (next to the title). It can be used to visually indicate the map's context or theme (e.g., GPS, map, transportation icons).

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/appearance-1.png', alt: 'Widget appearance settings' },
]} />

## Card appearance

**Card appearance** section controls the visual appearance of the widget on the dashboard.

- **Background**: Sets the background color of the card.
- **Show card buttons**: Enables or disables the edit and fullscreen buttons in the top-right corner.
- **Card border radius**: Defines the corner rounding of the card.
- **Card padding**: Sets the inner spacing between the map content and the card edges.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/card-appearance-1.png', alt: 'Card appearance settings' },
]} />

## Actions

You can add buttons to the widget header that will trigger predefined actions. You can learn more about the available action types in the <DocLink product={props.product} path="user-guide/ui/widget-actions">widget actions</DocLink> documentation.

As an example, let's add a button that allows users to place charging stations on the map:

To start, and to ensure that newly created entities are instantly displayed on the map, you need to properly configure the data source.

<Steps>
1. In the "**Overlays**" section, under the "**Marker**" tab, create a new entity alias as the data source. Name it "**EV stations**".
2. Since we'll be creating entities of the type "**EV station**", set the filter type to "**Asset type**" and specify the type as "**EV station**".
3. Apply changes.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/data-source-for-action-1.png', alt: 'Create EV stations entity alias', caption: 'In the "Overlays" section, under the "Marker" tab, create a new entity alias as the data source. Name it "EV stations".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/data-source-for-action-2.png', alt: 'Set Asset type filter for EV station', caption: 'Since we\'ll be creating entities of the type "EV station", set the filter type to "Asset type" and specify the type as "EV station". Apply changes.' },
]} />

Now let's move on to configuring the action:

<Steps>
1. Scroll down to the "**Actions**" section and click the "**Add action**" button.
2. A new window will open, displaying all your created actions. Click the "plus" icon button in the top-right corner to add one.
3. Select the action source as "**Widget header button**", enter a name for the button, and choose its type.
4. Optionally, change the icon that will appear next to the button or hide it entirely. You can also set a custom color for the button.
5. From the dropdown menu, select the type of action to be performed. In our case, it's "**Place map item**".
6. Next, choose the **type of map item** to be placed — we'll be placing a "**Marker**".
7. The **custom action function** field already contains a default function that opens a dialog for creating a device or an asset — exactly what we need.
8. Then click "**Add**".
9. Apply all changes and save the dashboard.
</Steps>

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/action-1.png', alt: 'Click Add action button', caption: 'Scroll down to the "Actions" section and click the "Add action" button.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/action-2.png', alt: 'Click plus icon to add action', caption: 'A new window will open, displaying all your created actions. Click the "plus" icon button in the top-right corner to add one.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/action-3.png', alt: 'Select Widget header button source', caption: 'Select the action source as "Widget header button", enter a name for the button, and choose its type.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/action-4.png', alt: 'Customize button icon and color', caption: 'Optionally, change the icon that will appear next to the button or hide it entirely. You can also set a custom color for the button.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/action-5.png', alt: 'Select Place map item action type', caption: 'From the dropdown menu, select the type of action to be performed. In our case, it\'s "Place map item". Next, choose the type of map item to be placed — we\'ll be placing a "Marker". The custom action function field already contains a default function that opens a dialog for creating a device or an asset — exactly what we need. Then, click "Add".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/action-6.png', alt: 'Apply all changes', caption: 'Apply all changes and save the dashboard.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/action-7.png', alt: 'Add EV station button on map', caption: 'A new action button labeled "Add EV station" has now appeared at the top of the map.' },
]} />

A new action button labeled "**Add EV station**" has now appeared at the top of the map. Let's use it.

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/action-place-marker-1.png', alt: 'Click Add EV station and place on map', caption: 'Click the newly created "Add EV station" button. Then, find the location on the map where you want to place the EV station and click on it.' },
  { src: '/src/assets/images/reference/widgets/map-widgets/action-place-marker-2.png', alt: 'Add entity dialog for EV station', caption: 'In the "Add entity" dialog that appears, make sure to enter a name for the new entity and select its type — Asset. The other fields are optional. Then click "Create".' },
  { src: '/src/assets/images/reference/widgets/map-widgets/action-place-marker-3.png', alt: 'EV station marker added to map', caption: 'The new "EV station" marker is now added to the map.' },
]} />

Go to the "**Assets**" page — there you'll find your newly created asset, "**EV station 1**".

<MultiProductImageGallery product={props.product} images={[
  { src: '/src/assets/images/reference/widgets/map-widgets/action-created-asset-1.png', alt: 'EV station 1 on Assets page', caption: 'Go to the "Assets" page — there you\'ll find your newly created asset, "EV station 1".' },
]} />
