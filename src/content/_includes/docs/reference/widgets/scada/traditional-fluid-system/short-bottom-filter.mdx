import { Image } from 'astro:assets';
import Filter from './_filter.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-short-bottom-filter.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Short bottom filter SCADA symbol"
  width={100}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '100px', height: '199px', objectFit: 'contain' }}
/>

The **Short bottom filter** widget renders a compact vertical filter symbol with the pipe connection at the bottom.
Use it to represent a filter unit in a fluid system diagram where vertical space is limited.

</div>

<Filter product={props.product}/>
