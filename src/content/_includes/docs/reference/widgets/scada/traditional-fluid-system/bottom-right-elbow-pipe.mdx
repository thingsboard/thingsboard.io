import DocLink from '@components/DocLink.astro';
import PipeBase from './_pipe.mdx';

The **Bottom right elbow pipe** widget represents a 90-degree elbow fitting that connects a vertical pipe from the bottom to a horizontal pipe going rightward.
It shares the same configuration as the <DocLink product={props.product} path="reference/widgets/scada/traditional-fluid-system/horizontal-pipe">Horizontal pipe</DocLink> widget,
using a bottom-right elbow SCADA symbol by default.

<PipeBase product={props.product}/>
