import { Image } from 'astro:assets';
import DrainPipeBase from './_drain-pipe.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-short-right-drain-pipe.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Short right drain pipe SCADA symbol"
  width={199}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '199px', height: '159px', objectFit: 'contain' }}
/>

The **Short right drain pipe** widget represents a short horizontal pipe segment with the drain outlet directed to the lower right.
It indicates leak conditions based on real-time device data.

</div>

<DrainPipeBase product={props.product}/>
