import { Image } from 'astro:assets';
import AnalogWaterLevelMeterBase from './_analog-water-level-meter.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-left-analog-water-level-meter.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Left analog water level meter SCADA symbol"
  width={199}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '199px', height: '199px', objectFit: 'contain' }}
/>

The **Left analog water level meter** widget renders an analog fill-level gauge with the scale and label positioned on the left side of the symbol.
Use it to show the current fill level of a tank or vessel where the meter face should read from the left.

</div>

<AnalogWaterLevelMeterBase product={props.product}/>
