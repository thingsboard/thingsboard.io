import { Image } from 'astro:assets';
import FlowMeterBase from './_flow-meter.mdx';
import symbolImg from '~/assets/images/reference/widgets/scada/traditional-fluid-system/symbol-vertical-inline-flow-meter.png';

<div style={{ overflow: 'auto', marginBottom: '1.5rem' }}>
<Image
  src={symbolImg}
  alt="Vertical inline flow meter SCADA symbol"
  width={100}
  style={{ float: 'left', marginRight: '1.25rem', marginBottom: '0.5rem', width: '100px', height: '199px', objectFit: 'contain' }}
/>

The **Vertical inline flow meter** widget displays a numeric flow rate value on a rectangular display face embedded directly within a vertical pipe run.
Use it where the meter is installed in-line with the pipe, with flow passing through the meter body.

</div>

<FlowMeterBase product={props.product}/>
