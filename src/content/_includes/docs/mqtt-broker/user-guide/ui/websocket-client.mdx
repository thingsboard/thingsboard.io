import ImageGallery from '~/components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';
import { Products } from '~/models/site.models';

The TBMQ WebSocket Client is a browser-accessible tool for debugging and testing MQTT clients.
Built on <DocLink product={props.product} path="user-guide/mqtt-over-ws">MQTT over WebSocket</DocLink> and the [MQTT.js](https://github.com/mqttjs/MQTT.js) library, it provides seamless management of MQTT connections, topic subscriptions, and message publishing/receiving.

{props.product === Products.TBMQ_PE ? (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/mqtt-broker/pe/user-guide/ui/ws-overview.png', alt: 'TBMQ WebSocket Client overview' },
  ]} />
) : (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-overview.png', alt: 'TBMQ WebSocket Client overview' },
  ]} />
)}

## Connections

The WebSocket Client supports multiple simultaneous MQTT connections.

### Add connection

1. Go to the **WebSocket Client** page and click **Select Connection** (expand more icon).
2. Click **Add new connection**, fill in the required fields, then click **Connect**.

<ImageGallery images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-add-1.png', alt: 'Open Select Connection panel and click Connect' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-add-2.png', alt: 'Click Add new connection button' },
]} />

### Edit connection

Connection parameters can be changed even after a connection is established.
If the client is connected when changes are saved, TBMQ first disconnects the session, then reconnects with the updated settings.

1. Go to the **WebSocket Client** page and expand the connections list (arrow icon).
2. Click the edit icon on the connection you want to change.
3. Make your changes and click **Connect** to save.

<ImageGallery images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-edit-1.png', alt: 'Expand connections list and click edit icon' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-edit-2.png', alt: 'Make changes and click Connect to save' },
]} />

### Delete connection

1. Expand the connections list (arrow icon).
2. Click the trash icon on the connection you want to delete.
3. Click **Yes** to confirm.

<ImageGallery images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-delete-1.png', alt: 'Expand connections list and click trash icon' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-delete-2.png', alt: 'Confirm deletion by clicking Yes' },
]} />

### Connection settings

#### Connection details

- **Name** — connection name (must be unique).
- **URL** — WebSocket URL of the broker, e.g., `ws://localhost:8084/mqtt`.
- **Authentication** — three options:
  - **Auto-generated credentials** — random Client ID and Username, empty Password. Corresponding credentials are created automatically.
  - **Custom authentication** — specify a custom Client ID, Username, and Password.
  - **Use existing credentials** — select existing [Basic](<DocLink product={props.product} path="security/authentication/basic" />) credentials. Password field appears when the selected credentials require one.

<ImageGallery images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-details-1.png', alt: 'Connection details — auto-generated credentials' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-details-2.png', alt: 'Connection details — custom Client ID, Username, Password' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-details-3.png', alt: 'Connection details — use existing credentials' },
]} />

#### Advanced settings

- **Clean start** — when true, the broker discards any previous session and starts fresh.
- **Keep alive** — idle time the client waits between control packets before the connection is considered lost.
- **Connect timeout** — maximum wait time for a CONNACK acknowledgement.
- **Reconnect period** — interval between reconnection attempts after a disconnection.
- **MQTT version** — MQTT 3.1 (3), 3.1.1 (4), or 5.0 (5).

MQTT 5 adds:

- **Session Expiry Interval** — duration (seconds) the broker keeps the session after the client disconnects.
- **Max Packet Size** — maximum payload size (bytes) the client accepts from the broker.
- **Topic Alias Maximum** — highest Topic Alias value the client accepts.
- **Receive Maximum** — maximum concurrent QoS 1/2 messages.
- **Request Response Information** — when true, the broker returns response information for publish operations.

<ImageGallery images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-advanced-1.png', alt: 'Advanced connection settings' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-advanced-2.png', alt: 'Advanced settings available for MQTT 5' },
]} />

#### Last will

The Last Will message is published by the broker if the client disconnects ungracefully.

- **Topic** — topic for the Last Will message.
- **QoS** — Quality of Service for the message.
- **Payload** — content of the Last Will message.
- **Retain** — when true, the broker stores the last will message and delivers it to future subscribers.

MQTT 5 adds: Payload Format Indicator, Content Type, Will Delay Interval, Message Expiry Interval, Response Topic, Correlation Data, User Properties.

<ImageGallery images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-will-1.png', alt: 'Last will settings' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-connection-will-2.png', alt: 'Last will user properties available for MQTT 5' },
]} />

### Connection status

Hover over the connection status label to view a history of status changes.

Possible statuses:

1. **Connected** — connection successfully established.
2. **Disconnected** — client has closed the connection.
3. **Reconnecting** — client is re-establishing a connection. Click **Cancel** to stop.
4. **Connection failed** — connection could not be established (includes reason, e.g., authentication failure or session takeover).

{props.product === Products.TBMQ_PE ? (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/mqtt-broker/pe/user-guide/ui/ws-logs.png', alt: 'Connection status history on hover' },
  ]} />
) : (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-logs.png', alt: 'Connection status history on hover' },
  ]} />
)}

## Subscriptions

### Add subscription

1. Click **Add Subscription** (plus icon).
2. Fill in the required fields and click **Add**.

<ImageGallery images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-subscription-add-1.png', alt: 'Click Add Subscription button' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-subscription-add-2.png', alt: 'Fill in subscription details and click Add' },
]} />

### Edit subscription

Click the edit icon on a subscription row, make changes, then click **Save**.

If the client is connected, TBMQ unsubscribes from the existing topic before subscribing to the updated one.

<ImageGallery images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-subscription-edit-1.png', alt: 'Click edit icon on the subscription row' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-subscription-edit-2.png', alt: 'Make changes and click Save' },
]} />

### Delete subscription

1. Click the delete icon on a subscription row.
2. Click **Yes** to confirm.

<ImageGallery images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-subscription-delete-1.png', alt: 'Click delete icon on the subscription row' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-subscription-delete-2.png', alt: 'Confirm deletion by clicking Yes' },
]} />

### Subscription settings

- **Topic filter** — MQTT topic filter. **Must be unique per connection.**
- **QoS** — Quality of Service of the subscription.
- **Retain as Published** — when true, forwarded messages keep the RETAIN flag they were published with.
- **Retain Handling** — `0` send at subscription time; `1` send only if subscription doesn't exist; `2` do not send.
- **No local** — when true, broker won't forward this client's messages back to this connection.
- **Color** — used to differentiate messages in the messages table.
- **Subscription Identifier** — unique numeric identifier (greater than 0) for tracking the subscription.

<ImageGallery images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-subscription-overview-1.png', alt: 'Subscription settings with MQTT 5 options' },
]} />

## Messages

The Messages table displays the most recent published and received messages. Each row shows:

1. **Type** — Received or Published.
2. **Topic** — the message topic.
3. **QoS** — Quality of Service level.
4. **Retain** — whether the message has the Retain flag set.
5. **Payload** — preview of the message content. Click the **Info** icon to view the full payload and copy it.
6. **Properties** — click to view additional MQTT 5 properties: User properties, Content type, Topic Alias, etc.

<ImageGallery images={[
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-table-msg-1.png', alt: 'Messages table with payload and properties access' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-table-msg-2.png', alt: 'Message payload detail view' },
  { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-table-msg-3.png', alt: 'Message properties detail view' },
]} />

### Filter messages

- **By type** (All/Received/Published) — click the type label in the Messages table header.
- **By Topic/QoS/Retain** — click the **filter** icon next to the **Clear messages** button.

{props.product === Products.TBMQ_PE ? (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/mqtt-broker/pe/user-guide/ui/ws-table-filter.png', alt: 'Messages table filter options' },
  ]} />
) : (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-table-filter.png', alt: 'Messages table filter options' },
  ]} />
)}

### Publish a message

To publish a message, the client must be connected and a topic must be specified (unless using Topic Alias). If the JSON format is selected, the payload must be valid JSON.

Basic publish options:

- **Topic** — MQTT topic for the published message.
- **QoS** — `0` (at most once), `1` (at least once), `2` (exactly once).
- **Retain** — when true, the broker stores the last message and delivers it to future subscribers.
- **Color** — marks published messages in the table for easier recognition.
- **Format** — JSON (with validation) or String (no validation).

Click the **Send** icon to publish.

{props.product === Products.TBMQ_PE ? (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/mqtt-broker/pe/user-guide/ui/ws-publish-msg.png', alt: 'WebSocket Client publish message interface' },
  ]} />
) : (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-publish-msg.png', alt: 'WebSocket Client publish message interface' },
  ]} />
)}

For **MQTT 5** clients, click the **Properties** button to configure additional publish options:

- **Payload Format Indicator** — when true, indicates UTF-8 encoded payload; false means unspecified binary.
- **Content Type** — describes the format of the payload.
- **Message Expiry Interval** — lifetime of the message in seconds.
- **Topic Alias** — integer shorthand for the topic (forbidden if Topic Alias Max is 0 on the connection).
- **Correlation Data** — binary identifier for correlating responses.
- **Response Topic** — topic for a response message.
- **User Properties** — key-value metadata attached to the message.

{props.product === Products.TBMQ_PE ? (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/mqtt-broker/pe/user-guide/ui/ws-publish-advanced-params.png', alt: 'MQTT 5 advanced publish properties' },
  ]} />
) : (
  <ImageGallery images={[
    { src: '/src/assets/images/docs/mqtt-broker/user-guide/ui/ws-publish-advanced-params.png', alt: 'MQTT 5 advanced publish properties' },
  ]} />
)}
