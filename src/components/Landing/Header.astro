---
import HeaderContent from './HeaderContent.astro';

interface Props {
	githubLink?: string;
	githubLabel?: string;
	platformQuery?: string;
}

const { platformQuery = '' } = Astro.props;
---

<header class="header">
	<HeaderContent platformQuery={platformQuery} />
</header>
