---
import { Products } from '~/models/site.models';

interface Props {
	product: Products;
	show: Products[];
}

const { product, show } = Astro.props;
const visible = show.includes(product);
---

{visible && <slot />}
