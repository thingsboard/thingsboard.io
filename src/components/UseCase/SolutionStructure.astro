---
import ImageGallery from '../ImageGallery.astro';
import SectionBadge from '../SectionBadge.astro';

interface Props {
	title: string;
	badge?: string;
	shortText: string;
	longText?: string[];
	schemeSrc: string;
	schemeAlt: string;
	schemeCaption?: string;
}

const { title, badge, shortText, longText, schemeSrc, schemeAlt, schemeCaption } = Astro.props;
---

<section class="uc-solution-structure">
	<SectionBadge
		text={badge || 'How it works'}
		color="var(--sl-color-accent)"
		borderColor="var(--sl-color-blue-high)"
	/>
	<h2>{title}</h2>
	{
		(shortText || longText) && (
			<div class="uc-solution-text">
				{shortText && <p set:html={shortText} />}
				{longText?.map((p) => (
					<p set:html={p} />
				))}
			</div>
		)
	}
	<div class="uc-scheme">
		<ImageGallery images={[{ src: schemeSrc, alt: schemeAlt, caption: schemeCaption }]} />
	</div>
</section>

<style lang="scss" is:global>
	@use '../../styles/variables' as *;

	.uc-solution-structure {
		> .section-badge {
			margin-bottom: $spacing-3;
		}

		> h2 {
			@include section-title;
			color: var(--color-text);
			margin: 0 0 $spacing-8;
		}
	}

	.uc-solution-text {
		display: flex;
		flex-direction: column;
		gap: $spacing-4;
		max-width: 928px;

		p {
			@include text-m;
			margin: 0;

			a {
				color: var(--color-primary);
				text-decoration: none;

				&:hover {
					text-decoration: underline;
				}
			}
		}
	}

	.uc-scheme {
		margin: $spacing-12 0;
		text-align: center;

		img {
			max-width: 100%;
			height: auto;
		}
	}
</style>
